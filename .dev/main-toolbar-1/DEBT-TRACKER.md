# DEBT-TRACKER — main-toolbar-1

Technical debt discovered during the Main Toolbar / Asset Browser / Unified Creation project.
Every debt is recorded here when found, assigned to a target batch/phase, and **must be
resolved (status ✅) before the project is declared done** (FINAL-REPORT gate).

**Key:** P0 = blocks correctness/gate · P1 = high · P2 = medium · P3 = low/nice-to-have

| ID | Pri | Found in | Description | Target | Status |
|----|-----|----------|-------------|--------|--------|
| DEC-1 | — | BATCH-01 | **Decision:** `AssetRoots` placed in `Hrot.Editor.AiShared` (not `Hrot.AI.Behaviors`) because the API is keyed by `AssetKind`, which is an editor type the game-side Behaviors assembly must not depend on. §13 "shared editor infra" option. | — | ✅ |
| DEC-2 | — | BATCH-01 | **Decision:** `AssetKind.Scenario` deferred to MTB-P5-T2 (adding it now ripples 127 usages/switches). T1 exposes scenario recipe root via dedicated `ScenariosRecipesRoot`; P5-T2 folds Scenario into `RecipesFor`. **RESOLVED in BATCH-13:** enum added; Scenario folded into `RecipesFor`/`RecipesRelative`, `AssetKindIcons.GetIconKey`, `AssetKindFilterMapping`; `AssetsFor(Scenario)` still throws (no Assets root). | MTB-P5-T2 | ✅ |
| DEC-3 | — | setup | **Decision (SUPERSEDED by DEC-4):** Coder role fulfilled by Agent-tool sub-agent (general-purpose, sonnet). | — | ✅ |
| DEC-4 | — | setup | **Decision:** Coder role fulfilled by the **claude-worker-orchestrator MCP** worker (`start_worker`, model `pro` = deepseek-v4-pro), per user directive. Agent-tool sonnet/opus subagents are NOT used for delegation. | — | ✅ |
| DEC-5 | — | BATCH-02 | **Decision:** MTB-P0-T2 (file moves) + MTB-P0-T3 (repoint) are run as **one batch**, internally ordered T2→T3. Splitting would leave the test suite red between batches (consumers point at old paths after the move), violating the non-negotiable green-gate rule (ORCH §2.1). The "one task per batch for risky moves" default yields to the green-gate rule here. | — | ✅ |
| DEC-6 | — | BATCH-02 | **Decision:** `AssetRoots` (output-dir absolute paths via `AppContext.BaseDirectory`) cannot serve **project-dir** consumers (`EditorSubsystem` recipe-save / quick-reload resolve the source `.csproj` dir via `AiBehaviorsProjectPath` to persist recipes to VCS). To keep `AssetRoots` the §16 single authority, T3 adds **relative-segment helpers** (`AssetsRelative(kind)`/`RecipesRelative(kind)`/`ScenariosRecipesRelative` → `Assets/Blueprints`, `Recipes/Blueprints`, …). Output-dir consumers use the absolute props; project-dir consumers combine their resolved project dir with the relative helper. Segment names live in ONE place. | — | ✅ |
| PRE-1 | P3 | BATCH-02 | **Pre-existing, OUT OF SCOPE (not a gate for this project):** 11 test failures exist at the pre-move baseline `e3bf645a` and are unrelated to main-toolbar: Blueprints.Tests — `AiPrimitive_EmitMatchesGoldenSource`(×2), `MoveToAndFire_GeneratedSource_Snapshot` (stale StructureHash goldens), `Stage8_PdbContainsEmbeddedSource` + `Stage8_RoslynCompiler_ProducesNonEmptyPeAndPdb` (CS0103 'self' compiler regression), `CF2_EndToEnd_DelayBreakpointPauses` + `CF7rev_…ThenPauses` (breakpoint-ID-drift), `AllocationFreeTests.TickFrame_1000Frames_AllocatesZeroBytes` + `WhenNode_ZeroAllocOnHotPath` (env-sensitive alloc); Generators.Tests — 2 `MigrationEquivalence` byte-stability round-trips. Verified identical at baseline (worktree run). These belong to compiler/breakpoint/perf workstreams, NOT main-toolbar — do not block this project's FINAL gate. | — | noted |
| DBT-2 | P1 | BATCH-16 | **Wiring gap (must resolve before FINAL):** `AssetPickActionRouter` (BATCH-16) + the browser hosts `AssetPickerModal`/`AssetBrowserDockedWindow` (BATCH-15) + `SaveAsDialog` UI (BATCH-20) are implemented and unit-tested but NOT yet instantiated/registered at a production composition point — nothing in the editor surfaces the asset browser, supplies `router.Route` as the host callback, or renders the Save-As dialog's ImGui name/folder picker popup yet. Surfacing the browser and dialogs is a **Phase 7** concern (Workspace dynamic submenu / Scenario menu open the browser / Save-As UI). T6's named success-condition tests (Route → Open/LoadScenarioByName, SaveAs_WritesNewFile, etc.) pass; this debt tracks the remaining production glue. | Phase 7 (MTB-P7-T1/T3) | open |
| DBT-1 | P3 | BATCH-03 | **In-scope debt:** the 15 new §5.1 icon keys were mapped to available `SilkIconProvider` atlas cells without auditing the actual icon art at each cell (some cells reused, e.g. `c8` for `browser/open`+`folder`). Key→cell→UV pipeline is correct; visual correctness needs review when the toolbar UI is actually rendered. | Phase 3 (when toolbar groups render) / Phase 4 (row icons) | open |
| PRE-2 | P3 | BATCH-03 | **Pre-existing, OUT OF SCOPE:** ~19 `Fdp.Presentation.Tests` failures verified at baseline `bb68fb00` — Vis2D `DebugGizmoLayer*`/`DebugPrimitiveRenderer2D*` NREs (15), `EntityInspectorPanelTests` (3), `EventBrowserPanelTests` (1). Also: running the FULL `Fdp.Presentation.Tests` suite **deadlocks** (an NRE thrown inside the ImGui-fixture `SemaphoreSlim` is not released, hanging subsequent ImGui tests) — run with a Vis2D exclusion or per-class. Belongs to Vis2D/test-infra workstream, NOT main-toolbar; do not block this project's FINAL gate. | — | noted |
| PRE-3 | P3 | BATCH-06 | **Pre-existing, OUT OF SCOPE:** `Hrot.SimHost.Tests` `EqsModuleTests` ("EditablePolyline not registered") fails nondeterministically under certain test-ordering/parallelization; passes in isolation and on clean re-runs (585/0). EQS module-registration test-isolation leak — unrelated to main-toolbar (BATCH-06 touches no EQS/SimHost code). Belongs to test-infra/EQS workstream; not a gate for this project. | — | noted |
| PRE-4 | P3 | BATCH-07 | **Pre-existing, OUT OF SCOPE:** `Hrot.Presentation.Tests` `RouteWaypointGizmoTests.OnCommit_WritesBackToEcs` fails nondeterministically under certain test-ordering; passes in class-filtered/isolated runs. Test-isolation leak (shared ECS state) — unrelated to main-toolbar (BATCH-07 touches only time-control panels). Same family as PRE-3. Not a gate for this project. | — | noted |
| DEC-12 | — | BATCH-18 | **Decision (INewAssetService persist asymmetry):** per §18.3 the BTree/HSM/Scenario `INewAssetService` impls **mint + persist** (write JSON via `BTreeJsonServices`/`HsmJsonServices`, or route to `IEditorLogic` for Scenario) — their success conditions assert written-JSON round-trip. The Blueprint impl (MTB-P6-T2) is **mint-only** (its write is handled by the New Asset dialog/subfolder-save path). The New Asset dialog (MTB-P6-T5) reconciles: it calls `CreateNew` and, for the blueprint kind, performs the explicit subfolder-aware save (MTB-P6-T7); for BTree/HSM/Scenario the persist already happened in `CreateNew`. Documented so T5 handles the per-kind save correctly. | MTB-P6-T5 | open |
| DEC-11 | — | BATCH-17 | **Decision (layering vs §17 "lift RecipeMetadata"):** the blueprint asset's `RecipeMetadata` lives on the JSON model in `Hrot.Blueprints.Compiler`, which **multi-targets netstandard2.0** (the source generator) — it CANNOT reference net8.0 `Hrot.Editor.AiShared`, and no neutral netstandard2.0 contracts assembly exists that both share. So: define the shared kind-agnostic `RecipeMetadata` (DisplayName/Category/Description/Difficulty/ConceptsTaught) in **net8.0 `Hrot.Editor.AiShared`** for the unified creation flow (INewAssetService + dialogs + BTree/HSM/Scenario). The blueprint **editor** code (net8.0, e.g. NewFromRecipeService/RecipeCreateModal) adapts to the shared type, **mapping** from the Compiler JSON-model `RecipeMetadata` when reading `BlueprintAsset.EditorMetadata.Recipe`. The Compiler serialization model keeps its own type (unchanged). Accepts a small field-identical duplication forced by the netstandard2.0 boundary. | — | ✅ |
| DEC-10 | — | BATCH-13 | **Decision (Phase-5 ordering):** run **MTB-P5-T2 before MTB-P5-T1**. T1's `ReferenceCatalog.OnCatalogChanged` must skip `AssetKind.Scenario`, but that enum value is introduced by T2 — so T2 (add `AssetKind.Scenario` + `ScenarioCatalogContributor`) lands first (BATCH-13), then T1 (typed `Changed` event + Scenario-skip, BATCH-14). Permitted by ORCH §3 (order dependent tasks within a phase). | — | ✅ |
| DEC-9 | — | BATCH-20 | **Decision (cross-phase):** the full **Save-As dialog** is MTB-P6-T6 (Phase 6). In MTB-P2-T4 (BATCH-06), the `SaveAs` command and the "empty SourceFilePath → Save-As" routing target a **seam** — a `requestSaveAs(AiDocument)` callback the registrar invokes. **RESOLVED in BATCH-20:** `SaveAsDialog` model created (mirrors `NewAssetDialog`), `ShellSaveCommands.requestSaveAs` seam now creates a seeded `SaveAsDialog` and calls `Confirm()` (fresh AssetId minting via `INewAssetService.CreateNew`). Production `EditorSubsystem.cs` wires the full service registry and delegates. **Remaining:** ImGui UI rendering of the dialog (name input, folder picker popup) deferred to Phase 7 / DBT-2. | MTB-P6-T7 / DBT-2 | ✅ |
| DEC-8 | — | BATCH-02 | **Decision (success-condition vs reality):** The MTB-P0-T2 condition `FolderLayoutTests.Output_HasAssetsAndRecipesRoots` is premised on finals shipping to **output** under `Assets/<Kind>`. Code reality: final `.bp.json/.hsm.json/.btree.json` are generator `AdditionalFiles` compiled into the assembly and (unchanged by this batch) NOT copied to build output; the editor scans finals from the resolved **source project** dir, recipes are discovered from **output** (`Content` copy). So `FolderLayoutTests` is added to assert the achievable invariants: (a) recipes ship to **output** `Recipes/Blueprints`; (b) the **source** Behaviors project has the §16 `Assets/{Blueprints,HSMs,BTrees}` + `Recipes/Blueprints` trees populated; (c) no `*.bp.json` directly under a bare `Blueprints/`. Closes the missing named test without asserting a falsehood. | BATCH-02 corrective | ✅ |
| DEC-7 | — | BATCH-02 | **Decision:** Tests with hardcoded pre-move paths (`RecipeIntegrityTests`, `SampleGuardDiscoveryTests`, `SampleScoutDiscoveryTests`, `MigrationEquivalenceTests`, `CF*_*Tests` Count4.bp.json, `FixedStringPinTests`, `WhenNodeEditorSmokeTest`) must be repointed to the new layout as part of T3. Updating a test's path to track a moved fixture is NOT test-weakening; deleting/skipping assertions would be. | — | ✅ |
