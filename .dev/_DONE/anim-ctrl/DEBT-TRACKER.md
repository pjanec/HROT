# DEBT-TRACKER — anim-ctrl

> P2/P3 deferred issues. P1 issues go directly into the next batch (never here).
>
> Add a row when implementation surfaces a deferrable issue. Reference the
> source (a DD section, a task ID, or a review file) and a target batch/phase.
> See [TASK-DETAIL.md](./TASK-DETAIL.md) for tasks and the DD-* documents for design.

| # | Priority | Source | Description | Target Batch |
|---|----------|--------|-------------|--------------|
| D-01 | P3 | DD-3 §3/§9.7 (architect ruling) | DD-3 doc body still allocates animation events to the `8000–8099` block (`8001–8013`). That block is **revoked** — `GlobalActionRequestedEvent` holds `[EventId(8059)]`. Implementation uses `8200–8299` (`8201–8213`) per ANC-P4-01. Reconcile DD-3 §3 event-id literals, §3 attribute examples, and §9.7 with the `8200–8299` block so the design doc matches the code. | DD-3 docs reconciliation |
| D-02 | P1 | BATCH-01 ANC-P0-08 (Finding 3) | DD-Fake §7.3 names `MuscleCharacterHostSubsystem` as diagnostic window host, but this class does not exist. Real host is `SimHostSubsystem`. P1-09 task description must use correct symbol. | ✅ BATCH-02 P1-09: Placeholder window created; registration target confirmed as `SimHostSubsystem` |
| D-03 | P2 | BATCH-01 ANC-P0-08 (Finding 3) | Confirm `SimHostSubsystem` implements `IWindowRegistrar` interface for diagnostic window registration. | BATCH-03 Phase 2 (full diagnostic window integration) |
| D-04 | P2 | BATCH-01 ANC-P0-08 (Finding 4) | Phase 3 systems (P3-01 through P3-08) must verify correct field names in dispatcher/executor systems: use `Params` and `State`, not `ActionParams` / `ActionState`. | ✅ BATCH-04 ANC-P3-01/02/03/04/05: All systems verified to use correct field names |
| D-05 | P2 | BATCH-02 ANC-P1-07 (Footstep Notify) | Footstep notify emission to pending queue stubbed in Phase 1 (would require arena allocator for event pooling). Full implementation deferred to later phase. | BATCH-03+ Phase 2+ |
| D-06 | P3 | BATCH-02 ANC-P1-09 (ImGui Window) | ~~Diagnostic ImGui inspection panel created as placeholder; visual rendering and registration deferred~~ **Visual rendering implemented in D-27** (`DrawClientArea` renders list+detail tabs over `FakeAnimBackendState`). Window-manager wiring on `SimHostSubsystem.RegisterWindows` still pending. | ✅ Visual done; registration tracked in D-03 |
| ~~D-07~~ | P2 | BATCH-03 REVIEW P2-A | Hashing tests verify determinism and collision resistance but lack known-value vectors. **Fixed** — pinned FNV-1a32/FNV-1a64 canonical reference vectors added to `Phase2DescriptorTests` (`StableIdHasher_ComputeMarkerHash_MatchesCanonicalFnv1a32_Vectors`, `StableIdHasher_ComputeMontageAssetId_MatchesCanonicalFnv1a64_Vectors_Masked`). | ✅ Fixed |
| ~~D-08~~ | P2 | BATCH-03 REVIEW P2-B | Baking tests do not verify NotifyInfo.Kind lookup from CharacterAnimationDefDto.NotifyMarkers registry. **Fixed** — verified the code already populates Kind correctly (lines 202–219 of `BakedAnimationDef.cs`); added two tests (`BakingUtils_BakeDef_PopulatesNotifyInfoKind_FromNotifyMarkersRegistry`, `BakingUtils_BakeDef_NotifyKindDefaultsToGeneric_WhenMarkerMissingFromRegistry`). | ✅ Fixed |
| D-09 | P2 | BATCH-04 REVIEW (Weak) | StagedPlayIntent reuses first 20 bytes of AnimationExecutorState.SlotsData for staging buffer. Type-unsafe, but works. Refactor to dedicated field if component restructured to maintain clarity. | BATCH-06+ |
| D-10 | P2 | BATCH-04 REVIEW (Weak) | LookAtExecutorState.TargetPointX uses bit-cast to store entity ID when TargetType==2. Type-unsafe. Consider explicit TargetEntityId field if component layout allows. | BATCH-06+ |
| D-11 | P2 | BATCH-04 REVIEW (Blocker) | PlayMontageQueueExecutor is a stub; queue entry sequencing not implemented. ANC-P5-02 depends on full implementation. | BATCH-05 Phase 3 Part 2 |
| D-12 | P2 | BATCH-04 REVIEW (Potential) | StanceTransitionSystem starts transitions but doesn't poll for backend completion. May need polling step for real (non-fake) backend. | BATCH-05/06 |
| ~~D-13~~ | P3 | BATCH-04 REVIEW (Design) | BakedAnimationCache ignores `CharacterAnimationDefDto.NotifyMarkers[].Hash` field. **Resolved** — intent is **informational only** (kept for editor display of precomputed hashes when import pipeline provides them); the runtime always uses `StableIdHasher.ComputeMarkerHash(name)` for `AnimNotifyEvent.MarkerHash` routing. Documented in code (`BakedAnimationDef.cs` step 2) and locked by `BakingUtils_BakeDef_TreatsNotifyMarkerDtoHash_AsInformationalOnly` test. | ✅ Resolved (informational) |
| D-14 | P2 | BATCH-09 REPORT (Deferred) | ANC-P5-07 — AiPrimitive registration + cross-subsystem reuse (BTree, HSM, Blueprint). Deferred due to Blueprint RCU protocol complexity. Requires focused design session on dispatcher patterns. Estimated effort: 6–8 hours. | BATCH-10 |
| ~~D-15~~ | P2 | BATCH-09 REPORT (Deferred) | ~~ANC-P5-08 — PlayMontageChainNode custom drawer (editor). Editor team ownership; out-of-scope for runtime development.~~ **Un-deferred** — now scheduled with a concrete plan in TASK-DETAIL [Addendum A](./TASK-DETAIL.md#addendum-a--anc-p5-08-implementation-plan-playmontagechainnode-custom-drawer) (sub-tasks ANC-P5-08a…d). | Superseded by Addendum A |
| D-16 | P3 | BATCH-09 REPORT (Infrastructure) | Parameter serialization in AnimationTestHelpers.cs uses temporary byte[] + Array.Copy workaround (fixed buffer cannot be cast to Span<T> in safe context). Acceptable for tests; minor perf cost. Defer infrastructure refactoring to phase 2+. | Phase 2+ infrastructure review |
| D-17 | P2 | BATCH-10 REPORT (Type Safety) | ComputeStructureHash uses simple type name + size hash; doesnt detect field reordering. Field layout change would not be caught at registration. Mitigation: Phase 7 tests validate layouts. | Phase 6 pre-replication |
| D-18 | P2 | BATCH-10 REPORT (Type Design) | PlayMontageChainNode contains managed int[] array, blocking serialization for cross-context dispatch. May need InlineArray if Phase 7 needs dispatch. Not blocking (drawer deferred). | Phase 6+ conditional |
| D-19 | P3 | BATCH-11 REVIEW (Infrastructure) | AnimationRuntimeBridgeSystem._entityClassIds dictionary grows unboundedly on ResetWorld() calls (entities destroyed, but packed handles never cleaned). Acceptable (GC mitigates). Recommend exposing FlushEntityState(Entity) method or resetting system state in future phases. | Phase 7+ infrastructure |
| ~~D-20~~ | P3 | BATCH-11 REVIEW (Enhancement) | Add `InternalsVisibleTo` for `BakingUtils.BakeForTest`. **Fixed** — added `<InternalsVisibleTo>` items for `Hrot.Animation.Integration.Tests` and `Hrot.MuscleCharacter.Animation.Tests` to `Hrot.MuscleCharacter.Animation.csproj`. | ✅ Fixed |
| ~~D-21~~ | P2 | BATCH-11 REVIEW (Safety) | Detect zero `MontageId` in `PlayMontageExecutor.OnEnter` to catch vacuous test passes. **Fixed** — `Debug.Assert(p.MontageId != 0, …)` plus explicit `Status=Failure` on zero so the channel surfaces the bug rather than passing silently. | ✅ Fixed |
| D-22 | P3 | BATCH-12 REVIEW (Docs gap) | IAnimationBackend.DrainNotifies(handle, span) and GetCurrentStance added post-hoc without DD-Fake update. DD-Fake §7.2 should reflect the span overload and GetCurrentStance purpose. | Post-Phase 7 docs update |
| D-23 | P3 | BATCH-12 REVIEW (Perf) | AnimationStateReporterSystem polls GetCurrentStance every frame while Phase==Transitioning. Currently harmless (guard on Transitioning). Flag for review if polling cost becomes measurable in Phase 8. | Phase 8 pre-perf review |
| D-24 | P2 | Reviewer audit v239 (Phase 6) | DD-2 replication needs `[DdsCase]` arms + DDS payload structs (`EntityDescriptorUnion`) for the eight animation descriptors. Enum ordinals added (100–107 in `EDescriptorType`) but the corresponding union arms / IDL types are not yet emitted. Without them, the `EntityDescriptorUnion` discriminator can be set to the new values but no payload is carried. | BATCH following Phase 6 wiring |
| ~~D-25~~ | P2 | Architect ruling v241 (concession) | **Resolved** — architect formally revoked the `Input`-phase mandate for dispatchers and approved them remaining in `SystemPhase.Simulation`, matching the canonical `LocomotionDispatcherSystem` / `WeaponDispatcherSystem` precedent. Registration order in `AnimationMuscleModule` already guarantees execution before the bridge/queue-advance systems. Cross-phase `[UpdateBefore]` attributes from v240 cleaned up. | ✅ Resolved (concession) |
| ~~D-26~~ | P2 | Architect ruling v241 (mea culpa rollback) | **Resolved** — architect retracted the v240 consolidation mandate after recognising the cross-assembly dependency flaw (D-32). Final shape: <br>• `CognitiveInterruptSystem` restored in `FDP.Toolkit.Behavior.Systems` (now `public` for cross-assembly `typeof` reference), owns first-frame `PreviousCapabilities` init + `CanMove` → blackboard interrupt + canonical `PreviousCapabilities` mutation. Phase: `Input`. Registered back into `CognitiveRuntimeModule.SimulationSystems`. <br>• `AnimationCapabilityChangeReactorSystem` restored in the animation project, owns only `CanPlayAnimations` / `CanAim` transitions, **does not mutate** `PreviousCapabilities`. Phase: `Input`. Carries `[UpdateBefore(typeof(CognitiveInterruptSystem))]` to enforce read-before-mutate ordering and prevent the data race. | ✅ Resolved (rollback) |
| ~~D-32~~ | P2 | Architect v240 follow-up → **obsolete** | The Brain-only deployment regression no longer exists because `CognitiveInterruptSystem` is restored in `FDP.Toolkits.Behavior` and re-registered by `CognitiveRuntimeModule`. Brain-only nodes regain `Interrupt_MobilityLost` writes and `PreviousCapabilities` mutation without depending on `AnimationMuscleModule`. The architect's v241 mea-culpa explicitly retracted the v240 mandate that created D-32. | ✅ Obsolete (v241 rollback) |
| D-27 | P2 | Reviewer audit v239 (Phase 1) | `FakeAnimBackendInspectorWindow.DrawClientArea` was an empty TODO. **Fixed** — implements DD-Fake §7 list+detail tabs (slots / aim / stance / locomotion / notifies) with JSON snapshot button (§8). | ✅ Fixed |
| D-28 | P2 | Reviewer audit v239 (Phase 5) | `PlayMontageChainNodeDrawer.WriteBackToNode` / `LoadFromNode` were placeholders. **Fixed** — storage-agnostic reflection accessors locate `ChainCount` + `ChainedMontages` on the node directly or via a nested reference-type wrapper; Span-cast write copies the working chain back and zeroes the tail (DEBT D-18 forward-compatible — `int[]` path now; an `[InlineArray(8)]` arm can be added when D-18 lands). | ✅ Fixed |
| D-29 | P2 | Reviewer audit v239 (Phase 6) | `EDescriptorType` ended at `dtEqsResult=96` with no animation entries. **Fixed** — added block 100–107 (`dtAnimationChannelIntent` / `Status`, `dtLookAtChannelIntent` / `Status`, `dtStanceIntent` / `Status`, `dtAnimationMontageQueue` / `State`). DDS union arms remain — see D-24. | ✅ Fixed (ordinals); union arms tracked as D-24 |
| D-30 | P2 | Reviewer audit v239 (Phase 3, recoverable) | `AnimationBackendCleanupSystem` was a no-op pending a `PendingDestroy` component. **Fixed** — switched to the `DestructionOrder` event pattern (matches `LocomotionDispatcherSystem` / `AnimationDispatcherSystem`); no engine change needed. DD-1 §20.5's `PendingDestroy` mention is documented as the original design altitude — actual engine uses lifecycle events. | ✅ Fixed |
| D-31 | P3 | Reviewer audit v239 (Phase 2, REJECTED claim) | Reviewer suspected `AnimationTkbTranslator.Inject` might miss `AnimationExecutorState` / `LookAtExecutorState`. **Verified false** — translator lines 149–158 inject both, with `LookAtExecutorState` correctly conditional on `def.AimConfig != null`. No action. | No action |
| D-24 | P3 | BATCH-13 REPORT (Infrastructure) | `DdsWriter<T>` validation rejects plain non-generated DDS structs at runtime. BATCH-13 added null-participant guard paths to keep tests running; create a shared guideline/helper to prevent future translator authors from accidentally instantiating invalid writer types in test contexts. | Phase 6+ replication infra hardening |
| D-25 | P3 | BATCH-13 REVIEW (Regression safety) | `CycloneNativeEventTranslator` null-safety behavior was changed in shared infrastructure for BATCH-13. Add focused regression tests in `Fdp.Network.Cyclone` to lock constructor/poll/scan behavior for null participant mode. | Phase 6+ network infra tests |
| D-26 | P3 | BATCH-14 REPORT (Type bounds) | `LookAtEntityParams.TargetEntityId` is `uint` while `NetworkEntityMap` IDs are `long`. If net IDs ever exceed `uint.MaxValue`, remap can truncate. Add guard/assertion or migrate field width in a controlled refactor. | Phase 8+ robustness review |
| D-27 | P3 | BATCH-14 REPORT (Protocol discipline) | Queue replication correctness depends on all Brain-side queue mutations bumping `QueueVersion`. No compile-time guard exists; consider helper API/analyzer to enforce bump-on-mutation discipline. | Phase 8+ replication hardening |
| D-28 | P2 | BATCH-15 REPORT (Backend parity) | `StrideAnimationBackend` smoke implementation currently routes all montage plays to slot 0 (no montage metadata slot mapping yet). Add slot-resolution from runtime montage metadata before parity-focused usage. | Post-P8-04 backend parity pass |
| D-29 | P2 | BATCH-15 REPORT (Integration depth) | Current Stride backend smoke layer simulates blend-tree behavior without real Stride SDK surface types. Track explicit integration step to bind to real Stride animation pipeline when engine package strategy is finalized. | Engine integration roadmap |
| D-30 | P3 | BATCH-15 REPORT (Marker capacity) | Marker firing mask uses `ulong` (64 markers max per montage). Document/guard overflow behavior if authored marker count exceeds 64 in future assets. | Post-P8 stability hardening |

