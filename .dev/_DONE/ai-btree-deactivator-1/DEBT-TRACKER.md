# DEBT-TRACKER — eqs-2

> P2/P3 deferred issues. P1 issues go directly into the next batch (never here).

| # | Priority | Source | Description | Target Batch |
|---|----------|--------|-------------|--------------|
| D-01 | P3 | DESIGN.md §3.2 | `Action_CreepToAndBeyondSlot` still clears `LocomotionChannel` explicitly on the `Failure` return path after TASK-EQL-006 adds the deactivator. Both paths clear the channel (deactivator fires on Failure too); the in-body clear is redundant. Remove the in-body clear once the deactivator is confirmed stable in integration. | Phase 3 follow-up |
| D-02 | P3 | DESIGN.md §3.4 | When `Action_RequestAreaQuery` is deactivated mid-flight, `CachedEqsRequestId` is reset to -1 but the in-flight area query slot in the solver pool is not cancelled. The slot remains occupied until the solver TTL evicts it. Proper cancellation requires an `AreaQueryBatchHelper.CancelAreaQuery` API that does not yet exist. | Future |
| D-03 | P3 | DESIGN.md §2.5 | `[BTreeDeactivator]` for 3-param bridge actions requires the `TargetAction` string to include the `@0` compound suffix. This is an unintuitive convention. Future work: let the generator accept the plain method name and append the suffix automatically when it detects the target is a bridge method. | Future generator polish |
| D-04 | P2 | DESIGN.md §3.3 | `HillAttackTankNodes.Action_AimAndFireSpecific` calls `ClearWeaponActionIfActive` explicitly before returning `NodeStatus.Success` on the `MaxRounds` path. After TASK-EQL-007, the deactivator also fires on that Success path (because RunningNodeIndex changes). The in-body `ClearWeaponActionIfActive` call on Success is now superseded by the deactivator. Confirm that double-clear (body + deactivator) is idempotent, then remove the in-body call. | Phase 3 follow-up |
| D-05 | P2 | DESIGN.md §1.5 L-02 | `ExecuteSelector` uses resume semantics (skips children based on RunningNodeIndex). ObserverSelector maps to the same method. Higher-priority condition re-evaluation on each tick is NOT implemented. The "critical case" branch-switch test (L-02) cannot be exercised. Proper ObserverSelector would require `ExecuteObserverSelector` starting from child 0 every tick. | Future (Interpreter refactor) |
| D-06 | P3 | Interpreter.cs | `NodeIndexStack` is never written during execution. Delta path slots 0–7 are always zero; only RunningNodeIndex (slot 8) carries non-zero values. Multi-running-leaf scenarios (future composite types) would need NodeIndexStack maintained. | Future |
| D-07 | P3 | ONBOARDING.md | `dotnet test FastBTree.sln` fails due to missing `Fbt.SourceGen.csproj`. Test command must use `Fbt.Tests.csproj` directly. BATCH-02 instructions corrected. | BATCH-02 (docs fix only) |
| D-08 | P3 | BATCH-02-REVIEW.md | Generator emits no diagnostic when two `[BTreeDeactivator]` methods declare the same `TargetAction` in the same group. Second registration silently overwrites the first. Future: add BHU-018 duplicate deactivator warning. | Future generator polish |
| D-09 | P3 | BATCH-03-REVIEW.md | Assemblies using manual `ActionRegistry.Register(...)` (no `[BTreeAction]` attributes) get no generator output. Deactivators in such assemblies also need manual `RegisterDeactivator(...)` in the same wiring location. The `InsurgentNodes` case was corrected in review; document this pattern in ONBOARDING.md. | Future docs |
| D-10 | P3 | BATCH-04-REVIEW.md | TreeValidator test fixtures use `RawPayloadIndex = -1` for Sequence/Selector nodes (old sentinel from pre-EQL-009). `-1` sets bit 31, making `IsResourceOwning == true` on these test-fixture nodes. Harmless (tests never run Interpreter on those blobs; bounds check in `SweepExitedNode` protects runtime). Fix: change `RawPayloadIndex = -1` to `RawPayloadIndex = 0` in test fixtures in `TreeValidatorTests.cs`. | Future cleanup |
