# DEC — BTree decorator authoring + demo assets

> **Origin:** user (2026-06-14). "I can add nodes that seem like decorators — they should be pills, not standalone nodes. No way to add decorators. Want demo btree assets: one rich render showcase + a set of trivial focused assets for developing authoring + testing compile/run."
> **Companions:** [../DEBT-TRACKER.md](../DEBT-TRACKER.md) · design [../../docs/blueprints/NodeEditor_Extension_NodeAttachments.md](../../../docs/blueprints/NodeEditor_Extension_NodeAttachments.md), [../../docs/blueprints/BTree_Editor_NodeEditor_Host_Design.md](../../../docs/blueprints/BTree_Editor_NodeEditor_Host_Design.md) §6, forward-plan EB-A/EB-B/EB-C.
> **Execution:** lead (opus) writes specs + hard-verifies; coding via sonnet agents. Token-constrained — lead stays lead-only.

## Diagnosis (confirmed in code)

Decorators are designed to render as **pills** (NodeAttachments extension), never free nodes (forward-plan EB-C: "decorators stay attach-to-node, never free nodes"). The render/projection/round-trip/sink substrate is **REAL and works** (`BTreeGraphModel` attachment projection, `AttachmentRenderer`, `BTreeCommandSink.ApplyAddPill`, `DecoratorPillCollapseTests`). **The gap is purely authoring UX:**

1. `NodeCatalogEntry` (NodeEditor core) has **no palette-action field** → picking a decorator entry emits `CreateNode` → standalone pinless decorator node (the user's bug). `BTreeNodeCatalog.MakeDecorator` lists them as plain entries.
2. No "Add Decorator →" node context menu. So **no UI path emits `AddAttachment`**; pills only appear from hand-authored JSON `Pills[]`.
3. Pill `Label` = bare enum, `Glyph` = null (no `↺×3`/`⏲2s`) — forward-plan EB-B.

## VE-DEBT-002 constraint (gates runnable leaves)

`BrainBlackboard` stores params as an untyped `fixed byte` blob — no typed `.Params` field. So the fluent expression-target binding `.Condition(bb => bb.Params, m)` can't be written, and the bare 4-param form needs a whole-blackboard delegate. ⇒ in a `BrainBlackboard` tree you can bind **whole-blackboard (FourParamFull) actions** (`Hrot.AI.Behaviors.Brains.CgfNodes.Action_Wander`) but **NOT** any real `[BTreeCondition]` or DTO-param action. Demo Condition leaves stay unbound/deferred until the VE-DEBT-002 blackboard-DTO machinery lands (BB1-adjacent, out of DEC scope).

## Batches

| ID | Layer | Title | Status |
|---|---|---|---|
| DEC-01 | content | Demo assets: rich render-showcase + 8 trivial focused assets (compile + round-trip) | ✅ DONE — `BTreeRenderShowcase` (all 7 pill types, Selector/Parallel/ObserverSelector/Subtree, bound `Action_Wander`) + `Authoring/T01..T08`. Surfaced + fixed a real emitter bug (see DEC-01b). Conditions deferred (VE-DEBT-002). Compile gate 0 errors; Persistence 129/0 (byte-identity green); BTree.Editor 524/0; Generators 52/54 (2 known pretty-print only). |
| DEC-01b | emitter | **Parallel codegen fix** — `BTreeEmitCore.EmitComposite` omitted the required `int policy` first arg of `BTreeBuilder.Parallel(int, Action<>, ...)` → any tree with a Parallel emitted non-compiling code (CS7036). | ✅ DONE — emit `0` (RequireAll) as leading arg for `methodName == "Parallel"`. Surgical (lead-inline, ~3 lines). SampleScout/CombatShowcase have no Parallel → byte-identity unaffected. (Agent had silently downgraded Parallel→Sequence to pass build; lead reverted + fixed root cause.) Authorable per-node policy = VE-DEBT-009 follow-up. |
| DEC-02 | NodeEditor core | Palette-action `AttachToSelected` on `NodeCatalogEntry` + palette/canvas emits `AddAttachment` on selected node | ✅ DONE — `NodePaletteAction` enum + 2 trailing-optional record params (`PaletteAction`, `AttachmentCategory?`); `CommandBuilder.AddAttachment`; `IdGenerator.NewAttachmentId`; `AttachmentHostPropertyKeys.Kind = "paletteKind"`; picker (`CanvasInput.cs:109`) routes AttachToSelected→`AddAttachment` on the single selected node (StackIndex=existing count, props[Kind]=`entry.Kind.Id`), safe no-op on 0/>1 selection. Additive — Blueprint/HSM/BTree untouched, all hosts build 0 errors. Core.Tests 190/0, UI.Tests 78/0. Picker wiring itself not unit-driveable (ImGui-bound); decision logic covered at CommandBuilder/record level — known gap. |
| DEC-03 | BTree host | Decorator entries `AttachToSelected` + kind→pill in sink (picker path) | ✅ DONE — `BTreeNodeCatalog.MakeDecorator` flips the 7 decorator entries to `AttachToSelected`/`Decorator`; `ApplyAddPill` resolves `NodeType` from `decoratorType` (existing) OR `paletteKind`→`KindIdToNodeType` (picker), safe no-op otherwise, with default params (Repeater=1, Cooldown=1f). Now: select node → picker (Space/Tab) → pick decorator → pill attaches, persists, round-trips. BTree.Editor 531/0, Persistence 129/0 (byte-identity green), Generators 52/54 (2 known only). |
| DEC-03b | NodeEditor core + BTree host | **NEW node-context-menu seam** + BTree "Add Decorator →" right-click submenu + **fix standalone-decorator bug** in the canvas "Add Node…" picker | ✅ DONE — `INodeContextMenuProvider` seam + `IEditorHostServices.NodeContextMenu` (default-null); `ContextMenuItem.Children` (submenus); `RenderItems` recursive renderer; `PaletteEntryExecutor` shared by BOTH pickers (CanvasInput Tab/Space + CanvasRenderer "Add Node…") → decorators can no longer become free nodes; `BTreeNodeContextMenuProvider` adds "Add Decorator →" (7 types) on any node. Right-click a node → Add Decorator → Repeater → pill attaches. Core 192/0, UI 78/0, BTree.Editor 536/0; all 5 hosts build 0 errors. **Codegen verified via `dotnet build-server shutdown` + rebuild** (the build server cached a stale analyzer — see gotcha above; committed Parallel fix is correct, emits `.Parallel(0, par => …)`). |
| DEC-03c / DEC-08 | NodeEditor picker framework | Picker categories + icons | ✅ DONE (DEC-08) — threaded `Category`+`IconKey` through `IPickerSource` (default-impl getters) → `AdaptedItem` → `PickerWindow` `PickerEntry` (were hardcoded null); `PickerItemListHelper` groups the list by category (browse mode) via a testable `ComputeGroupedDisplayOrder` + draws 16px row icons via the real `IIconProvider` (wired through `AiEditorAdapterBundle.SetServices`→`SilkIconProvider`, maps all `bt/*` keys); `BTreeNodePickerSource` returns `CategoryPath`/`IconKey`. Additive. Part D (left category filter panel via `CategoryRoot`) skipped (needs new split-pane layout). Core 195/0, UI 90/0, BTree.Editor 560/0; 6 hosts/demo build clean. (Tab-only-opens-over-empty-canvas remains a minor separate item.) **DEC-08b follow-up:** the BTree picker actually uses `WideLayout` (not `StandardLayout`/`PickerItemListHelper`) — its category sidebar already worked off `Entry.Category`, but icons weren't drawn there and its 2-line row's description line was blank. Fixed: threaded `Description` (new `IPickerSource.GetDescription` default + `AdaptedItem`/`PickerWindow`/`BTreeNodePickerSource`) so the muted 2nd line shows the node description, and added a 24px inline icon to `WideLayout` rows via the real `IIconProvider`. UI 90/0, BTree.Editor 560/0; all hosts+Demo build clean. |
| DEC-04 | NodeEditor core + BTree host | **Pill editing**: click-select (frame), Del-remove (with undo), inspector property editing (Repeater count / Cooldown duration) | ✅ DONE — core: `IAttachmentModel.HostProperties` (additive), `HoverKind.Attachment` click-select in `CanvasInput` (replace/toggle/add), attachment removal in BOTH delete paths with inverse `AddAttachment` (undo restores the pill via `HostProperties`→`ApplyAddPill`), selection outline in `AttachmentRenderer`. Host: `BTreePillSelection` sub-selection; `BTreeSelectionBridgeHelper` maps a selected pill; `BTreeFacetMapper` Build/ApplyPillFacet routes the 7 existing facets (count/duration/comment) with direct asset mutation + `MarkDirty`; `BTreePillAttachmentModel.HostProperties` for undo. Core 195/0, UI 78/0, BTree.Editor 548/0; all 5 hosts build clean. (EB-B glyph/param labels were already rendering — folded away; pill appearance is fine.) |
| DEC-05 | blackboard | (deferred) real condition binding — needs VE-DEBT-002 DTO-field machinery; separate/larger | ⬜ deferred |
| DEC-06 | NodeEditor.UI + BTree host | Pill **undo** (add/remove) + nested-decorator **safety** (validation + prevention) | ✅ DONE — (1) right-click Add Decorator routes through `view.Execute` (recorder wired from the view in `BTreeDocumentFactory`) → undoable; (2) Del consolidated: `CanvasInput` delegates to `EditCommands.DeleteSelectedUndoable` so pill+node delete both undo (no double-delete); (3) `BTreeValidator` detects nested Repeater/Parallel (new `BTreeDiagnosticCode.NestedRepeater/NestedParallel`, Error) walking pills-as-wrappers; (4) prevention: Repeater menu item disabled when host already has one + `ApplyAddPill` no-op guard. **L1 boot resilience already existed** (PU-402, `AiHotReloadCoordinator.cs:259-276` — generic catch skips a failing registrar; the boot "crash" was a debugger first-chance break). Core 195/0, UI 78/0, BTree.Editor 555/0; all 5 hosts build clean. **Out of scope:** inspector field-edit undo (`ApplyFacet` mutates the asset directly for nodes AND pills — framework-wide, separate). |
| DEC-07 | BTree host | Fix broken editor-created leaf nodes | ✅ DONE — `BTreeCommandSink.ApplyAddNode` set `DisplayLabel` to the raw kind id (e.g. "bt.leaf.wait") and never initialized the payload for non-Action/Condition kinds → a created Wait had `node.Wait == null` (uneditable Duration + error/red frame), titles showed kind ids. Now sets a friendly `DisplayLabel` + initializes payload (Wait→`{Duration=1}`, Subtree→empty). Likely also resolves the "red frame after undo" symptom (null-payload Wait validated as error). BTree.Editor 556/0. |

## ⚠ Build-verification gotcha (learned DEC-03)
The BTree source generator is **incremental** (VE-DEBT-003): editing only `.btree.json` assets, or editing `BTreeEmitCore` without rebuilding the `Hrot.AiEditor.Generators` analyzer, can leave **stale `.g.cs`** in place — so `dotnet build Hrot.AI.Behaviors` reports success against cached codegen. The DEC-01 Parallel-fix "compile gate" was a false green for this reason; a later clean build surfaced CS7036. **For any codegen/asset change, verify with `dotnet build Hrot.AI.Behaviors -t:Rebuild`** (and rebuild the Generators analyzer) — not an incremental build.

## Verification discipline (per batch)
- Lead re-reads diffs, independently re-runs builds + the relevant test projects. Never trust agent reports.
- Keep CombatShowcase.btree.json + SampleScout.btree.json untouched (byte-identity gate `ByteIdenticalGateTests`).
- Pre-existing failures (do not chase): 2 pretty-print round-trip + 7 Blueprints DEBT-006/perf.
