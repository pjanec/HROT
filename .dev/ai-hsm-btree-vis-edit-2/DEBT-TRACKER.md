# BTree / HSM Visual Editing — Technical Debt Tracker

> Deferred issues found during implementation / review. **P1 never enters this tracker** — it becomes Corrective Task 0 of the next batch. Track P2/P3 here; **do not delete resolved rows** (mark ✅/RESOLVED in place).
> Companions: [TASK-TRACKER.md](./TASK-TRACKER.md) · [TASK-DETAIL.md](./TASK-DETAIL.md) · design [docs/blueprints/BTree_HSM_Editor_State_And_Forward_Plan.md](../../docs/blueprints/BTree_HSM_Editor_State_And_Forward_Plan.md)

| ID | Title | Priority | Origin | Description / Issue | Status / Resolution |
|---|---|---|---|---|---|
| VE-DEBT-001 | HSM-state 4-slot param binding (= DEBT-BF-04) | P2 | blueprint-finalize/BATCH-BB1A review | BB1's type-filtered param-binding picker + Promote covers HSM **transitions/globals only, NOT states**. An HSM state hosts 4 action slots (Entry/Exit/Activity/Timer); the "one DTO → one variable" model needs a per-slot extension. **Needs an architect design call** — not an autonomous guess. Blocks `REVIEW-BB1(HSM)` (NOT the HSM authoring tasks HS-01..08). Cross-ref: `.dev/blueprint-finalize/DEBT-TRACKER.md` DEBT-BF-04. | OPEN — design call (NotebookLM/architect) before/at the HSM visual pass |

| VE-DEBT-002 | Real-condition codegen binding for BrainBlackboard trees | P2 | BATCH-06 review | The BTree generator emits `.Condition(Method)` as a 4-param `NodeLogicDelegate<TBB,TContext>`, but every real `[BTreeCondition]` takes a typed DTO param and `BrainBlackboard` has no DTO-typed field for the `ThreeParamReusable` expression-target path → a real condition can't be bound into a BrainBlackboard-typed generated tree that compiles. CombatShowcase therefore omits its bound Condition guard (so the **OBSERVES-badge demo** is not exercised). Needs the expression-target/blackboard-field machinery (BB1-adjacent) — likely resolved alongside BB1 / Blackboard Slice 1.5. | OPEN — flag at REVIEW-BT |

| VE-DEBT-003 | BTree generator incrementality | P3 | BATCH-17 | `BTreeJsonGenerator` now `Combine`s with the full `CompilationProvider` (for the BT-17 method-compatibility symbol-check), so generation re-runs on any compilation change, not just `.btree.json` changes. Acceptable for the small asset count; a fancier incremental symbol-extraction would restore per-asset incrementality. | OPEN (best-effort) |

| VE-DEBT-004 | No real HSM guard registered (showcase transitions can't bind a guard) | P2 | BATCH-HS-07 scout | HSM **actions** exist (`Hrot.AI.Behaviors.CgfHsmNodes.StubIdle`, `unsafe void(void*,void*,HsmCommandWriter*)`) so state action slots (OnEntry/Activity/...) and transition `ActionFunction` can bind. But **no production `[HsmGuard]`** is registered anywhere (only test stubs); `HsmValidator` has `UnboundGuard`/`ActionSignatureMismatch`. So the HSM showcase binds actions to `StubIdle` but **leaves transition `GuardFunction = null`** — directly analogous to [[VE-DEBT-002]] (BTree real-condition gap, D-05). The guard-binding demo is deferred until a real `[HsmGuard]` exists (likely BB1 / blackboard-slice adjacent). | OPEN — flag at REVIEW-HS |

| VE-DEBT-005 | HsmEventsWindow / HsmGlobalsStrip not registered in the HSM perspective | P2 | BATCH-HS-08 scout | Both window classes exist + are implemented but are **asset-bound** (hold a readonly `HsmAsset` from their ctor; `HsmEventsWindow(asset, refactor, findResults)`, `HsmGlobalsStrip(asset, store, dispatcher)`) and are NOT `ManagedWindow`s. The HSM canvas window (`AiGraphCanvasWindow`) is instead a doc-aware singleton reading the active doc from `_aiDocumentManager`. So registering events/globals is **not** a mechanical `RegisterExtraWindow` like the canvas — each needs a `ManagedWindow` adapter + a doc-retarget bridge (rebind/rebuild on `_aiDocumentManager` ActiveChanged), mirroring how the Blueprint Details/Variables windows retarget via a selection-store bridge. That's window-refactoring on the shared composition root → a design decision, not a safe autonomous mechanical edit. Deferred from HS-08 (which delivers the create→edit→save→reopen round-trip test). | OPEN — wire via a doc-retarget adapter; lead/architect at/after REVIEW-HS |

| VE-DEBT-006 | HSM emitter crashes on **forward/cross child transitions** (codegen, not editor) | **P1→tracked** | HsmShowcase boot crash 2026-06-13 | `HsmEmitCore` defers TOP-LEVEL state transitions to a 2nd pass (forward-ref-safe) but **nested children still emit `.On().GoTo()` INLINE inside their `Child(...)` lambda** (`EmitChildCall`). `HsmBuilder.GoTo(name)` resolves eagerly via `_graph.FindState` and throws "Target state not found" if the target's `Child`/`State` call hasn't run yet. ⇒ any machine where a nested state transitions to a **later-declared** sibling/state generates a builder that throws at `Compile()` → **editor crashes at boot** (the registrar runs `Compile()`). This is the BTree-class "editor must never emit code that crashes" guarantee, for HSM — a user drawing a back-edge / cross transition in the visual editor WILL hit it. **Immediate mitigation (done):** reordered the HsmShowcase `GuardComposite` children topologically so all its transitions are backward refs + added a runtime `HsmShowcase.Compile()` execution guard test (Hrot.Hsm.Editor.Tests). **Proper fix (TODO):** extend the two-pass to nested children — declare every transition-bearing child capturing its `StateBuilder` into a pre-declared `StateBuilder x = null!;` var (assign inside the lambda), then emit ALL transitions (top-level + nested) in Pass 2. Mind the `MigrationEquivalenceTests` byte-equivalence goldens (the emitted C# structure changes). | ✅ RESOLVED 2026-06-13 — `HsmEmitCore` now allocates a method-body-scope `StateBuilder x = null!;` for every nested transition-bearing state, captures the lambda param into it, and emits ALL transitions (top-level + nested) in Pass 2 after every state is declared. Forward/cross GoTo is now boot-safe. Showcase `GuardComposite` reverted to natural [03,04,05,06] order to prove it (Idle→Scanning, Scanning→AlertState are forward refs). Safety verified: byte-identity tests compare two paths both through `HsmEmitCore` (pass by construction); blob-equivalence holds because transitions are stored per-source-`StateNode` and each state's own transition order is preserved (cross-state registration order doesn't affect the blob). Removed dead single-pass `EmitTopLevelState`. Tests: Hsm.Editor 458/0 (incl. `Compile()` exec guard), Persistence 123/0, Generators 52/54 (2 = pre-existing committed-JSON pretty-print-vs-compact-serializer drift, proven on clean tree — NOT this change). |

| VE-DEBT-007 | HSM canvas overlays in wrong coordinate space + no state theming | P2→workstream | REVIEW-HS 2026-06-13 | Container nesting renders correctly, but every custom-renderer overlay (transition wires/labels, initial-state arrows, H/F glyphs, reroute dots) reads the raw asset `StateNode.Position`/`Waypoints` and `GraphToScreen`s them as absolute, while NodeEditor treats a child's `Position` as interior-LOCAL (`CanvasLayout.GetVisualCanvasPosition`). ⇒ overlays float detached from their nodes; wires detour through absolute-authored waypoints. Enabler: `ICanvasRenderContext` exposes no per-frame node/pin geometry, so renderers can't anchor off real positions. Plus: all states hardcode `NodeCategory.Custom` → no theming; parallel region dividers absent. | ✅ RESOLVED 2026-06-13 via the **RHS workstream** ([RHS/RHS-PLAN.md](./RHS/RHS-PLAN.md)), Option 1. RHS-01 exposed per-frame node/pin screen geometry on `ICanvasRenderContext`; RHS-02/03 re-anchored all 6 HSM renderers off it; RHS-04 mapped state-kind→`NodeCategory` colors; RHS-05 attached regions to their parent parallel state (dividers); RHS-08 fixed the deeper bug that transition wires never drew (hidden pins were `IsAdvanced` → never laid out → `WireRenderer` skipped them; added `PinShape.None` so pins are positioned but glyph-less). User visual gate PASSED: canvas now renders wires+labels, colors, dividers, H/F glyphs, initial markers. Optional polish (arrowheads = RHS-09) deferred. |

| VE-DEBT-008 | Wire reroute points not handled by host editors | P2→workstream | user request 2026-06-13 | NodeEditor UI fully emits `InsertReroute`/`MoveReroute`/`RemoveReroute` (context menu, ctrl/double-click, drag) and renders reroute dots + bent beziers, but no host command sink (HSM/BTree/Blueprint) handles these commands — all drop them via a `default` arm. So users can't add reroute points in any editor. HSM = trivial (waypoints already modeled + persisted); Blueprint = medium (add waypoint field to `Link` + model + sink); BTree = larger (no edge/waypoint store — needs model+DTO+mapper). | OPEN — tracked as the **RR workstream** ([RR/RR-PLAN.md](./RR/RR-PLAN.md)). |

---

## Notes

- **Pre-existing test failures** (not ours; do not chase): `Hrot.Blueprints.Tests` DEBT-006 set + flaky sub-150ns WhenNode perf (DEBT-014). Keep any failing set a *subset* (0 new) in our test projects.
- **Out of scope (Phase 2 — separate thread):** runtime debug overlays, breakpoints, stepping, heatmaps, trace-timeline population, `GetCurrentStateSnapshot()` kernel wiring. Do not pull these into the authoring tasks.
- **Cross-workstream, do not touch:** the merged `main-toolbar-1` (asset browser, create-new, Save, toolbar/menus) and committed `blueprint-finalize/BB1` files — except the explicit composition-root wiring named in a task.

## Legend
- **P1** Critical — never tracked here; becomes Corrective Task 0 next batch.
- **P2** Should fix — tracked, assigned when scheduled.
- **P3** Nice to have — tracked, best-effort.
- Status: OPEN / RESOLVED (do not delete resolved rows).
