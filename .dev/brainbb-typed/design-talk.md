The current BrainBlackboard ECS component is now implemented as a plain byte array. But it is not used as such. In fact, it is already partitioned to several areas and just one of them is used as a raw byte array with variable meaning, while the others use fixed and strict format (what bute at what offset means). There is already a structure descripbing this memory layout. Pls find it.

The structure you are looking for is **BlackboardMemoryLayout**, which is located in `FDP/Toolkits/Fdp.Toolkits/Behavior/Components/BlackboardMemoryLayout.cs`.

This structure acts as an explicit overlay Data Transfer Object (DTO) that maps the flat 128-byte `BrainBlackboard.Memory` buffer into compiler-verified named regions. Developers access it with zero-allocation by casting the byte array via `Unsafe.As<BrainBlackboard, BlackboardMemoryLayout>`.

Here is how the 128 bytes are strictly partitioned:

-   **Bytes 0–59 (****BehaviorParameters****)**: This is the raw byte array with variable meaning. It stores the polymorphic behavior parameters and is projected into behavior-specific DTOs (e.g., `FireAtTargetParams`).-   **Bytes 60–125 (****SoftAdvice****)**: This region is used for contextual routing data and "soft advice" written by engine subsystems (like `RouteContextSystem`). Keeping it separate guarantees that large behavior parameters cannot corrupt these values.-   **Byte 126 (****Interrupt\_MobilityLost****)**: A dedicated edge-triggered interrupt register. It is set to 1 by the `CognitiveInterruptSystem` on the exact tick an entity loses its ability to move, and cleared back to 0 at the end of the frame.-   **Byte 127 (****Interrupt\_Reserved****)**: A reserved hardware-level interrupt slot.

I want to redefine the ECS BrainBlackboard to no longer contain just public fixed byte Memory\[BehaviorConstants.BrainBlackboardByteSize\];

I want it to be redefined to something like a BlackboardMemoryLayout, but having normal named fields (positioned at proper offset using \[FieldOffset\]) for all the fields whose offset is already fixed anyway already.

The only truly variable part that should stay as fixed byte array is "BehaviorParameters" field.

This removes one level of confusion. We stop pretending the blackboard is an arbitrary byte array and will explicitly give a meaning to different areas and fields using standard csharp stuct syntax.

Of course, this will require refactoprs, as many parts of the code accesses the current "Memory" field. Escpecially the roslyn code generator (and maybe others) harcode this field name "Memory". It needs to be changed to "BehaviorParameters" to retain the ability to be offset indexed from various autogenerated code.

I need you to find all usages of the BrainBlackboard ECS structure and suggest how to safely perform this refactoring.

To safely perform this refactoring, you will merge the explicit layout definitions from `BlackboardMemoryLayout` directly into `BrainBlackboard` and update all subsystems, tools, and code generators that currently hardcode the `.Memory` field.

Here is the step-by-step refactoring plan based on the codebase's current usages.

1\. Redefine the `BrainBlackboard` Struct

You can safely remove the `BlackboardMemoryLayout` overlay struct and redefine `BrainBlackboard` to explicitly define its memory regions using `[StructLayout(LayoutKind.Explicit)]`:

```
[StructLayout(LayoutKind.Explicit, Size = BehaviorConstants.BrainBlackboardByteSize)]
[ComponentId(GlobalComponentIds.BrainBlackboard)]
[DataPolicy(DataPolicy.NoSave)]
public unsafe struct BrainBlackboard
{
    [FieldOffset(0)]
    public fixed byte BehaviorParameters[BehaviorConstants.MaxBehaviorParamByteSize];

    [FieldOffset(BehaviorConstants.MaxBehaviorParamByteSize)]
    public fixed byte SoftAdvice[BehaviorConstants.SoftAdviceByteSize];

    [FieldOffset(BehaviorConstants.Interrupt_MobilityLost_Offset)]
    public byte Interrupt_MobilityLost;

    [FieldOffset(BehaviorConstants.Interrupt_Reserved_Offset)]
    public byte Interrupt_Reserved;
}
```

2\. Update Code Generators (Roslyn)

The FastHSM source generator hardcodes `.Memory` when generating adapter thunks for `[SharedAiAction]` and `[SharedAiCondition]` methods.

-   **HsmActionGenerator.cs**: Update `EmitSharedAiGuardThunk` and `EmitSharedAiActionThunk` to reference `BehaviorParameters` instead of `Memory`.-   _(Note:_ _BTreeActionGenerator.cs_ _already projects offsets by casting the entire_ _BrainBlackboard_ _reference to a byte reference via_ _Unsafe.As<TBlackboard, byte>(ref bb)__, so it won't break with this change)._

3\. Remove `Unsafe.As` Casting in Core Systems

With named fields natively on the blackboard, you no longer need to cast to `BlackboardMemoryLayout`.

-   **CognitiveInterruptSystem.cs**: Remove the layout cast and set the interrupt byte directly.-   **CognitiveCleanupSystem.cs**: Remove the cast and clear the bytes natively at the end of the frame.-   **RouteContextSystem.cs**: Write soft advice directly to the native `SoftAdvice` fixed buffer instead of bouncing through the overlay.

4\. Fix `BehaviorIngressSystem` Shadow Copying

In `BehaviorIngressSystem.cs`, behavior parameter parsing creates a 128-byte stackalloc shadow buffer and copies data via `&bb.Memory`. Because `BehaviorParameters` is only 60 bytes, pinning it will strictly only pin those 60 bytes. You should update the shadow copy to operate on the struct's base address:

```
// Read from ECS
ref readonly var bbRO = ref repo.GetComponentRO<BrainBlackboard>(evt.Entity);
fixed (BrainBlackboard* src = &bbRO, dst = (BrainBlackboard*)shadow)
{
    Buffer.MemoryCopy(src, dst, BehaviorConstants.BrainBlackboardByteSize, BehaviorConstants.BrainBlackboardByteSize);
}

// ... After parsing ...

// Write to ECS
ref var bbW = ref repo.GetComponentRW<BrainBlackboard>(evt.Entity);
fixed (BrainBlackboard* src = (BrainBlackboard*)shadow, dst = &bbW)
{
    Buffer.MemoryCopy(src, dst, BehaviorConstants.BrainBlackboardByteSize, BehaviorConstants.BrainBlackboardByteSize);
}
```

5\. Update UI, Serializers, and View Providers

The Presentation toolkits parse the raw blackboard for debugging and file extraction:

-   **BrainBlackboardTranslator.cs**: Change `fixed (byte* ptr = &bb.Memory)` to `fixed (byte* ptr = &bb.BehaviorParameters)` when reflecting the DTO properties.-   **BrainBlackboardViewProvider.cs**: Update the StructEdit JSON-path hook to intercept `$.BehaviorParameters` instead of `$.Memory`:-   **BrainBlackboardRenderer.cs**: When rendering the raw fallback bytes, swap `bb.Memory` for `bb.BehaviorParameters`. You could also easily add explicit rendering for `bb.Interrupt_MobilityLost` now that it's a first-class field.

6\. Fix Unit Tests

Several tests manually mock blackboard memory by indexing the `Memory` array:

-   **BehaviorValidationScenario.cs**: Update `bb.Memory[MemAmmoCount] = ...` to `bb.BehaviorParameters[MemAmmoCount] = ...`.-   **CognitiveCleanupSystemTests.cs** & **CognitiveInterruptSystemTests.cs**: Update `bb.Memory` and `bb.Memory` to use `bb.Interrupt_MobilityLost` and `bb.Interrupt_Reserved` respectively.-   **BehaviorIngressSystemTests.cs**: Update `bbPtr->Memory` access to `bbPtr->BehaviorParameters`.-   **FdpAutoSerializerFixedBufferTests.cs**: Update the dummy data loop from `bb.Memory[i] = ...` to safely write via `Unsafe` or directly target the relevant buffer.

ad "BTreeActionGenerator.cs already projects offsets by casting the entire BrainBlackboard reference to a byte reference via Unsafe.As<TBlackboard, byte>(ref bb)" This is extremely fragile. It should use the "BehaviorParameters" field as the start offset.

You are absolutely right. My previous statement was short-sighted—casting the entire struct to a `byte` reference via `Unsafe.As` locks the generated offsets to the physical memory start of the struct. This breaks the robust abstraction you are building and leaves the system vulnerable to any padding or reordering at the top of the blackboard.

Since `Fbt.SourceGen` is already coupled to specific FDP domain types (like the Actuator channels), we can safely hardcode it to use the new `BehaviorParameters` fixed buffer as the explicit memory anchor.

Here is how to fix the fragility in **FDP/ExtDeps/FastBTree/src/Fbt.SourceGen/BTreeActionGenerator.cs**.

You need to update four specific `sb.AppendLine` calls across two methods where this projection occurs:

1\. In `EmitSharedAiAdapter`

Update the offset calculation for the standard shared AI adapters.

**Change this:**

```
sb.AppendLine("                        ref Unsafe.AddByteOffset(ref Unsafe.As<" + tb + ", byte>(ref bb), (nint)" + entry.Offset + "));");
```

**To this:**

```
sb.AppendLine("                        ref Unsafe.AddByteOffset(ref bb.BehaviorParameters, (nint)" + entry.Offset + "));");
```

2\. In `EmitHeavySharedAiAdapter`

Update the exact same offset calculation for the "heavy" shared AI adapters.

**Change this:**

```
sb.AppendLine("                        ref Unsafe.AddByteOffset(ref Unsafe.As<" + tb + ", byte>(ref bb), (nint)" + entry.Offset + "));");
```

**To this:**

```
sb.AppendLine("                        ref Unsafe.AddByteOffset(ref bb.BehaviorParameters, (nint)" + entry.Offset + "));");
```

Why this is the correct fix:

Because `BehaviorParameters` is a `fixed byte` array, indexing it via \`\` and prefixing with `ref` perfectly satisfies the \`ref byte\` signature expected by \`Unsafe.AddByteOffset\`. It explicitly anchors the offset to the 60-byte parameter partition no matter where that block physically sits inside the \`BrainBlackboard\` struct.

_(Note: When you make the updates in_ _HsmActionGenerator.cs_ _from my previous message, ensure you also use the \`\` indexer, i.e.,_ _ref bb.BehaviorParameters__, exactly as the old HSM generator used \`bb.Memory\`)_.

the SoftAdvice partition, isn't it also alreafy pretty fixed in meaning? do we need to keep as byte array?

You are correct. The `SoftAdvice` partition isn't just an arbitrary scratchpad; it also has strict, predefined byte offsets for specific contextual data.

In the codebase, engine subsystems—specifically the `RouteContextSystem`—write contextual routing data (derived from waypoint JSON extensions) into exact, hardcoded locations within this 66-byte block.

For example, the `BlackboardOffsets` class explicitly reserves a byte for **ExpectedThreatLevel** at offset 120 (which is exactly 60 bytes into the `SoftAdvice` region). When the `RouteContextSystem` parses a `"dangerLevel"` from a waypoint's JSON, it writes the value directly into this byte.

Do we need to keep it as a byte array?

**No, you don't.** If your goal is to eliminate raw byte offset arithmetic and make the layout compiler-verified, you can absolutely refactor the `SoftAdvice` region into standard named fields.

Currently, it is likely kept as a byte array to act as a generic "bulletin board" so that application-layer systems (like the HROT `RouteContextSystem`) can define their own offsets without modifying the core FDP engine's `BrainBlackboard` struct.

However, if you are standardizing the struct, you can replace the array with explicit properties. For instance:

```
// Bytes 0-59
[FieldOffset(0)]
public fixed byte BehaviorParameters[BehaviorConstants.MaxBehaviorParamByteSize];

// Bytes 60-119: Unused/Available Soft Advice space
[FieldOffset(60)]
public fixed byte UnusedSoftAdvice[5];

// Byte 120: Specific Soft Advice field
[FieldOffset(120)]
public byte ExpectedThreatLevel; 

// Bytes 121-125: Remaining padding
[FieldOffset(121)]
public fixed byte ReservedSoftAdvice[6];

// Byte 126
[FieldOffset(126)]
public byte Interrupt_MobilityLost;

// Byte 127
[FieldOffset(127)]
public byte Interrupt_Reserved;
```

This perfectly aligns with your refactoring goal: it removes the illusion of a variable byte array and explicitly documents what each byte in the 128-byte chunk actually does.

I would like to extend the BehaviorParameters to the lenght of 100 bytes (because 60 bytes is way too small buffer for behavior parameters). BUt i know the "60" is hardcoded (magic number) some places. Pls find them.

To extend the `BehaviorParameters` buffer to 100 bytes, you will need to update the hardcoded "60" values in a few specific places, as well as address the overall memory budget of the blackboard.

Here are the locations where the "60" magic number (and related size constraints) are hardcoded:

1\. `BehaviorConstants.cs`

The primary source of truth for the toolkit. You need to update the constant here:

-   **File:** `FDP/Toolkits/Fdp.Toolkits/Behavior/BehaviorConstants.cs`-   **Change:** `public const int MaxBehaviorParamByteSize = 60;` → `100`

2\. `BehaviorParameterSizeAnalyzer.cs`

The Roslyn analyzer cannot reference the runtime assembly because it targets `netstandard2.0`, so it duplicates the constant explicitly. If you do not change this, your builds will fail with `FDP_001` compiler errors when DTOs exceed 60 bytes.

-   **File:** `FDP/Toolkits/Fdp.Toolkits.Analyzers/BehaviorParameterSizeAnalyzer.cs`-   **Change:** `private const int MaxBehaviorParamByteSize = 60;` → `100`

3\. Comments in `BlackboardOffsets.cs` and `RouteContextSystem.cs`

While the actual code dynamically calculates the offset using the constant, the documentation heavily hardcodes the "60" value, which will be misleading if left unchanged:

-   **BlackboardOffsets.cs****:** "Placed at the start of the SoftAdvice region (offset 60) + 60 additional bytes = 120."-   **RouteContextSystem.cs****:** "SoftAdvice region starts at byte 60; ExpectedThreatLevel is at offset 120 (60 bytes into the SoftAdvice block)."

⚠️ CRITICAL: Overall Blackboard Size Expansion

Currently, the `BrainBlackboard` is strictly **128 bytes**. The layout uses:

-   60 bytes for `BehaviorParameters`-   66 bytes for `SoftAdvice`-   2 bytes for Interrupts _(Total = 128 bytes)_

If you increase `BehaviorParameters` to 100 bytes, the required footprint becomes **168 bytes** (100 + 66 + 2). Because ECS component chunks and struct layouts rely on these exact sizes, you **must also increase the total blackboard size**, ideally to the next clean boundary (e.g., 256 bytes) to maintain proper memory alignment.

You will need to update:

-   **BehaviorConstants.cs**: `public const int BrainBlackboardByteSize = 128;` → `256` (or at least `168`).

no changing of total size, it shoudl stay 128. It is the Soft Advice partition that needs to shrink accordingly. This is the issue probably just in the comments. Pls find the places where the commants hsould be rewritten to stop documenting concrete sizes and offsets. The only source of truth should newly be the revamped BrainBlackboard stucture. There should be no concrete numbers in the comments. Pls locate the places to be changed.

To keep the `BrainBlackboard` at 128 bytes by shrinking the `SoftAdvice` partition (from 66 bytes to 26 bytes) and expanding `BehaviorParameters` to 100 bytes, you are correct that the main cleanup involves purging concrete numbers from the documentation.

Here are the specific files and locations where comments currently hardcode these concrete sizes and offsets, which should be rewritten to reference the revamped structural fields instead:

**1\.** **Hrot/Subsystems/Hrot.CGF/Systems/Routing/BlackboardOffsets.cs**

-   **Class** **<summary>****:** Remove mentions of "SoftAdvice region of BlackboardMemoryLayout (bytes 60-125)", "parameter payload (bytes 0-59)", and "interrupt registers (bytes 126-127)".-   **ExpectedThreatLevel** **<summary>****:** Remove the comment explaining the math: "Placed at the start of the SoftAdvice region (offset 60) + 60 additional bytes = 120". This offset should be redefined as a standard field on the new structure, and the numeric documentation stripped.

**2\.** **Hrot/Subsystems/Hrot.CGF/Systems/Routing/RouteContextSystem.cs**

-   **Inside** **ApplyExtensionJson****:** Remove the inline comment: `// SoftAdvice region starts at byte 60; ExpectedThreatLevel is at offset 120 (60 bytes into the SoftAdvice block).`.

**3\.** **FDP/Toolkits/Fdp.Toolkits/Behavior/BehaviorConstants.cs**

-   **MaxBehaviorParamByteSize** **<summary>****:** Remove the text stating that this keeps payloads clear of "the soft-advice region (bytes 60-125) and the interrupt registers (bytes 126-127)".-   **Note:** You still need to update the actual constant values here: `MaxBehaviorParamByteSize` to `100`, and `SoftAdviceByteSize` to `26`.

**4\.** **FDP/Toolkits/Fdp.Toolkits/Behavior/Components/BlackboardMemoryLayout.cs** **(or your newly revamped** **BrainBlackboard.cs****)**

-   **Class** **<summary>****:** Remove the explicit `<list type="table">` that documents "0-59", "60-125", "126", and "127".-   **Field** **<summary>** **blocks:** Remove "Bytes 0-59", "Bytes 60-125", "Byte 126", and "Byte 127" from the respective field comments.

**5\.** **FDP/Toolkits/Fdp.Toolkits/Behavior/Systems/CognitiveInterruptSystem.cs**

-   **Class** **<summary>****:** Remove the phrase "blackboard byte (index 126)" and rewrite "Register 126 — MobilityLost" to simply reference the `Interrupt_MobilityLost` struct field.

**6\.** **FDP/Toolkits/Fdp.Toolkits/Behavior/Systems/CognitiveCleanupSystem.cs**

-   **Class** **<summary>****:** Rewrite the phrase "Clears per-frame interrupt bytes... Registers 126 and 127 are one-shot signals" to generically reference the interrupt fields.

**7\.** **FDP/Toolkits/Fdp.Toolkits.Analyzers/BehaviorParameterSizeAnalyzer.cs**

-   **Class** **<summary>****:** Remove the specific numbers from "128-byte BrainBlackboard layout" and "the 60-byte BehaviorParameters region".-   **Note:** Don't forget to update the private constant `MaxBehaviorParamByteSize = 60;` to `100` inside this file.

**8\. Unit Tests (Comments and Assertions)**

-   **CognitiveCleanupSystemTests.cs****:** Remove the comment "Verifies that interrupt bytes 126 and 127 are cleared each frame".-   **BehaviorValidationScenario.cs****:** The documentation block explicitly maps out `BrainBlackboard.Memory is fixed byte``. We reserve: ThreatVisible...`. Ensure any index references are updated or removed.-   **Various HSM tests:** Several test method names and comments explicitly mention "byte 126" (e.g., `B1_MobilityLostEdge_WritesByte126_HsmTransitionsToStopped`, `B3_BTreeEntity_Byte126_ClearedByCleanupSystem`). These should be renamed to reflect the semantic field rather than the byte index.

