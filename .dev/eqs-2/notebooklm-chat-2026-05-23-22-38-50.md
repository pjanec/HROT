---
exported: 2026-05-23T20:38:50.213Z
source: NotebookLM
---

# NotebookLM Conversation

Exported: 5/23/2026, 10:38:50 PM

---

## User

## FDP 226 - EQS

4 zdroje·23\. 5. 2026

The provided documents outline the **technical infrastructure and build system** for a C# software project named FDP, which incorporates complex diagnostic and networking capabilities. The project utilizes **automated batch scripts** for compiling solutions and filtering source code, alongside a **Visual Studio solution** containing numerous engine, toolkit, and example projects. A significant portion of the code focuses on a **thread-safe debug drawing system**, featuring a specialized buffer that manages transient and persistent visual primitives for real-time diagnostics. This system supports **interactive entity anchoring** and integrates with external dependencies like CycloneDDS for networked synchronization. Additionally, the sources define **metadata management for ECS components**, ensuring efficient tracking of data authority and modifications. Collectively, these files establish a framework for high-performance **simulation or visualization software** that requires robust debugging tools and distributed data handling.

Dnes • 17:41

I am designing various engine parts improval with external consultant called Claude - lie environment queries, path planning, animation control, all from the perspective of brain-muscle separation of our distributed system.

pls answer Claude's wuestions, one by one, pls reply to the first one only now:

1.  **Current `AreaQuerySolverSystem` internals.** What exactly does an `AreaQueryRequestEvent` carry today — just a polygon and a filter bitmask, or also a context entity, distance constraints, faction filter? How does the `EqsTargetPool` actually pack results (flat array offset+count? slab allocator? generational?) and how does the BTree poll completion — by request ID lookup each frame, or via a completion event on the scoped bus?
2.  **Variable-size payloads over DDS.** Path data, EQS result sets, animation event timelines — none of these are fixed-size. What's the current pattern for variable-size data on the wire? Is there a chunked/streaming descriptor pattern, a "ID handle + fetch-by-request" pattern, or are all variable-size things kept local to one node and the Brain only ever sees a fixed-size summary? This single answer will dictate whether EQS scoring happens on Muscle (with summary results to Brain) or whether full result sets ever cross the boundary.
3.  **`ParallelHint` cost model and budgeting.** When `AreaQuerySolverSystem` runs `Parallel.For`, is there a frame-budget mechanism that throttles how many requests it processes per tick, or does it drain the queue every frame? With 5-10k agents potentially issuing EQS queries, we need a clear budget story. Is there an existing priority queue pattern, or do requests get processed FIFO?
4.  **Spatial hash grid future.** Will the 5m-cell `SpatialHashGrid` persist when Stride3D arrives with proper colliders and a navmesh, or is the grid getting replaced by Stride's bullet/bepu physics broadphase? This affects whether EQS generators should depend on the grid API or a more abstract `IBroadphaseQueryProvider`.
5.  **`BehaviorInstanceId` lifetime for long-running queries.** If a BTree issues an EQS query that takes 3 frames to complete, and the entity's `BehaviorState.InstanceId` increments during that window (behavior interrupt), how does the existing async-request pattern handle the orphaned result? Does the request ID embed the instance ID, or does the requester check on retrieval?
6.  **Scoped bus vs. global bus for query results.** The perception module uses a module-private scoped `FdpEventBus`. Should EQS results be delivered via a scoped bus within the EQS module, or do BTree nodes (running on the Brain) need EQS results delivered via the cluster-replicated global bus? Put differently: where does the EQS solver actually live — Muscle (with results sent across DDS) or Brain (with a snapshot of the Muscle's spatial data)?
7.  **TKB and "tags" for filtering.** EQS queries inevitably need to filter by gameplay tags ("find allies", "find enemies of faction X", "find cover points of size 'large'"). Does the engine have a tag system layered on TKB, or are filters expressed purely as component-mask predicates today?
8.  **Navmesh integration intent.** Are you planning to bake the navmesh from Stride geometry once (offline, at scenario load) or rebuild dynamically when the world changes? And will the navmesh be voxelized like Recast, or a simpler triangulated walkable surface? This affects EQS heavily because positional generators usually want to project candidate points onto the navmesh.

To answer Claude's first question regarding the `AreaQuerySolverSystem` internals and our current Environmental Query System (EQS) implementation:

**1\. Contents of** **AreaQueryRequestEvent** Currently, the `AreaQueryRequestEvent` is a highly focused unmanaged struct designed for our distributed Brain-Muscle architecture. It carries:

-   `RequestId`: A stable `long` identifier used for correlation across the distributed pipeline.-   `TargetAreaEntity`: An ECS `Entity` handle pointing to the area boundary, which must carry an `EditablePolyline` component defining the polygon.-   `TargetForce`: A `ForceId` enum acting as a faction filter (e.g., to exclusively query hostiles).-   `SourceNodeId`: An `int` identifying the originating Brain node, ensuring the Muscle node knows where to route the response.

It does _not_ currently carry specific distance constraints or a separate context entity; the bounds are strictly defined by the target polygon entity itself.

**2\. Packing Results in** **EqsTargetPool** The `EqsTargetPool` is implemented as a flat, linear array allocator using a `NativeArray<long>` to store packed entity handles. It does not use a slab or generational allocator.

Instead, it relies on a simple `NextFreeIndex` cursor. When the `AreaQuerySolverSystem` resolves a query, it writes the matching packed entity handles sequentially into the pool starting at the current `NextFreeIndex`. The resulting `AreaQueryResult` is then populated with a `TargetGroupHandle` (the starting index offset) and a `TargetCount`. Consumers simply read the contiguous slice from `TargetGroupHandle` to `TargetGroupHandle + TargetCount - 1`. The pool is sized to hold up to 64 concurrent queries with up to 16 results each (`PoolCapacity`).

**3\. BTree Polling Mechanism** The BTree does _not_ consume completion events directly from a scoped bus. Instead, it polls for completion by performing a lock-free Request ID lookup every frame.

Here is how the pipeline flows:

-   The solver publishes an `AreaQueryResultEvent` via the command buffer.-   A main-thread system (`AreaQueryResultMaterializationSystem`) consumes this event and materializes the result into the `AreaQueryBatchData` singleton, which acts as a ring buffer.-   The BTree action node (e.g., `Condition_IsAreaQueryResolved`) polls completion by calling `AreaQueryBatchHelper.GetAreaQueryResult`.-   This helper computes the ring-buffer slot using modulo indexing (`requestId % DefaultCapacity`) and verifies that the slot's `RequestId` matches and its `IsReady` flag is true.

This design intentionally decouples the asynchronous solver timing from the BTree tick, allowing the cognitive tier to check query status synchronously without subscribing to event streams.

ok now pls answer Claude's question 2

To address Claude's second question regarding variable-size payloads over DDS:

**Variable-Size Data on the Wire vs. Local ECS** The current pattern relies on a strict architectural separation between our internal ECS (which enforces unmanaged, zero-allocation, fixed-size memory) and our DDS wire protocols (which permit variable-sized data using CycloneDDS's managed serialization).

Full result sets _do_ cross the Brain-Muscle boundary. We do not restrict the Brain to seeing only fixed-size summaries, nor do we require an "ID handle + fetch-by-request" streaming pattern for EQS or Path data. Instead, variable-sized collections are transmitted over the wire using `System.Collections.Generic.List<T>` fields decorated with the `[DdsManaged]` attribute. The impedance mismatch between the variable-sized network payloads and our unmanaged ECS tier is handled entirely within the ingress and egress translators at the network boundary.

**EQS Result Sets Example** For EQS queries, the solver runs on the Muscle node and writes packed entity handles sequentially into the fixed-size, unmanaged `EqsTargetPool` array. When it is time to replicate this to the Brain:

-   The `AreaQueryMuscleEgressTranslator` reads the flat unmanaged pool slice, resolves the local ECS handles to Network IDs, and packs them into a variable-length `List<long> TargetNetworkIds` inside a `DdsAreaQueryResponse`.-   These responses are batched into an `AreaQueryResponseBatch` topic, which itself contains a `[DdsManaged] List<DdsAreaQueryRequest>`.-   On the Brain node, the `AreaQueryBrainIngressTranslator` receives this variable-length payload, maps the Network IDs back to local generational ECS handles, and writes them directly into the Brain's own unmanaged `EqsTargetPool` allocating a contiguous slice from its ring-buffer.

**Path Data Example** Path data follows the exact same pattern. The Navigation Solver computes the path and registers the absolute waypoints in its local `TrajectoryPoolManager`. The `PathResponseSolverEgressTranslator` extracts these waypoints, converts them into a `[DdsManaged] List<RelativeVector3> CoarseWaypoints` to preserve floating-point precision, and transmits them within a `PathResponseBatch`. The requesting Brain node's ingress translator receives the variable-size list, reconstructs the absolute Cartesian points, and registers them into its local trajectory pool.

In short: scoring and path computations happen on the Muscle/Solver nodes, and the full variable-length result sets cross the DDS boundary via `[DdsManaged]` lists before being flattened back into fixed-size or pooled unmanaged structures on the Brain.

now please answer clade's question 3 - maybe once we switch to Stride 3d engine based muscle node, we might no longer need SpatialHashGrid? (we expect 5k-10k entities max)

To address your point about the `SpatialHashGrid` and Stride 3D (which actually aligns with Claude's 4th question):

We will absolutely continue to need the `SpatialHashGrid`, and it will not be replaced by Stride's physics broadphase. Our distributed architecture relies on a strict Brain-Muscle separation where the FDP ECS acts as the authoritative simulation layer (the Muscle) and Stride acts purely as the presentation layer (the Eyes). As demonstrated by our `EyesAndMuscleModule` and `SyncFdpToStrideScript` integrations, the ECS runs the simulation and merely pushes transform data out to the Stride data bridge. Because Stride's colliders are not authoritative for the simulation state, we cannot couple simulation-critical logic to them.

The custom `SpatialHashGrid` is deeply embedded across our deterministic toolkits. It is required by the `CarKinematicsSystem` for collision avoidance, the `VisionBroadphaseSystem` and `AudioPerceptionSystem` for sensory queries, and the `AreaQuerySolverSystem` (EQS) for point-in-polygon evaluations. Therefore, EQS generators must continue depending directly on our grid API rather than an abstract broadphase provider.

Regarding the 5k-10k entity target: the grid is already heavily optimized for a scale far beyond this. `SpatialHashConstants.MaxEntities` is pre-allocated for 100,000 entities. We recently upgraded the grid to support incremental updates and zero-allocation free-list slot reuse. Instead of a costly full rebuild every frame, the system only incurs a tiny O(1) removal and re-insertion cost for the specific entities that moved. It will effortlessly handle 10k agents.

To also answer Claude's literal 3rd question regarding the `ParallelHint` cost model and EQS budgeting:

The `AreaQuerySolverSystem` does not actually run `Parallel.For` or use a frame-budget mechanism today. Instead, it is isolated via a Separation-of-Duties (SoD) snapshot and runs asynchronously on a background thread at 10 Hz within the `EqsModule`. When it ticks, it completely drains the queue of all `AreaQueryRequestEvent`s accumulated since the last tick using a standard sequential loop. While this prevents stalling the main 60Hz loop, if 10k agents issue EQS queries simultaneously, it will stretch that background tick duration.

For standard ECS queries that _do_ use parallel iteration, the `ParallelHint` (Light, Medium, Heavy, VeryHeavy) drives an adaptive tuner that dynamically splits the 64KB unmanaged chunks into cache-friendly batches based on the active entity count and available CPU cores.

clarification for Calude: SoD means 'Snaphot on Demand' and represents a way how parallely runing tread can safely access a clone of ECS without locks. Plea exaplin the concept of ECS snaphost to Claude

To clarify the Snapshot on Demand (SoD) pattern for Claude:

In our distributed engine architecture, SoD is our primary concurrency mechanism to allow background modules (like the EQS area solver, pathfinding, or heavy AI) to safely read the ECS world without introducing lock contention on the main 60Hz simulation loop. Instead of locking the live ECS to prevent race conditions, we hand background threads isolated, point-in-time clones of the world state.

Here is how the ECS snapshot architecture works under the hood:

**1\. Zero-Allocation Pooling** Snapshots are fully instantiated `EntityRepository` objects kept in a thread-safe `ConcurrentStack` pool, managed by an `OnDemandProvider`. When an asynchronous module needs to execute, it acquires a snapshot from the pool. Once the background task completes, the snapshot is "soft-cleared" (preserving its underlying buffer capacities) and pushed back to the pool. This ensures that creating the snapshot generates zero heap allocations on the hot path.

**2\. High-Speed Synchronization (****SyncFrom****)** Upon acquiring a snapshot from the pool, the provider performs a `SyncFrom` operation against the live world. To make this fast enough to execute seamlessly within the main loop budget, it uses several optimizations:

-   **Component Mask Filtering:** Modules declare their required components, allowing the provider to compute a `BitMask256`. The snapshot only synchronizes the specific component types the background module cares about, completely skipping irrelevant data like rendering or UI state.-   **Unmanaged Bulk Copy:** For unmanaged ECS structs (Tier 1 data), the engine bypasses per-entity copying. Instead, it relies on chunk-level versioning (`_chunkVersions`) to detect changes and uses high-speed 64KB `Memcpy` block transfers (`Unsafe.CopyBlock`) to duplicate entire memory chunks at once.-   **Data Policies:** We enforce explicit `[DataPolicy]` attributes on components. Developers can mark temporary or thread-unsafe data with `DataPolicy.NoSnapshot` to exclude it from the copy, or use `DataPolicy.SnapshotViaClone` to force safe deep-clones for managed reference types.

**3\. Event History Accumulation** Because background modules often run at lower frequencies (e.g., the Navigation Solver runs at 10 Hz), simply copying the current component state is insufficient; they would miss transient one-frame events. To solve this, the live world uses an `EventAccumulator` to capture event history from the live bus every single frame. During the `SyncFrom` process, this accumulated history is injected into the snapshot's private replica bus, ensuring the background thread reliably sees all events that occurred since its last execution.

**4\. The "Convoy" Shared Snapshot Optimization** If multiple background modules run at the same frequency and require similar data (e.g., several AI subsystems running asynchronously), the kernel automatically clusters them into a "convoy" via the `SharedSnapshotProvider`. The provider computes a `UnionMask` combining all their component requirements, performs exactly one `SyncFrom` on the live world, and shares that single read-only snapshot across multiple async tasks using thread-safe reference counting.

This ensures that the main thread never stalls waiting for heavy solvers, while providing those subsystems with a strictly consistent, lock-free view of the simulation.

now please answer question 5

The `RequestId` generated for an EQS query does not embed the `BehaviorState.InstanceId`. Instead, it is a monotonic identifier constructed by packing the requesting entity's index and the `GlobalVersion` (the current simulation tick).

When a behavior interrupt occurs, the async-request pattern handles the orphaned result by simply abandoning the listener. Specifically, when the `BehaviorIngressSystem` processes an interrupt, it increments the `InstanceId` and explicitly resets the entity's `BrainBTreeState` to `default`. This fully wipes the BTree's execution pointer so the new behavior starts fresh from the root.

Because the execution pointer is reset, the BTree is no longer executing the `Condition_IsAreaQueryResolved` node and will never again poll for that specific `CachedEqsRequestId`. The background `AreaQuerySolverSystem` will still blindly finish its work and publish the `AreaQueryResultEvent`. The main-thread `AreaQueryResultMaterializationSystem` will then write the completed data into the `AreaQueryBatchData` ring buffer.

The orphaned result is entirely harmless. It simply sits unread in the ring buffer until a future query naturally overwrites its `requestId % DefaultCapacity` slot. Any packed entity handles allocated in the `EqsTargetPool` for that orphaned query are similarly harmless, as the pool is a transient linear allocator where the `NextFreeIndex` cursor naturally wraps or resets for future batches.

now pls answer question 6

The EQS solver (`AreaQuerySolverSystem`) lives strictly on the **Muscle node**. It runs asynchronously on a background thread (using the Snapshot-on-Demand pattern) because it requires direct, high-performance access to the `SpatialGridData` singleton and the entity spatial hash grid, which are maintained by the Muscle tier's physics and ground kinematics layer.

Regarding the bus architecture: unlike the autonomous perception pipeline—which quarantines its high-frequency inter-stage events on a module-private scoped bus—EQS results are delivered via the standard **global bus**.

Because BTree nodes execute on the Brain node, the EQS pipeline explicitly crosses the distributed DDS boundary rather than the Brain keeping a synchronized replica of the Muscle's spatial data. The round-trip flow works like this:

-   **Request:** A BTree node running on the Brain publishes an `AreaQueryRequestEvent` to its local bus. The `AreaQueryBrainEgressTranslator` reads this, converts it into a `DdsAreaQueryRequest`, and sends an `AreaQueryRequestBatch` over DDS to the Muscle node.-   **Solver Handoff:** The `AreaQueryMuscleIngressTranslator` receives the DDS batch and publishes local `AreaQueryRequestEvent`s on the Muscle's event bus, which the background `AreaQuerySolverSystem` picks up.-   **Resolution:** Once the spatial query is resolved, the solver publishes an `AreaQueryResultEvent` via its command buffer to the Muscle node's global `FdpEventBus`.-   **Egress to Brain:** The `AreaQueryMuscleEgressTranslator` reads the completed event from the global bus, resolves the internal targets to network IDs, and transmits an `AreaQueryResponseBatch` via DDS back to the originating Brain node.-   **Materialization:** Finally, the `AreaQueryBrainIngressTranslator` receives the DDS response, translates the network IDs back into local ECS entity handles, and directly writes the results into the Brain's local `EqsTargetPool` and `AreaQueryBatchData` ring buffer.

This cleanly separates the tiers: the Muscle handles all heavy spatial queries, and the Brain only ever receives the materialized result set which the BTree can synchronously poll.

now please answer question 7; we store the "tags" as part of the TKB data (often just the DIS entity type); for a quick access we might copy those as some flags to a dedicated ECS component (but we can't use flag-like empty components because of the severe 256 component limit)

To address Claude's seventh question regarding TKB, tags, and filtering:

We do not use a separate tag system or traditional ECS "tag" components because we simply cannot afford to. Our engine relies on a highly optimized, AVX2-accelerated `BitMask256` to track component existence. This enforces a strict, hardcoded limit of exactly 256 component types across the entire engine. Wasting these precious bits on transient or categorical empty flag structs (like `[IsCover]` or `[IsLarge]`) would exhaust our component budget immediately.

Instead, our filtering is handled through two distinct mechanisms that avoid the component limit while preserving O(1) query performance:

**1\. Faction and Allegiance Filtering** To satisfy queries like "find enemies of faction X", we rely on the `ForceId` enum (`Neutral`, `Friend`, `Hostile`). This data is stored inside the `EntityInfo` component. For EQS specifically, the `AreaQueryRequestEvent` struct natively carries a `TargetForce` filter, allowing the solver to immediately discard entities of the wrong allegiance during the broadphase or narrow-phase checks.

**2\. Gameplay "Tags" via TKB and DIS Types** For categorical gameplay tags (like "find large cover" or "find land vehicles"), we store this information as part of the Transient Knowledge Base (TKB) data, primarily leveraging the standard DIS (Distributed Interactive Simulation) entity classification.

For lightning-fast runtime access, we extract this classification at spawn and copy it directly into the 96-byte unmanaged `EntityHeader` as a `DISEntityType`. This struct packs the Domain, Kind, Category, and Subcategory into a single 64-bit integer (`ulong Value`).

Our `EntityQuery` architecture features dedicated, built-in support for filtering against this header data without needing separate components. Systems construct queries using `QueryBuilder.WithDisType(type, mask)`, which supplies a target value and a bitmask. Inside our zero-allocation `EntityEnumerator` hot loop, evaluating this "tag" filter resolves to a single-instruction bitwise check against the header (`(header.DisType.Value & _disFilterMask) != _disFilterValue`).

This design means EQS generators and broadphase queries can rapidly filter entities by precise tactical categories directly from the memory-contiguous entity headers, without allocating heap memory, without jumping to component tables, and most importantly, without burning through our 256 component-type limit.

and now pls the answer to claude's question 8. IN the final imlementation we want to use a dynamically changeable navmesh (by baking-in navmesh patches where the world has changed). For stride this would be an overkill but we should count with that in all the APIs. Navmesh will be voxelized like Raycast.

Currently, our navigation solver relies on a simple Dijkstra search over a static `RoadNetworkBlob` node graph. However, in the final implementation, we will transition to a dynamically changeable navmesh.

Rather than baking the geometry once offline at scenario load, we will rebuild the navmesh dynamically by baking in localized navmesh patches specifically where the world geometry has changed. While this dynamic patching approach might be an overkill for our current Stride 3D presentation layer, we must design all our navigation and EQS APIs to anticipate and fully support it from the ground up.

Additionally, the navmesh will be voxelized—similar to a Recast-style approach—rather than relying on a simpler triangulated walkable surface. This voxelized structure will provide our EQS positional generators with robust, volumetric data when projecting candidate points onto the walkable space, ensuring stability even as the underlying terrain and obstacles are dynamically patched during the simulation.

How do your BTree nodes currently consume async query results — do they suspend returning Running until a result is ready (polling by request ID), or do they fire-and-forget and rely on a result-arrived event to wake them?

Our BTree nodes suspend execution by returning `NodeStatus.Running` and actively poll for completion using the request ID each frame; they do not use a fire-and-forget pattern with result-arrived events to wake them up.

When an asynchronous query, such as an EQS area query or a pathfinding request, is issued, the BTree node submits the request and caches the returned monotonic `RequestId` in the entity's blackboard or local node state. On each subsequent execution tick, the node explicitly polls a main-thread materialized ring buffer (such as `AreaQueryBatchData` or `PathfindingBatchData`) using that cached ID.

If the background solver has not yet finished the work, the polling helper indicates the result is not ready, and the node returns `NodeStatus.Running`. This yields execution back to the BTree interpreter for the current frame without advancing the tree.

Once the background solver publishes the completion event and the main thread materializes it into the ring buffer, the polling node observes `IsReady == true` alongside a matching request ID. The node then processes the payload—such as caching the `TargetGroupHandle` for EQS targets or evaluating the returned path—and returns `NodeStatus.Success` or `NodeStatus.Failure` to cleanly advance the tree's execution path.

Entity Header space. The current 96-byte EntityHeader holds component mask, authority mask, lifecycle state, DISEntityType, and ForceId. Is there any unused space — even a single byte or two — that could hold a 16-bit or 32-bit "tactical tag bitfield" for fast EQS filtering (cover, sniper-perch, doorway, etc.)? If yes, how many bits are available without breaking the 32-byte AVX2 / 64-byte cache-line alignment? If no, the fallback is a TacticalTags component (~4-8 bytes per entity that has tags, ECS-side lookup cost). Just want to confirm what's affordable.

First, to correct a minor assumption: `ForceId` is actually not stored in the `EntityHeader`; it lives inside the `EntityInfo` component.

To answer your question directly: yes, we have space. There are exactly 11 contiguous bytes of unused padding at the end of the `EntityHeader`, spanning from offset 85 to 95. The `LifecycleState` occupies offset 84, bringing the used total to 85 bytes, with the remaining 11 bytes reserved strictly to pad the struct out to exactly 96 bytes.

Because the 96-byte total size is deliberately chosen as a multiple of 32 to guarantee our AVX2 and 64-byte cache-line alignments, you can safely insert a 16-bit, 32-bit, or even 64-bit tactical tag bitfield into this trailing padding without altering the struct's footprint or breaking alignment. For example, placing a `uint TacticalMask` at `[FieldOffset(88)]` would consume 4 of those 11 bytes while perfectly maintaining all memory invariants.

Furthermore, the `Flags` `ushort` at offset 66 currently only uses bit 0 to track the `IsActive` state, leaving bits 1-15 completely reserved for future use.

You have more than enough room for a 32-bit field in the raw padding or a 15-bit mask in the existing flags, so there is no need to incur the lookup cost of a separate `TacticalTags` ECS component.

pls check 512-components-design-talk.md Is this a valid and feasible change? can it really bring more component at no performance impact (or even increate the performance as the design talk suggests)?

The proposed transition to a 512-bit component mask using a Hot/Cold split is both technically sound and architecturally correct. It is entirely feasible and will successfully double our component capacity to 512 while actually increasing iteration performance.

If we were to naively double the `BitMask256` inside the existing `EntityHeader`, the struct would balloon from 96 bytes to 160 bytes. This would cripple performance by forcing the CPU to fetch three 64-byte cache lines per entity, wasting massive memory bandwidth loading irrelevant metadata (like `Generation` or `LifecycleState`) just to evaluate the component mask.

The design talk's solution applies a Structure of Arrays (SoA) layout by splitting the data into a tightly packed 64-byte `BitMask512` hot array and a parallel 128-byte `EntityMetadataCold` array. Because the hot array perfectly aligns with a single 64-byte L1 cache line, the hardware prefetcher can stream through it sequentially, allowing us to evaluate the 512-bit mask using bare-metal AVX2 VPTEST instructions without wasting cache space.

This guarantees a performance increase because the engine will only pay the memory bandwidth penalty to fetch the 128-byte cold metadata if the 64-byte mask evaluation actually matches. Since over 90% of entities typically fail a given query filter, reducing the memory footprint of the filtering check from 96 bytes to 64 bytes decreases overall frame time, resulting in ECS queries that are approximately 40% faster. Furthermore, because destroying an entity clears its component mask to zero, the AVX2 query will instantly short-circuit and bypass dead entities without ever needing to fetch the `IsActive` flag from the cold table.

To execute this feasibly, we must implement a few critical architectural updates:

-   **ID Expansion:** We must upgrade the backing storage of `ComponentIdAttribute` and `GlobalComponentIds` from `byte` (which hard-caps at 255) to `int`.-   **Dual Chunk Tables:** `EntityIndex` will need to replace its single `NativeChunkTable` with two synchronized, parallel chunk tables (`_hotMasks` and `_coldMeta`).-   **Flight Recorder Adjustments:** The `RecorderSystem` and `PlaybackSystem` must be rewritten to serialize the hot and cold arrays as two separate, parallel chunk streams.

As an added benefit, splitting the chunks actually makes the Flight Recorder more efficient; removing the padding overhead from the legacy 96-byte struct improves the LZ4 compression ratio, resulting in `.fdp` recordings that are smaller on disk and faster to save.

In such a new 512 component world, will there be the space for the "tactical tag bitfield" accesble for quick filtering?

In the new 512-component architecture, we will not use a separate tactical tag bitfield because we can simply represent these tags as native ECS components.

Our new hot array consists strictly of the `BitMask512`, which is exactly 64 bytes and perfectly fits a single L1 cache line. If we were to append a custom tactical bitfield to this hot data, we would overflow the 64-byte boundary, breaking our perfect cache-line alignment and ruining the traversal speed. Conversely, placing the tactical bitfield into the 128-byte `EntityMetadataCold` array would defeat the purpose of quick filtering, as evaluating it would force the CPU to fetch cold cache lines that we explicitly designed the system to bypass during failed query matches.

Instead, because we are doubling our component capacity to 512, we no longer need to strictly hoard component IDs. We can safely define tactical tags (like cover or doorways) as standard marker components, which will be tracked natively inside the 64-byte `BitMask512` hot array. This ensures they are evaluated at maximum speed using the same pure, sequential AVX2 SIMD instructions as our standard component queries without requiring any extra memory fetches.

the dual chunk stream you mentioned for 512 component refactor, does that mean 2 separate files instead just one fdp file?

No, it does not mean we will use two separate files; everything still goes into a single `.fdp` file. The "dual stream" concept refers entirely to the internal serialization layout written by the `RecorderSystem` into that file.

Instead of writing a single stream of interleaved `EntityHeader` chunks, the recorder will write two separate sequential blocks per chunk: one stream for the Hot masks using `ENTITY_INDEX_HOT_TYPE_ID` (-1) and an adjacent stream for the Cold metadata using `ENTITY_INDEX_COLD_TYPE_ID` (-2). Because these Hot and Cold arrays are written as contiguous, homogeneous blocks of data inside the file, our LZ4 compression ratio will actually increase significantly. This architectural change ensures our single `.fdp` recordings will be smaller on disk and faster to save, completely offsetting the fact that our overall per-entity memory footprint is growing.

Q1 — Snapshot facility. Does the engine already have a generalized "Snapshot-on-Demand" facility that systems can request snapshots from, or is each module (Perception, etc.) hand-rolling its own component-data snapshot? If generalized, what's the API? If hand-rolled, what's the canonical pattern — copy-out of specific component arrays under a lock, or something more sophisticated? This decides how the EQS solver gets its world view consistently.

The engine possesses a fully generalized Snapshot-on-Demand (SoD) facility; modules do not hand-roll their own snapshots. This system is deeply integrated into our `ModuleHostKernel` and is driven declaratively by a module's `ExecutionPolicy`.

When a background module, such as the EQS solver, declares its execution policy as `SlowBackground` (or explicitly requests `DataStrategy.SoD`), the kernel automatically provisions the appropriate `ISnapshotProvider`. The API contract is strictly abstracted behind `AcquireView()` and `ReleaseView(ISimulationView view)`, which the kernel's dispatch loop invokes automatically before and after the module's `Tick()` method executes.

Under the hood, the canonical pattern works as follows:

-   **Zero-Allocation Pooling:** Snapshots are full `EntityRepository` instances kept in a thread-safe `ConcurrentStack` via a `SnapshotPool`. When a view is released, it is soft-cleared and pushed back to the pool, guaranteeing zero heap allocations on the hot path.-   **Filtered Synchronization:** When `AcquireView()` is called, the provider pulls a snapshot from the pool and invokes `SyncFrom` against the live world. It uses a `BitMask256` derived from the module's `GetRequiredComponents()` to selectively synchronize only the specific component types the module cares about. Unmanaged structs bypass per-entity copying and rely on high-speed block memory transfers.-   **Convoy Sharing:** To optimize memory bandwidth, if multiple asynchronous modules run at the same frequency and require similar data, the kernel clusters them into a "convoy" using a `SharedSnapshotProvider`. The provider computes a union mask of their component requirements, performs exactly one `SyncFrom` copy against the live world, and shares that read-only snapshot across the background threads using reference counting (`_activeReaders`).-   **Event History Accumulation:** Because background modules tick at lower frequencies (e.g., 10 Hz), the snapshot process uses an `EventAccumulator` to inject any transient events that occurred since the module's last execution directly into the snapshot's private replica bus.

Thus, the EQS solver simply receives an `ISimulationView` interface in its `Tick` method. It gets a consistent, thread-safe snapshot of the spatial data and components it requested, completely isolated from the live world's 60Hz mutations and without introducing lock contention.

Q2 — Background thread pool. Is there a shared background-thread pool that EQS can submit work to (alongside Parallel.For), or should it own its own thread(s)? I'm specifically thinking about whether long-running solver work (a 4ms tick) should spin its own dedicated thread that wakes on a timer, or use Task.Run/ThreadPool for each candidate batch.

The EQS solver does not own a dedicated thread that wakes on a timer, nor does the engine implement a bespoke shared background-thread pool. Everything relies on the standard .NET `ThreadPool`.

The `AreaQuerySolverSystem` is registered within a module (like `CognitiveSpatialModule` or `EqsModule`) that defines its execution policy as `ExecutionPolicy.SlowBackground(10)` to run asynchronously at 10 Hz. The `ModuleHostKernel` handles the orchestration. When the frequency threshold is met, the kernel automatically offloads the module's entire tick to the ThreadPool via a standard `Task.Run` dispatch.

Because the module executes on the ThreadPool, if the solver needs to evaluate candidate batches concurrently during its tick, it can seamlessly use standard `Parallel.For` or the engine's `ForEachParallel` utility. Both the kernel's async module dispatch and the parallel partitioner share the same underlying ThreadPool, naturally balancing the load without thread oversubscription.

Furthermore, spinning a dedicated thread would bypass the kernel's safety envelope. When the `ModuleHostKernel` dispatches the background task, it wraps it in a cooperative timeout race (`Task.WhenAny` against a `MaxExpectedRuntimeMs` delay) tied to a Circuit Breaker. If a long-running solver heavily overruns its budget or hangs, the kernel will log a timeout, record the failure in the circuit breaker, and safely abandon the task to ensure the background pool isn't permanently starved.

Q3 — Time source. The architect mentioned TimeSystem. For per-sensor scheduling ("refresh at 5 Hz"), should the solver use simulation time (which can pause/slow under TimeSystem control) or wall-clock time? My guess is simulation time so EQS pauses with the game, but I want to confirm.

Yes, your guess is entirely correct. For per-sensor scheduling or background refresh rates like a 5 Hz EQS evaluation, the solver must strictly use simulation time.

Our engine enforces a strict separation between simulation time and wall-clock time through the `TimeSystem` and the `GlobalTime` singleton. Using simulation time (`GlobalTime.TotalTime` or `ISimulationView.Time`) ensures that the EQS solver natively respects the game's `TimeScale` (smoothly handling slow-motion or fast-forward) and properly halts when the simulation pauses.

Furthermore, our architecture demands strict determinism. If the solver relied on wall-clock time (such as `DateTime.UtcNow` or `GlobalTime.TotalWallTicks`), the frequency of queries would vary depending on the host machine's CPU speed and render framerate. This would instantly break our lockstep cluster synchronization and our deterministic `FlightRecorder` replays. Wall-clock time is explicitly reserved for real-world synchronization layers, like recording keyframes or updating UI elements that shouldn't be affected by slow-motion.

Under the hood, you actually won't need to hand-roll this 5 Hz timer inside the solver logic. The `ModuleHostKernel` manages module execution frequencies natively. If you assign the EQS module an `ExecutionPolicy.SlowBackground(5)`, the kernel's `ShouldRunThisFrame` method will automatically evaluate its execution based on elapsed simulation frames. Since the engine runs at a base rate of 60 Hz, the kernel will simply dispatch the solver task every 12 simulation frames.

If your solver needs to track internal, per-sensor cooldowns or individual query timeouts across its execution ticks, it should read `ISimulationView.Time` or `ISimulationView.Tick` provided in its `Execute` or `Tick` method signature. This transparently provides the correct, deterministic simulation time without needing to interact with the `TimeSystem` directly.

Q4 — Async raycast batching. When AutonomousPerceptionModule submits raycasts, can multiple modules submit concurrently, and is there a batching mechanism in the raycast solver (group N rays into one SIMD operation), or are they processed one at a time? This affects whether the EQS solver can naively submit "30 LOS checks for this candidate batch" or needs to do its own coalescing.

Multiple modules can safely submit raycast queries concurrently because submission is completely lock-free; systems simply publish `RaycastRequestEvent`s via their thread-local `EntityCommandBuffer`s to the event bus. You do not need to build your own coalescing mechanism inside the EQS solver.

When the `RaycastSolverSystem` executes, it reads the entire accumulated batch of requests from the bus at once. However, it does not group N rays into a single vectorized SIMD operation (i.e., we do not use SIMD ray packets). Instead, it uses a `Parallel.For` loop to distribute the batch across the `.NET` thread pool, resolving each ray independently. Each thread performs its own broad-phase AABB spatial hash query, followed by narrow-phase `Intersection2D.RaycastCircle` checks against the candidate colliders.

Because of this architecture, your EQS solver can naively fire off 30 LOS checks for a candidate batch simply by publishing 30 `RaycastRequestEvent`s. The `RaycastSolverSystem` will automatically aggregate them alongside requests from the perception or ballistics modules and efficiently load-balance the work across available CPU cores.

The only strict limitation to observe is the global `PhysicsConstants.RaycastBatchCapacity`, which is pre-allocated to 4096. As long as the combined volume of in-flight raycasts across all systems per frame stays under this limit, the unmanaged `RaycastBatchData` ring buffer will easily handle your naive submissions.

Q5 — Per-tick budget existing patterns. The 10Hz dispatcher and Perception module both run at fixed rates with no apparent budget enforcement. If EQS introduces a real wall-time budget (e.g., "stop processing at 4ms"), does that fit any existing pattern in the engine, or is it the first system to do so? If first, are there preferences about how budget overruns are logged/reported (counter, event, log line)?

Introducing a wall-time budget fits perfectly into a well-established pattern within the engine; EQS would absolutely not be the first system to do this.

The engine natively supports time-sliced execution to distribute heavy workloads across multiple frames without stalling the CPU.

**Existing Time-Slicing Patterns** The primary pattern relies on the `EntityRepository.QueryTimeSliced` method, which takes an `IteratorState` object to allow pausing and resuming execution across multiple frames,. You can pass it a specific budget alongside a `TimeSliceMetric`, such as `TimeSliceMetric.WallClockTime`,. Under the hood, the enumerator periodically checks the system stopwatch (every 64 entities) against the budget,. If the elapsed time exceeds the budget, it saves the current index into the `IteratorState.NextEntityId`, sets `IsComplete = false`, and cleanly exits the loop.

We already use this extensively in the `MapLayerAssignmentSystem`, which allocates a strict 1.0ms wall-clock budget per frame (`PerFrameBudgetMs = 1.0`) to avoid burning through the CPU budget when evaluating massive numbers of entities,. Additionally, the `TimeSystem` provides a `HasTimeRemaining(double estimatedCostMs)` helper to check if a frame still has available CPU time,.

You should utilize these mechanisms in the EQS solver to evaluate candidate batches.

**Budget Overruns and Reporting** There is a distinct difference between gracefully yielding your time slice and violating a hard execution budget:

-   **Graceful Yielding:** When your EQS solver reaches its 4ms budget and exits its evaluation loop to resume on the next tick (using `IteratorState`), this is normal, expected behavior. You do not need to log this as an error or budget overrun.-   **Hard Overruns:** If your solver gets stuck, ignores its budget, and exceeds the `MaxExpectedRuntimeMs` defined in its `ExecutionPolicy` (which defaults to at least 1 frame's worth of time based on frequency), the `ModuleHostKernel` will intervene,. The kernel wraps background module execution in a cooperative cancellation race (`Task.WhenAny` against a delay task),. If your solver times out, the kernel will automatically log the overrun to `Console.Error` and record a "Timeout" failure in the module's `CircuitBreaker`.

Therefore, you do not need to hand-roll custom logging for catastrophic overruns; the kernel already enforces this safety envelope. Simply design your EQS batches to check the stopwatch or use `QueryTimeSliced` to exit cleanly before that threshold is reached,.

Q6 — Navmesh cost model. Once Recast is integrated, do you have prior data on how much a typical navmesh query costs (point-projection, path-existence-check, full path)? Even rough order-of-magnitude (1μs/10μs/100μs/1ms) would help me size the budget realistically.

The sources do not contain prior performance data or order-of-magnitude cost estimates for the planned voxelized navmesh queries, including point-projections, path-existence checks, or full path computations.

Q7 — Variable-size list size limits. Variable \[DdsManaged\] List in translators — is there a practical upper bound on list length per message? E.g., if a sensor produces a 50-entry ranked list, is that fine on the wire, or should we cap at 20-ish for DDS health?

While CycloneDDS and our `[DdsManaged] List<T>` network serialization natively handle variable-size payloads and network fragmentation, you will not need to worry about DDS health because we strictly cap these sizes at the unmanaged ECS tier _before_ they ever reach the network translators.

For your specific sensor example, it will never produce a 50-entry ranked list. In our engine, both the `TargetMemory` cognitive buffer and the raw `SensorContactList` are strictly hardcapped by `PerceptionConstants.MaxTrackedTargets`, which limits them to exactly 4 targets. If a sensor detects more targets than this limit, the system evaluates the threat scores and seamlessly replaces the lowest-scoring target with the new one.

This strict capacity capping is applied across all subsystems to preserve zero-allocation, deterministic performance:

-   **EQS Queries:** The unmanaged `EqsTargetPool` limits area query results, sizing the pool to support up to 64 concurrent queries with a maximum of 16 results each.-   **Raycasts:** Raycast batches are globally capped by `PhysicsConstants.RaycastBatchCapacity` at a strict limit of 4096 rays per frame, silently dropping excess rays if the budget is exceeded.

Because our engine enforces these tight, fixed-size capacities in its internal memory structures, the variable-length payloads mapped into `[DdsManaged]` lists for DDS transit are naturally kept extremely small and network-friendly by design.

ThreadPool saturation under concurrent solver waits. Slow-background modules dispatched via Task.Run may block their ThreadPool worker thread while waiting for raycast batch completion (EQS solver awaiting accurate-LOS results, possibly Perception doing similar). With ~4-8 ThreadPool workers on typical hardware and multiple concurrent slow-background modules each blocking, can we saturate the pool? Is there a recommended pattern (e.g., async-await with continuation) or is the existing pool sized to absorb this? Not blocking for v1 — just flagging.

You do not need to worry about ThreadPool saturation because slow-background modules in our architecture never actually block their worker threads while waiting for asynchronous results.

Our engine strictly enforces a stateless, cross-tick polling pattern. We do not use `async/await` continuations or thread-blocking waits inside module ticks.

Here is exactly how the execution flow prevents ThreadPool starvation:

**1\. Fire and Yield** When a slow-background module (like your EQS solver or the Perception module) needs accurate LOS raycasts, it publishes `RaycastRequestEvent`s to its `IEntityCommandBuffer`. Crucially, the module's `Tick()` method does not wait for the result; it finishes its execution immediately and returns. This instantly frees the .NET ThreadPool worker to process other tasks.

**2\. Asynchronous Materialization** The `RaycastSolverSystem` processes those requests independently and publishes `RaycastResultEvent`s. On the main thread, the `RaycastResultMaterializationSystem` catches these events and writes the data into the unmanaged `RaycastBatchData` ring buffer.

**3\. Stateless Polling** On the background module's _next_ scheduled execution (e.g., 100ms later for a 10Hz tick), or during the next frame for a BTree node, the system uses the cached Request ID to poll the ring buffer. If the result is not yet ready, the system simply skips evaluation for that tick or returns `NodeStatus.Running`, again immediately yielding the thread.

**Why** **async/await** **or Blocking is Architecturally Forbidden** If you were to introduce `Task.Delay`, thread-blocking waits, or `async/await` continuations inside your solver's `Tick()`, you would trigger two catastrophic engine failures:

-   **Circuit Breaker Timeouts:** The `ModuleHostKernel` wraps every background module dispatch in a cooperative `Task.WhenAny` race against a hard timeout defined by `MaxExpectedRuntimeMs` (which is tightly budgeted, e.g., ~100ms for a 10Hz module). If your thread blocks waiting for a raycast batch, the kernel will assume the module has hung. It will log a timeout, open the circuit breaker, and permanently abandon the zombie task to protect the ThreadPool.-   **Snapshot Memory Corruption:** The `ISimulationView` (the SoD snapshot) handed to your module is leased from a zero-allocation `SnapshotPool`. The kernel's `HarvestEntry` method explicitly releases this snapshot back to the pool the exact moment your synchronous `Tick()` method returns. If you used an `async` continuation, your background logic would wake up and attempt to read ECS memory that has already been cleared and handed to another system, resulting in severe data corruption.

In short, stick to the existing pattern: submit the request, cache the ID, exit the tick, and poll the ring buffer next time. The ThreadPool will effortlessly absorb thousands of concurrent requests this way.

new batch of claude's questions, pls reply to the first one

Q1 — Template persistence model. EQS query templates would naturally be authored by AI engineers and designers. You mention that user-changeable AI stuff compiles to C# and hot-reloads from assemblies. Should EQS templates follow this exact pattern — i.e., templates are written as C# code in a designer-edited source folder, compiled to an assembly at edit-time, and hot-reloaded — or do they need a separate persistence story? Concretely: where do templates live on disk, what's the format (C# source, JSON, IR), how are they edited, and how do they get into the registry at runtime?

Q2 — Comparison with blueprint persistence. How are blueprints saved today? Are they C# source files, serialized objects, or something else? Is there an editor tool that emits them, or is the workflow "hand-write the C#, save the file, hot-reload"? I want EQS templates to follow the same pattern so users don't learn two different authoring systems.

Q3 — Stable template IDs across refactors. EQS templates need stable identifiers because the Brain subscribes to a sensor by template ID, and that ID crosses the DDS wire. If a designer renames a template's C# class or moves it to a different namespace, what happens? Options I see: (a) ID is a class attribute (\[EqsTemplate(Id = "FindCoverFromTarget")\]) and doesn't change on refactor; (b) ID is derived from the fully-qualified type name and refactors break subscriptions; (c) ID is a hash of the canonical form and refactors that change semantics break subscriptions while pure renames don't. What's the existing pattern for blueprint IDs and refactoring survival?

Q4 — Hot reload semantics for templates. When a template is hot-reloaded mid-scenario, what happens to sensors currently subscribed to that template? Options: (a) sensors continue with the old template until they're unsubscribed; (b) all sensors using that template are forcibly re-evaluated under the new template at next tick; (c) hot-reload of templates is disallowed during a scenario. What's the convention?

Q5 — Generator and test type extensibility. New IEqsGenerator and IEqsTest implementations are essentially new code. Do they hot-reload the same way templates do? If so, are templates that reference a generator/test type that was removed in the hot-reload handled gracefully (e.g., templates fail validation and are removed from the registry), or is this a hard crash?

To maintain a unified authoring pipeline, EQS query templates should absolutely follow the exact same persistence and compilation pattern currently used by our Blueprint and AI behavior systems. You will not need a separate persistence story.

Here is exactly how the architecture handles this:

**Format and Disk Location** Templates are saved on disk as declarative JSON files (specifically using the `.bp.json` extension). They reside in the designer's standard project workspace, such as the `Subsystems/Hrot.AI.Behaviors/Blueprints/` directory.

**How They Are Edited** Designers and AI engineers do not hand-write the C# source. They author the templates visually using the engine's built-in node graph UI (`GraphEditorWindow`), which serializes the visual graph directly into the JSON format.

**Compilation to C#** At edit-time, the engine relies on a Roslyn Incremental Source Generator (`BlueprintIncrementalGenerator`) hooked into the background MSBuild process. The generator intercepts the `.bp.json` files, parses the JSON, lowers the node graph into an Intermediate Representation (IR), and ultimately emits highly optimized, zero-allocation C# source code. This generated C# is compiled directly into the application's AI assembly (e.g., `Hrot.AI.Behaviors.dll`).

**Runtime Registration and Hot-Reloading** To get the templates into the registry at runtime, the engine uses the `AiHotReloadCoordinator`. This coordinator runs on a background thread and watches the target DLL for changes. When the background build finishes, the coordinator:

-   Loads the new assembly into an isolated, collectible `AssemblyLoadContext` (ALC).-   Scans the assembly via reflection for generated static classes decorated with specific attributes like `[BlueprintRegistrar]`.-   Extracts their `Register` or `RegisterAll` methods.-   Queues a callback for the main thread to invoke these registrars, injecting the compiled execution delegates directly into the central staging registries before performing an atomic swap.

Q2 please

Today, blueprints are saved to disk as declarative JSON files using the `.bp.json` extension, not as hand-written C# source files.

The authoring workflow relies entirely on a dedicated visual tool, the `GraphEditorWindow`, which allows designers to construct logic via a node-based interface. When a designer saves their work, the editor serializes the in-memory `BlueprintAsset` directly into this JSON format.

You do not need to hand-write C# and manually hot-reload. Instead, the engine automates the translation to code: the `.bp.json` files are included in the project as MSBuild `AdditionalFiles`. A Roslyn component called the `BlueprintIncrementalGenerator` automatically intercepts these JSON files during the build process. It parses the JSON and passes the data to the `BlueprintCompiler`. The compiler validates the structure, lowers the graph into an intermediate representation, and emits highly optimized, zero-allocation C# source code behind the scenes.

To ensure users do not have to learn two different authoring systems, EQS templates should simply adopt this exact same visual-to-JSON-to-C# pipeline, hooking into the existing `BlueprintAsset` schema and `GraphEditorWindow` workflow.

Q3

The existing pattern for blueprint IDs ensures refactoring survival by deriving a 32-bit integer identifier from a stable Asset GUID using an FNV-1a hash. Because your EQS templates will be authored visually and saved as `.bp.json` files, they will follow this exact mechanism, storing their `AssetId` GUID directly within the JSON document.

During edit-time compilation, the source generator extracts this GUID from the JSON file to compute the 32-bit `BlueprintId`. This integer is then embedded directly into the generated C# source code as a `const int`.

If a designer renames an EQS template or moves it to a new category, only the string metadata in the JSON file is updated. The compiler will generate a new C# class name that reflects the new name (using the format `{SanitizedName}_{BlueprintId:X8}_Bp`), but the underlying GUID and the resulting integer ID will remain perfectly stable. This guarantees that network subscriptions and DDS references used by the Brain to poll sensors are never broken by pure renames.

For purely code-authored assets (such as state machines or behavior trees), the engine provides a similar attribute-based pattern. Developers can explicitly specify a stable GUID via the definition attribute (e.g., `[HsmDefinition(MachineName = "...", AssetId = "...")]`) to survive class renames. If omitted, the engine falls back to hashing the string name. However, for your visual EQS templates, the explicit GUID stored inside the `.bp.json` file will act as the single source of truth.

Q4

To answer your fourth question regarding hot reload semantics:

The engine's convention does not strictly lock into just option (a) or (b); instead, it uses a severity-based classification system that compares the old and new templates using two distinct hashes: the `StructureHash` and the `ParamHash`. Your EQS templates should follow this exact pattern.

When a template is hot-reloaded mid-scenario, the engine evaluates the hashes to determine the appropriate response:

**1\. Soft Reload (Parameter Changes)** If a designer only tweaks values (e.g., changing a radius from 10m to 15m or adjusting a scoring weight) without altering the template's node topology, the `StructureHash` remains identical but the `ParamHash` changes. The engine classifies this as a `SoftReload`. In this case, live sensors currently subscribed to the template will seamlessly continue their execution without dropping their current state, but they will immediately begin using the updated parameters on the next tick.

**2\. Hard Reset (Structural Changes)** If the designer adds, removes, or rewires nodes (e.g., adding a new `IEqsGenerator` or `IEqsTest`), the `StructureHash` changes. This triggers a `HardReset`. When this happens, the engine actively iterates over a span of all live instances utilizing that template and invokes a hard-reset delegate to wipe their runtime execution state. For your EQS sensors, following this convention means any in-flight queries would be forcibly abandoned, and the sensors would be completely reset to re-evaluate the new template from scratch on their next tick.

Therefore, hot-reloading mid-scenario is fully supported (discarding option c), and the intrusiveness of the reload dynamically scales based on whether the designer made a cosmetic parameter tweak or a fundamental structural change.

Q5

Yes, new `IEqsGenerator` and `IEqsTest` implementations are standard C# code and will hot-reload exactly the same way as your templates. They are compiled directly into the AI assembly and managed by the same AssemblyLoadContext (ALC) hot-reloader.

If a template references a generator or test type that was removed, it is handled gracefully and will absolutely not cause a hard crash. The engine guarantees this through two distinct safety mechanisms depending on the reload path:

**1\. Full Rebuild (File-Watcher)** If a designer saves a blueprint that references a deleted C# type, the compilation will fail during the background MSBuild process. Because the build fails, a new `.dll` is never emitted. The `FbtAssemblyHotReloader` never detects a file change, and the old ALC continues running the existing logic completely uninterrupted. Even if the assembly somehow loads but fails registration, the reloader catches the exception, fires an `OnReloadFailed` event, unloads the new ALC, and leaves the simulation running on the old ALC.

**2\. Quick Reload (In-Memory)** If the reload is triggered via the editor's in-memory `QuickReloadService`, the `BlueprintCompiler` will attempt to lower the graph and compile it using Roslyn. When it fails to resolve the missing generator or test type, it generates diagnostic errors and throws a `BlueprintCompileException`. The `QuickReloadService` explicitly catches this exception, safely aborts the reload, and returns a failed result. If the failure happens deeper during the atomic staging swap, the `AiHotReloadCoordinator` unloads the new patch ALC to prevent memory leaks, leaves the previous `_currentAlc` entirely intact, and propagates the failure to the UI.

In both scenarios, live sensors simply keep executing their previous, valid templates without any interruption or crash.

next buch of claude qustion, pls reply to Q9

Q9 — Co-existence with .bp.json paths. If EQS templates can be authored as either hand-written C# classes (\[EqsTemplate(AssetId = ...)\]) or as .bp.json files (compiled by the source generator), what's the recommended convention for detecting and reporting AssetId collisions between the two paths? Build-time error in the source generator? Runtime error during registration? Some other facility?

Q10 — Hot-reload of hand-written C# template classes. The architect described hot-reload for the AI assembly via AssemblyLoadContext and AiHotReloadCoordinator. Hand-written C# classes live in that same AI assembly. When a designer edits a hand-written \[EqsTemplate\] class and saves, does the hot-reload pipeline catch the change the same way it catches .bp.json changes — i.e., MSBuild rebuilds, AiHotReloadCoordinator swaps ALCs, the registry re-scans and applies Soft/Hard reload classification? Or do hand-written templates require a full process restart?

Collisions between hand-written C# template classes and `.bp.json` templates are detected and reported as a runtime error during the registration phase, rather than a build-time error in the source generator.

When templates are loaded into the engine, they are first added to a staging buffer, such as `BlueprintRegistryStaging`, which indexes them by the 32-bit integer ID derived from their `AssetId`. If the registry attempts to add a new template and detects that its ID already exists in the staging dictionary, it immediately throws an `InvalidOperationException`. This exception explicitly reports the collision by stating which new template name is attempting to replace the existing one, and advises the developer to regenerate one asset's GUID.

Because this validation occurs during the atomic staging process at runtime, the engine is able to handle the collision gracefully. If a designer introduces a collision that is compiled and picked up during a hot-reload, the `AiHotReloadCoordinator` will catch the resulting exception when it invokes the registrars. The coordinator will then safely abort the atomic commit, fire an `OnReloadFailed` event, and unload the new AssemblyLoadContext (ALC) to prevent memory leaks. The live simulation is entirely protected from the collision and will continue executing the previous, valid templates without interruption.

q10

Hand-written C# template classes do not require a full process restart; they participate in the exact same hot-reload pipeline as `.bp.json` templates.

Because both the hand-written C# classes and the source-generated C# code emitted from `.bp.json` files are compiled into the identical AI assembly (`Hrot.AI.Behaviors.dll`), they share the same `AssemblyLoadContext` (ALC) lifecycle.

When a designer edits a hand-written C# class and saves the file, the hot-reload workflow operates as follows:

-   **Recompilation:** A background MSBuild process is triggered (e.g., via the editor's `RebuildAndReloadAI` action) to recompile the assembly.-   **Detection & ALC Load:** Once MSBuild overwrites the `.dll` on disk, the file-watcher inside the `AiHotReloadCoordinator` detects the change. It immediately loads the newly built assembly into a fresh, collectible ALC on a background thread and scans for registrars.-   **Atomic Swap & Re-Scan:** On the main thread, the coordinator clears stale pointers, invokes the registrars to stage the updated definitions, and atomically swaps the ALC.-   **Soft/Hard Classification:** The exact same reload classification is applied to the hand-written C# classes. The engine's `HotReloadManager` iterates through the chunks and compares the `StructureHash` and `ParamHash` of the newly registered templates against the old ones to dynamically determine if live entities require a hard reset or seamlessly receive a soft parameter update.

Ultimately, by the time the code reaches the `AiHotReloadCoordinator`, the engine treats hand-written C# templates and visually-authored templates as completely indistinguishable.

On the relationship between \[EqsTemplate\] and \[BlueprintRegistrar\]. The architect mentioned \[BlueprintRegistrar\] as the attribute the hot-reload coordinator looks for on generated registrar classes. For hand-written \[EqsTemplate\] classes, the registrar can either be: (a) the template class itself implements registration directly, or (b) a separate \[BlueprintRegistrar\] static class is generated by a tiny source generator that scans for \[EqsTemplate\] attributes and emits the registrars. Option (b) preserves the same hot-reload pattern as the JSON path. I lean (b) — it keeps the hot-reload coordinator's contract uniform.

You are completely correct to lean toward option (b); employing a tiny source generator to emit a `[BlueprintRegistrar]` static class perfectly aligns with our established architectural patterns.

Our `AiHotReloadCoordinator` enforces a strict contract: during a reload, it explicitly scans the compiled assembly's types looking solely for the `[BlueprintRegistrarAttribute]`. When it finds a matching class, it extracts and invokes its public static `Register` or `RegisterAll` method to stage the definitions. If you were to force hand-written templates to implement registration directly (option a), developers would have to manually maintain these registration methods, or the coordinator would have to be modified to scan for multiple distinct attribute types, which would break the uniformity of the hot-reload pipeline.

Instead, option (b) mirrors exactly how we already handle C# authoring for Behavior Trees and Hierarchical State Machines. For example, our `BTreeDefinitionGenerator` acts as an incremental source generator that scans the syntax tree for methods annotated with `[BTreeDefinition]`, automatically aggregating them into a catalog. Similarly, the `HsmActionGenerator` scans for `[HsmAction]` and `[SharedAiAction]` attributes and emits a centralized `HsmActionRegistrar` class decorated with the required `[HsmActionRegistrar]` attribute.

By building a small source generator that intercepts your `[EqsTemplate]` classes and emits a single, centralized `[BlueprintRegistrar]` class containing the `RegisterAll` calls, you guarantee that both the visual `.bp.json` path and the hand-written C# path converge into the exact same registration mechanism. This keeps the `AiHotReloadCoordinator` perfectly agnostic to how the templates were originally authored, preserving its uniform contract and ensuring safe, atomic staging on the main thread.

On a slightly subtle point about Build()'s purity. The Build() method must be deterministic (given the same code, produces the same template, produces the same hash). It must not depend on runtime state, the current scenario, or anything mutable. This is obvious in principle but worth declaring explicitly because it's the kind of thing where someone might be tempted to write if (currentScenario.IsUrban) UseDifferentGenerator() — which would be wrong. Templates are static authoring; runtime variation is expressed through sensor parameters (the runtime parameter struct on the EqsSensor component), not by templates that look at the world. This should be a documented rule and possibly enforced via an analyzer that flags Build() methods that read non-parameter state. Worth doing?

You are absolutely correct; the template `Build()` method must be strictly pure and deterministic. In our architecture, templates represent the immutable "ROM" definition of a behavior or query, whereas runtime and environmental variations must be managed exclusively through the "RAM"—the ECS components and blackboard parameters.

The engine already provides a structural baseline to prevent this anti-pattern. For example, our Roslyn source generator `BTreeDefinitionGenerator` enforces via the `BTree002` diagnostic that any method defining a behavior tree must be `static` and take exactly zero parameters. By enforcing a parameterless static signature for your `[EqsTemplate]` builder methods, you inherently prevent developers from injecting transient state like an `ISimulationView` or `ScenarioContext`.

However, as you pointed out, a developer could still theoretically reach out to global static state (e.g., reading a static `currentScenario.IsUrban` flag) inside the builder. Enforcing purity via a dedicated Roslyn analyzer is highly recommended and aligns perfectly with how we already police the engine.

We currently use the `Fdp.Toolkits.Analyzers` project to enforce domain-specific architectural constraints at compile time, such as the `BehaviorParameterSizeAnalyzer` which issues an `FDP_001` error if a behavior's parameter struct exceeds our strict memory-layout budgets. Creating a similar analyzer to scan `[EqsTemplate]` methods and flag any references to forbidden global state, singletons, or non-deterministic APIs (like `DateTime.Now`) would be a completely idiomatic and valuable addition to the pipeline.

Q8 — Squad/identical-evaluation sharing. A common case: 10 squad members each have a FindNearestEnemy sensor with the same template and the same context = squadLeader. The solver can detect these as identical evaluations and run them once, fanning out the result to all 10 subscribers. Does the existing blueprint runtime have a similar evaluation-sharing pattern (e.g., behavior tree subtrees that produce identical values being shared across instances), or would EQS be inventing this? If inventing, is it OK to bake it into v1 or should it be a deferred optimization?

The existing blueprint runtime does not implement an identical-evaluation sharing pattern across instances. While the compiled tree templates (`BehaviorTreeBlob`) are strictly immutable and shared across all entities using them, the runtime execution is entirely independent. Each entity maintains its own isolated 64-byte `BehaviorTreeState`, and the `BTreeTickSystem` evaluates the logic for each entity individually during its tick without any cross-instance result sharing or memoization.

Regarding whether EQS should invent this squad-level evaluation sharing for v1 or if it should be treated as a deferred optimization, the sources do not contain any relevant information or architectural directives to make that decision.

Q11 — Subscription lease/heartbeat patterns in the engine. For Brain-Muscle CQRS state across DDS, is there an existing pattern for leased subscriptions — i.e., Brain-side state that Muscle should retain only as long as Brain is healthy and reaching it? EQS sensors would benefit from a TTL + heartbeat model so that a crashed Brain doesn't leave orphaned sensors consuming solver budget on Muscle nodes. If no pattern exists, we'd invent one for EQS, but if there's a convention (e.g., the existing channel intents/statuses use one), we should match it.

There is no bespoke lease or TTL/heartbeat pattern for individual CQRS components like channel intents or sensors, because the engine handles crash recovery natively through global DDS instance lifecycles. You do not need to invent a custom TTL model for EQS sensors.

In our architecture, the existence of any networked entity is strictly governed by the `EntityMaster` descriptor. If a Brain node crashes or ungracefully disconnects, CycloneDDS automatically detects the loss of the writer and transitions the `EntityMaster` DDS instance state away from `Alive` (e.g., to `NotAliveNoWriters`). When the `EntityMasterIngressTranslator` on the Muscle node polls the topic and observes this non-alive state, it immediately publishes a `DestroyEntityCommand` to the local event bus. The `NetworkSpawningSystem` and entity lifecycle module then tear down the entire ghost entity, which naturally destroys your EQS sensors and any other CQRS intents along with it.

At the node level, we also have a 1 Hz heartbeat system where the `ClusterSlave` publishes `NodeHeartbeatEvent`s, allowing the `ClusterMaster` to detect unresponsive nodes and safely eject them from the cluster.

For component-level data consistency over unreliable UDP topics, we do employ a data heartbeat, but it is strictly for packet loss recovery rather than TTL leasing. The `SmartEgressUtil` manages this using a salted rolling window (typically 600 ticks, or 10 seconds) to periodically force a republish of components even if they haven't been marked explicitly dirty.

To integrate EQS sensors safely, simply attach your sensor state components to the networked entity and transmit them via standard DDS translators. The existing `EntityMaster` lifecycle will perfectly guarantee that a crashed Brain never leaves orphaned sensors consuming solver budgets on your Muscle nodes.

The architect's answer reveals something about the protocol I'd been mis-modeling. I described:

Brain → Muscle: SensorSubscribe, SensorUnsubscribe, SensorParameterUpdate. Muscle → Brain: SensorResult.

But if sensors are just components on a networked entity, replicated via the standard DDS translators, then there's no bespoke "SensorSubscribe" message. Brain creates the EqsSensor component on its locally-authoritative entity → DDS replication carries it to Muscle → Muscle's local ECS gets the component → the solver's component-iteration finds it next tick. This is actually cleaner. The protocol I described would have been a parallel sideband to the entity replication, prone to ordering issues (what if SensorSubscribe arrives before the entity itself is replicated?). The component-on-entity model is naturally ordered — DDS guarantees the entity exists wherever its components are. The only direction that needs bespoke wire schema is Muscle → Brain for the result delivery: SensorResult(sensorId, epoch, refreshTick, rankedList). And even that might be expressible as just an EqsSensorResult component on the same entity, replicated Muscle → Brain via a translator. This puts both directions of the sensor lifecycle through the same component-replication mechanism.

How does it work now for perception?

Your realization about using component replication instead of bespoke sideband messages is spot-on. The engine's autonomous perception pipeline uses this exact architectural pattern, split into two distinct halves: **Component Replication** for the downward configuration (Brain → Muscle) and **Discrete Event Messages** for the upward result delivery (Muscle → Brain).

Here is exactly how it works for perception:

**1\. Brain → Muscle (Configuration via Component Replication)** There are no "SubscribeToSensor" network messages. If an entity needs perception, the Brain node simply attaches a `PerceptionReceptor` component to it, which defines its `VisionRange`, `HearingRange`, and FOV.

-   The `SensorConfigEgressTranslator` on the Brain monitors these components. Using `SmartEgressUtil` for dirty-tracking, it publishes the configuration to the `SensorConfig` DDS topic only when it changes.-   On the Perception Solver (Muscle), the `SensorConfigIngressTranslator` receives the DDS sample and applies the `PerceptionReceptor` component to the local ghost entity (precomputing the FOV cosine at the network boundary to keep the spatial-hash hot path fast).-   Because the component now exists on the Muscle entity, the `VisionBroadphaseSystem` automatically includes it in its next 10Hz sweep.

**2\. Muscle → Brain (Results via Discrete Events)** For the return trip, the engine intentionally does _not_ continuously replicate the Muscle's raw tracking component back to the Brain, as replicating full arrays every tick would saturate network bandwidth.

-   **Muscle Local State:** The Perception Solver maintains a `SensorContactList` component locally. This holds the raw physical tracks and manages the debounce/hysteresis state (Pending → Acquired → Lost).-   **Discrete Egress:** When `SensorTrackDebounceSystem` determines a contact has formally transitioned to Acquired or Lost, it publishes a local `SensorTrackStateEvent`. The `SensorTrackStateEgressTranslator` catches this event and publishes a `SensorTrackState` DDS message using Reliable/TransientLocal QoS. This ensures only state-changes hit the wire, not continuous updates.-   **Brain Ingress & Local State:** On the Brain, the `SensorTrackStateIngressTranslator` reads the DDS message and bridges it back to the Brain's local event bus. The `ActiveSensorTracksUpdateSystem` consumes this event and updates the `ActiveSensorTracks` component (a cognitive buffer of currently acquired contacts) on the observer entity.-   **Cognitive Evaluation:** Finally, the `ThreatEvaluationSystem` (running on the Brain) reads the `ActiveSensorTracks` buffer each tick, continuously boosting threat scores inside the entity's `TargetMemory` component and applying temporal decay.

**Application to your EQS Design** Your intuition to use an `EqsSensor` component for the Brain → Muscle request perfectly matches the `PerceptionReceptor` pattern. For the Muscle → Brain result delivery, you have two options depending on frequency:

-   If the EQS result changes rarely, you could replicate an `EqsSensorResult` component directly.-   If the EQS solver re-evaluates and produces new ranked lists frequently (like perception), you should mirror the `SensorTrackState` pattern: have the Muscle emit an `EqsResultEvent` translated to a DDS message, which an ingress translator on the Brain uses to update an `EqsCognitiveBuffer` component. This keeps your network traffic purely event-driven while maintaining clean ECS component state on both sides.

added EQS\_Design\_v1.2\_final.md source. Pls check for flaws and if it fits our egine.

The EQS v1.2 design is architecturally sound and fits the engine's paradigms exceptionally well. You have successfully integrated the autonomous perception pipeline's CQRS pattern (using `EqsSensor` for configuration and `EqsResultEvent` for discrete state changes), respected the snapshot-leasing constraints by using cross-tick polling, and aligned with the Roslyn-based `.bp.json` compilation and hot-reload pipelines.

However, there are a few implementation-level details and engine-specific traps you must address before moving to production.

**1\.** **EqsResultEvent** **Payload and Event Bus Constraints** In Section 3.1, the design states the Muscle solver "emits a local `EqsResultEvent` on Muscle's event bus," which is then translated to a DDS message with a `[DdsManaged] List<T>`. You must strictly observe the boundary between unmanaged and managed event streams. The standard `FdpEventBus.Publish<T>` requires `T` to be an unmanaged struct. If your local `EqsResultEvent` carries the variable-size ranked list directly, you have two options:

-   **The Area Query Pattern (Recommended):** Keep `EqsResultEvent` purely unmanaged by having it carry an integer handle (e.g., `TargetGroupHandle`) pointing into a shared native array like the existing `EqsTargetPool`. The `EqsResultEventEgressTranslator` then reads this pool to construct the `[DdsManaged] List<T>` for the network boundary.-   **Managed Event Stream:** If you prefer to emit the list directly, you must use `Bus.PublishManaged<T>()` and read it via `ReadManagedEvents<T>()`, which safely bypasses the unmanaged generic constraint.

**2\. The C# 12** **\[InlineArray\]** **Mutation Trap in** **EqsCognitiveBuffer** Section 8 notes that `EqsCognitiveBuffer` is populated by the Brain-side update system and stores the top-K results. If you define the 16-element result array inside `EqsCognitiveBuffer` using C# 12's `[InlineArray]` attribute (similar to `MissionPlanQueue` or `PassengerBuffer`), you must be extremely careful when updating it. The C# compiler emits an `ldobj` defensive copy when indexing directly into a `ref` struct containing an `[InlineArray]`. Writing directly to the index (e.g., `buffer.Results = newResult`) will write to a JIT temporary variable and the mutation will be silently lost. You must cast the buffer to a span first: `Span<Result> results = buffer.Results; results[i] = newResult;`.

**3\. Validation of the 512 Component ID Extension** In Section 13, you resolved the tactical tags issue by relying on the component ID space extending to 512. This assumption perfectly aligns with the engine's ongoing architectural migration. To overcome the memory bandwidth bottleneck of AVX2 comparisons, the engine is replacing the 96-byte `EntityHeader` with a Structure-of-Arrays (SoA) layout. This splits entity data into a 64-byte `BitMask512` hot array and a 128-byte `EntityMetadataCold` array, explicitly upgrading the component limit from 256 to 512 while maintaining blazing-fast AVX2 SIMD queries. Your design is safe to assume 512 available component slots.

What is the recommended path forward from here? is this EQS design actionale already (to be implemented in CGF and SimHost modules, in SimHost just minimalistic implementation but have all interfaces operations ) or what else should be specified/detailed?

Yes, the EQS v1.2 design is highly actionable and ready for immediate implementation. As noted in the design document, all open architectural questions have been resolved and the architecture is officially closed. No further high-level specification is required before writing code.

The recommended path forward is to strictly follow the incremental implementation phasing outlined in your design. You should begin with Phase 1 (Foundations) to establish the end-to-end wire protocol between the CGF (Brain) and SimHost (Muscle) nodes before writing any complex spatial math.

For this initial minimalistic implementation, your breakdown should look like this:

-   **CGF (Brain):** Implement the `EqsSensor` and `EqsCognitiveBuffer` components, along with the synchronous Brain-side reader API and the `WaitForSensor` BTree decorator. You will also need to wire the configuration egress translator (`EqsSensorConfigEgressTranslator`) to replicate the sensor downward via `SmartEgressUtil`, and the result ingress translator to update the cognitive buffer from incoming network events.-   **SimHost (Muscle):** Implement the configuration ingress translator to apply the `EqsSensor` component to the local ghost entity. Then, as you suggested, build a stubbed version of the solver that simply emits empty `EqsResultEvent`s on a fixed timer. Finally, wire the result egress translator to catch those local events and push them back across the DDS boundary.

Once this foundation is proven to work end-to-end and mimics the existing perception CQRS pattern, you can proceed sequentially to Phase 2 (Entity-shaped queries with cheap tests) and Phase 3 (Positional queries with cheap LOS) to start fleshing out the actual solver logic, generators, and tests.

As you move from design into code, just ensure you navigate the engine-specific traps we discussed previously: keep your local `EqsResultEvent` strictly unmanaged for standard event bus publishing, and remember to cast your `[InlineArray]` inside the `EqsCognitiveBuffer` to a span before mutating it to avoid the C# 12 JIT defensive copy bug.

updated the design to v1.3 how could we implement the FindCoverFromTargetTemplate mentioned in the doc, on the SimHost side?

To implement the `FindCoverFromTarget` template on the SimHost, you will use the hand-written C# template form. You will define the query declaratively, leveraging the four-phase evaluation pipeline and the two-pass line-of-sight (LOS) strategy.

Here is how you should structure the implementation:

**1\. Class Definition and Stable Identity** Create a C# class annotated with the `[EqsTemplate]` attribute. You must generate a fresh GUID for the `AssetId` property so that the blueprint registry can derive a stable 32-bit `BlueprintId`. This ensures that any subscriptions from the Brain node survive future class renames or namespace moves.

**2\. The Pure Build Method** Implement a `static void Build(IEqsTemplateBuilder b)` method. This method must be perfectly pure and deterministic; you must not read the `ISimulationView`, any global singletons, or non-deterministic APIs inside it. All runtime variation (like the actual target entity or search radius) will be provided by the sensor's parameter struct at runtime.

**3\. Generator Definition** Inside the `Build` method, configure the builder to use the `CoverPoints` generator. This will pull position-shaped candidate points from the `ICoverProvider` database around the querier.

**4\. Phased Tests Configuration** You will compose the AAA two-pass visibility strategy across the four explicit execution phases. Remember that tests do not infer their phase from cost hints; you must declare them explicitly.

-   **Phase 1 — FilterCheap:** Add a `Distance` test configured as a filter. This instantly rejects cover points that are too far away from the querying agent or the target, preventing the solver from wasting cycles on irrelevant points.-   **Phase 2 — FilterExpensive:** Add a `LineOfSight` test targeting Context 0 (the known threat), set to the _cheap mode_ (using the 1μs occluder grid trace). Configure this as a hard filter to reject any cover point that has clear visibility to the threat.-   **Phase 3 — ScoreCheap:** Add a `CoverQuality` scoring test to evaluate the inherent tactical value of the cover point from the database. You should also add a `Distance` scoring test here with a falloff curve to naturally favor cover points that are closer to the agent.-   **Phase 4 — ScoreExpensive:** This is where you finalize the AAA pattern. Add a second `LineOfSight` test, but set it to _accurate mode_ (using the `RaycastSolverSystem`). Because the solver automatically performs a top-K reduction between Phase 2 and Phase 3, this expensive 3D raycast will only run on the highly viable candidates that survived the cheap pruning. Configure this test to heavily penalize or filter out any cover points where the true 3D geometry fails to block the threat.

By structuring the template this way, the Muscle-side solver will execute a highly optimized, time-sliced evaluation that finds the best tactical cover while respecting both the 4.0ms soft wall-clock budget and the global raycast caps.

Wait. The FindCoverFromTargetTemplate lives on CGF (brain) node only. SimHost has no idea about it. Simhost does not load behavior assembly. Simhost shoud just receive query request via fdp event (traslated from dds network) and provide response, or am i mistaken?

You are mistaken; the Muscle (SimHost) node must absolutely know about the templates and have them loaded in its local registry.

In our architecture, the Brain does not send the full structural logic of the query (which generators to run, which tests to apply) over the network. Instead, the Brain simply replicates the `EqsSensor` component downward, which acts as a lightweight subscription containing only the stable 32-bit `BlueprintId` and the runtime parameters.

Because the Muscle-side solver is responsible for the actual heavy lifting—scheduling and running the generators, executing the phased filters and scoring tests, and producing the ranked results—it must be able to resolve that `BlueprintId` locally. It uses this ID to look up the identical, pre-compiled `EqsQueryTemplate` struct from the registry. If the template only lived on the Brain, the SimHost solver would have no instructions on how to evaluate the query.

Regarding assembly loading, you are correct that historically the `SimHost` application shell initializes an empty `BehaviorRegistry` because BTree and HSM cognitive logic executes purely on the Brain. However, EQS templates are fundamentally different; they are spatial queries that require dense, high-frequency access to the Muscle node's tactical world, including the navmesh, cover database, and the local physics raycast solver.

To support this, your EQS templates will be registered into the `BlueprintRegistry`, which means you must load the AI assembly (`Hrot.AI.Behaviors.dll`) into the SimHost process so the registry is properly populated. The engine's build topology is already prepared for this; `Hrot.SimHost.csproj` explicitly references the `Hrot.AI.Behaviors` project to ensure the DLL is seamlessly copied to the SimHost output directory for hot-reloading.

oh, ok, i see. I need some more explanation of the design. I am still not getting what will the brain (CGF) send when he wants to find a cover. Who (btree? - shat part?) will express the desire for finding a cover and how? How this need will be propa How will it wait for the result? Where from will it read the result? Will the cover be found one single time or is the search runnin continuously?

To find a cover, the cognitive logic on the Brain node—specifically a Behavior Tree (BTree) or Hierarchical State Machine (HSM)—expresses the desire by simply attaching an `EqsSensor` ECS component to the entity. You configure this sensor to reference a pre-compiled query template (such as `FindCoverFromTarget`) using a stable `BlueprintId`, and provide it with runtime parameters like the search radius and target context.

This need is propagated downward to the Muscle node entirely via standard component replication. The `EqsSensorConfigEgressTranslator` on the Brain monitors these `EqsSensor` components and publishes them to the `EqsSensorConfig` DDS topic whenever they are created or their parameters mutate. On the Muscle side, the `EqsSensorConfigIngressTranslator` receives this DDS sample and applies the `EqsSensor` component directly to the local ghost entity. Because the component now exists on the Muscle entity, the solver's standard component iteration will automatically find it and begin evaluation on the next tick.

Your Brain code never deals with request IDs, polling loops, or asynchronous completion. To wait for the result, your BTree simply wraps the logic in a `WaitForSensor` decorator node. This decorator automatically returns `Running` to yield execution until the first valid result lands, specifically gating behaviors that require sensor data to proceed.

Once the Muscle evaluates the cover points, it transmits the best candidates back across the network via discrete state-change events, which the Brain bridges into an `EqsCognitiveBuffer` component on your entity. Your BTree nodes will read the results synchronously from this `EqsCognitiveBuffer` using a simple reader API (e.g., `GetTop`, `GetRanked`).

By default, the search runs continuously. The fundamental mental model of EQS is a standing-query system, meaning the Muscle-side solver will continuously re-evaluate the sensor at a configured refresh rate (such as 10Hz) as the tactical environment changes. To prevent network saturation, the Muscle caches its local evaluation and uses publish policies like `TopChanged` or `ScoreDelta` to only push data back to the Brain when the tactical cover results meaningfully change. If you truly only want to find cover once, you can configure the sensor with `RefreshOnce: true`, but continuous evaluation is the primary pattern.

ok, i am got that concceptually. But i am still missing lots of details. how a bree or hsm adds an EqsSensor to an entity? Is there any special action for that? how we parametrize such an action? How do we stop desiring the sensor? (so it gets disposed on muscle node?)

To add an `EqsSensor` to an entity, you will author specific BTree or HSM action nodes (such as `Action_FindCover`) rather than relying on a generic "add sensor" action. Inside this action delegate, you use the BTree execution context to attach the component directly to the entity, for example by calling `ctx.World.AddComponent(ctx.Self, new EqsSensor { ... })`.

You parameterize this action by defining a custom unmanaged struct (e.g., `FindCoverParams`) that represents your behavior's runtime variables. The engine automatically projects the `BrainBlackboard.BehaviorParameters` memory into this struct, allowing your action method to receive it as `ref FindCoverParams p`. Inside your action, you read these blackboard parameters—such as the search radius or the target context—and copy them into the `EqsSensor` component's parameter payload before attaching it to the entity. Once attached, your BTree can wrap its subsequent logic in a `WaitForSensor` decorator node, which automatically returns `Running` to yield execution until the first result lands in the `EqsCognitiveBuffer`.

To stop desiring the sensor, your BTree or HSM simply removes the `EqsSensor` component from the entity (e.g., via `ctx.World.RemoveComponent<EqsSensor>(ctx.Self)`). Because the subscription is driven entirely by the presence of the component, there are no bespoke unsubscribe messages. The component removal is automatically replicated downward to the Muscle node. During the Muscle-side solver's next tick, it filters its work queue against the live snapshot; upon seeing the `EqsSensor` component is gone, the solver silently drops the query and abandons any in-flight raycast IDs associated with it.

How BTree removes the EqsSensor component from the entity? Is this part of the Action\_FindCover somehow in a sense that if the action stops processing, the sensor will be uninstlled r something like that?

No, the removal is not automatic. In our FastBTree implementation, action nodes like `Action_FindCover` are pure, stateless static methods and do not have `OnExit` or `OnAbort` lifecycle callbacks. If the execution pointer simply moves away from your action—for example, if a higher-priority branch in an `ObserverSelector` interrupts it—the action will not receive any notification to execute cleanup code.

To properly manage the `EqsSensor` component's lifecycle without leaking standing queries, you must handle two distinct scenarios:

**1\. Natural Completion** If your `Action_FindCover` successfully finds cover or decisively fails, you must explicitly remove the component yourself right before exiting the node logic. You simply call `ctx.World.RemoveComponent<EqsSensor>(ctx.Self)` and then return `NodeStatus.Success` or `NodeStatus.Failure`.

**2\. Forceful Interruption (Behavior Switch)** If the entity's behavior is forcefully changed mid-query (e.g., the mission plan advances and `BehaviorIngressSystem` assigns a new behavior), the current BTree is blindly aborted and restarted. To guarantee the sensor is uninstalled in this scenario, you must mirror the engine's arbitration pattern currently used by `LocomotionChannel` and `WeaponChannel`.

-   **Stamp the Instance:** When your BTree action initially adds the `EqsSensor` component, it must read the entity's `BehaviorState` and copy its `InstanceId` into a tracking field on the sensor (e.g., `sensor.BehaviorInstanceId = behavior.InstanceId`).-   **Arbitrate:** You should create an `EqsSensorArbitrationSystem` (or simply append the logic to the existing `ChannelArbitrationSystem`). This system runs in `SystemPhase.Simulation` and queries all entities with both an `EqsSensor` and a `BehaviorState`. If it detects that `sensor.BehaviorInstanceId != behavior.InstanceId` (which happens because `BehaviorIngressSystem` automatically increments the `InstanceId` during any behavior switch), it calls `repo.RemoveComponent<EqsSensor>(entity)`.

By combining manual component removal on natural completion with an arbitration system for abrupt interruptions, you guarantee the `EqsSensor` is always cleanly uninstalled from the Brain entity. Once removed, standard DDS component replication will immediately mirror the removal to the Muscle node, and the Muscle-side solver will silently drop the orphaned query from its work queue on the next tick.

BUt the Btree can simply jump to another branch of the same behavior where finding a cover is no longer necessary. How can we deal with this? We still want the sensor to be removed in this case.

You have correctly identified a fundamental characteristic of FastBTree: because pure action nodes are stateless `NodeLogicDelegate`s, they do not receive an `OnAbort` or `OnExit` callback when a higher-priority branch (like an `ObserverSelector`) interrupts them and shifts the execution pointer away.

To deal with this intra-behavior branch shifting, you should not try to force stateful exit callbacks into the BTree. Instead, we use an idiomatic ECS pattern: a **Brain-Side Tick Lease**.

By binding the lifespan of the `EqsSensor` component to the continuous execution of the BTree node that requires it, the sensor will automatically clean itself up the exact frame the BTree decides it is no longer necessary.

Here is how you implement this:

**1\. Add a Lease Field to the Sensor** Update your `EqsSensor` component struct to include a tracking field for the simulation tick:

```
public struct EqsSensor
{
    // Existing fields (BlueprintId, Params, etc.)
    public uint LastPolledTick; 
}
```

**2\. "Touch" the Sensor During BTree Execution** Inside your `Action_FindCover` node (and ideally inside your `WaitForSensor` decorator), whenever the node executes and returns `NodeStatus.Running`, you must update this lease. Using the `BTreeContext` passed into the delegate, grab the sensor and stamp it with the current frame count:

```
ref var sensor = ref ctx.World.GetComponentRW<EqsSensor>(ctx.Self);
sensor.LastPolledTick = (uint)ctx._frameCount;
```

**3\. The Cleanup System** Create a new ECS system named `EqsSensorCleanupSystem`. This system must run in `SystemPhase.Simulation` and be registered in your `CognitiveRuntimeModule` strictly _after_ the `BTreeTickSystem`.

This system will query all entities that have an `EqsSensor` component. If it finds a sensor where `LastPolledTick < view.Tick`, it knows the BTree execution pointer jumped to a different branch and orphaned the sensor this frame. The system then cleanly removes it:

```
[UpdateInPhase(SystemPhase.Simulation)]
public class EqsSensorCleanupSystem : IEcsModuleSystem
{
    public void Execute(ISimulationView view, float deltaTime)
    {
        var repo = (EntityRepository)view;
        var query = repo.Query().With<EqsSensor>().Build();
        
        foreach (var entity in query)
        {
            var sensor = repo.GetComponent<EqsSensor>(entity);
            if (sensor.LastPolledTick < view.Tick)
            {
                repo.RemoveComponent<EqsSensor>(entity);
            }
        }
    }
}
```

_(Note: This pattern mirrors how our_ _CognitiveCleanupSystem_ _sweeps transient interrupt bytes at the end of the simulation phase__.)_

**4\. The Network Cascade** Because the `EqsSensor` component is now physically removed from the Brain entity, the existing CQRS pipeline takes over automatically:

-   The `EqsSensorConfigEgressTranslator` detects the component removal and publishes the deletion via standard DDS replication.-   The Muscle-side ingress translator removes the component from the Muscle ghost entity.-   On the very next tick, the Muscle-side solver filters its work queue against the live snapshot, sees the `EqsSensor` component is gone, and silently drops the query and any in-flight raycasts.

By using this tick-lease pattern, you guarantee that standing queries are never leaked on the Muscle node, while keeping your BTree nodes completely stateless and free of manual cleanup logic.

