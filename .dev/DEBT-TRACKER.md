# Technical Debt Tracker

This document tracks P2 and P3 technical debt, refactoring opportunities, and deferred minor issues discovered during development and reviews. P1 critical issues are fixed immediately in the next batch.

| Status | Priority | Category | Source Batch | Description | Target Fix |
|---|---|---|---|---|---|
|   | P3 | Architecture | PACK3-BATCH-01 | Two `INetworkTopology` interfaces exist: `Fdp.Interfaces.INetworkTopology` (takes `long tkbType`) vs `ModuleHost.Core.Network.Interfaces.INetworkTopology` (takes `ReliableInitType`). Ongoing ambiguity resolved with `using` aliases. Full migration to `Fdp.Interfaces` version recommended. | Opportunistic |
|   | P2 | Architecture | PACK3-BATCH-02 | FDP system ordering audit: `[UpdateAfter]`/`[UpdateBefore]` attributes should be audited across all FDP systems used in cluster-mode flat `_kernelGroup`. Root cause: `BallisticsSystem` was sorted after `HitResolutionSystem` in flat group, breaking raycast pipeline. Audit multi-system interactions for missing constraints. | Opportunistic |
|   | P3 | Architecture | PACK3-BATCH-02 | `ZoneEnvironmentData` struct placed in `FDP.Toolkit.CarKinem` but is conceptually geographic. Move to `FDP.Toolkit.Geographic` in a future cleanup. | Opportunistic |
|   | P3 | Logging | PACK3-BATCH-03 | `ScenarioFileService.LoadScenario` silently discards zone data when no `IZoneManagerService` is injected. Add a warning log for diagnosability. | Opportunistic |
|   | P3 | Documentation | PACK3-BATCH-04 | Canvas coordinate convention differs between `ActivateAreaAuthoringTool` (ENU: X=East, Y=North) and `ActivateRouteAuthoringTool` (XZ: X=East, Z=North). Add explicit comments explaining the convention in both methods to avoid future authoring tool errors. | Opportunistic |
|   | P3 | Correctness | PACK3-BATCH-01 | `EntityLifecycleModule.AcknowledgeConstruction` has no idempotency guard — calling it twice for the same entity publishes two `ConstructionAck` events. Add guard when persistence/recording is scoped. | Opportunistic |
|   | P3 | Performance | PACK3-BATCH-01 | `UrbanCombatValidator` rebuilds `TkbIdentity` query each tick. Negligible for small scenarios; for large-entity scenarios consider cached query or early-exit once both actors are found. | Opportunistic |
| ✅ | P2 | Correctness  | TC3-BATCH-02 | `OnTimePulseReceived` hard-snap branch sets `_lastUpdateRawTicks = SyncedWallTicks` (master-domain). Must be `_getTick()` (local raw tick). Corrupts `rawDelta` by −offset on the frame following a hard-snap when offset ≠ 0. Fix: `_lastUpdateRawTicks = _getTick()`. | ✅ TC3-BATCH-03 Corrective-01 |
|   | P3 | Correctness  | TC3-BATCH-02 | `hardSnap = _masterWallClockOffset == 0` fires on every sync when the legitimate offset is zero (same-machine scenario). Use a separate `_firstSyncDone` sentinel. | Opportunistic |
|   | P3 | Testing      | TC3-BATCH-01 | `MasterSyncController_Step_EmitsDebugLog` uses a global NLog `MemoryTarget` (not thread-safe under parallel test execution) and asserts a raw string literal `"[TC3][Master] STEP"`. Improve by (a) using a test-scoped FdpTestLogSink, and (b) referencing a `public const string DebugStepPrefix` on the controller. | Opportunistic |
|   | P3 | Correctness  | TC3-BATCH-01 | `MasterSyncController.SeedState` restores `_totalWallTicks` but does NOT reset `_lastPulseTicks`. After a seed with a low `TotalWallTicks`, the first `MaybePublishTimePulse` call may fire immediately. Self-corrects within one pulse interval; low impact. | Opportunistic |
|   | P1 | Architecture | BUG1-BATCH-02 | `BehaviorFinished` string trigger from IOS is not implemented in `MissionControlRequestSystem` parser, falling back to `TimerElapsed` 0s causing vehicles not to move. | BUG1-BATCH-03 |
| ✅ | P2 | Correctness  | TC3-BATCH-04 Corrective-02a | `DrainModeSwitchEvents` `_isTimeSynced` guard consumed+dropped `SwitchTimeModeEvent` for same-machine slaves (no NTP translator), breaking TimeControlIntegrationTests. Fixed: guard removed; multi-machine protection is the natural `SyncedWallTicks >= barrier` check. | ✅ TC3-BATCH-05 |
| ✅ | P2 | Correctness  | TC3-BATCH-04 Corrective-02b | `UpdateBarrierPending` advanced sim-time via `AdvanceContinuousTime` for 200ms while barrier waited (regression from TC3-P2-T01 fixing barrier to real-clock base). Integration tests saw 200ms of advancing sim-time while paused. Fixed: freeze sim-time in BarrierPending. | ✅ TC3-BATCH-05 |
|   | P3 | Testing      | TC3-BATCH-04 | `ClockSkewDriftTests.ClockSkew_WithPeriodicResync_OffsetStaysWithin2ms` uses `Stopwatch.Frequency * 0.002` for 2ms tolerance. On very-high-frequency hardware counters the window is very large; consider using a fixed-tick test with a mocked frequency. (D-TC3-04) | Opportunistic |
|   | P3 | Testing      | BUG1-BATCH-02 | `SimHostApp.OnLoad()` translator separation lacks test coverage. | BUG1-BATCH-03 |
|   | P3 | Architecture | BUG1-BATCH-02 | `IosSubsystem` node-id isn't fully plumbed into `IosMock.InitializeEmbedded` yet. | BUG1-BATCH-03 |
|   | P3 | Testing      | BUG1-BATCH-02 | No integration test covering the full `HandleAbort → SendControlCommandAsync → OnAckReceived → CommitInFlight = false` round-trip. | BUG1-BATCH-03 |
|   | P3 | Testing     | TwoAck-BATCH-02  | Headless ImGui context creation is costly per test. Consider sharing a single `ImGuiTestFixture` across test classes. | TwoAck-BATCH-03 |
|   | P2 | Engine Maths | OC1-BATCH-02 | Coordinate conversion bug matching Canvas Y to World Y (Altitude) instead of World Z (North) in `ActivateAreaAuthoringTool`, etc. causing route flattening bugs. | OC1-BATCH-03 |
|   | P2 | Bug          | OC1-BATCH-02 | `RouteRenderLayer.PickEntity` hardcoded to return `null`, rendering drawn routes untouchable via Mouse/Map UI. | OC1-BATCH-03 |
|   | P2 | Networking   | OC1-BATCH-02 | IG inspector network state divergence when running deletion; local DestroyEntity system invoked exclusively over propagating Network `DeleteEntityRequest`. | OC1-BATCH-03 |
|   | P3 | Documentation | BATCH-17 review | `DEM1-DESIGN.md` §6.5 latch table still lists InsurgentKilled **≤tick 400** as a design window; `DEM1-TASK-DETAIL` / tests normatively use **maxTicks 600** only. Add matching caveat or soften the table row for consistency. | Opportunistic |
|   | P3 | Architecture | BATCH-17 review | `TransformSyncSystem` exists in both `Fdp.Examples.Common` and `Fdp.Examples.NetworkDemo` (intentional duplicate). Risk: logic drift on future edits — consolidate later or add a shared test/fixture if both paths must stay identical. | Opportunistic |
|   | P3 | Process       | BATCH-16 review | **Batch ID collision:** repo-root `.dev-workstream/batches/BATCH-16-INSTRUCTIONS.md` (DEM1) vs `FDP/.dev-workstream/batches/BATCH-16-INSTRUCTIONS.md` (UrbanCombat Phase 7). Prefer disambiguation in future (e.g. prefix **DEM1-BATCH-*** vs **BCS-BATCH-***). | Opportunistic |
|   | P2 | Correctness   | CGF-1-BATCH-02 review | `ClusterSlave.DispatchCommand` invokes `PrepareAsync` without awaiting then calls `Commit` — not real 2PC. **CGF1-S0202** delivered event/handler wiring only; the fire-and-forget stub is the current intended behaviour. Replace with proper prepare/commit/abort sequencing when persistence / recording is scoped. | CGF1-S0304 |
|   | P3 | Testing       | CGF-1-BATCH-02 report | `ClusterSlaveHeartbeatTests` does not assert 1 Hz heartbeat cadence (task detail §S0104). | Opportunistic |
|   | P3 | Testing       | CGF-1-BATCH-09 report | No automated subprocess test for `dotnet run --project Hrot.ClusterRunner -- --mode ci --scenario minimalci_01` (task-detail success condition); in-process harness + entity snapshot used instead. | Opportunistic |
|   | P3 | Testing       | CGF-1-BATCH-23 report + **CGF-1-BATCH-24** | **IG** `TestHook_ClusterSlave` / handler registration: no unit harness when DDS/network disabled — deferred from BATCH-23 Part C. | Opportunistic |
|   | P3 | Testing       | CGF-1-BATCH-22 review | **`ClusterMasterStoryTests`**: BATCH-22 report cited a **multi-node mixed-ACK** test — **not** in repo; add when multi-node **ManageEpisode** is normative. | Target: **CGF-1-BATCH-23** Part A (optional) |
|   | P3 | Hygiene       | CGF-1-BATCH-21 report | Report lists **DESIGN** under `Hrot.Orchestrator/CGF-1-DESIGN.md` — actual file is **`.dev/cgf-1/CGF-1-DESIGN.md`**. Correct in **REPORT** / onboarding docs on next edit. | Opportunistic |
|   | P3 | Specification | CGF-1-BATCH-19 review | §**CGF1-S0308** lists **`PrefetchStory`**; **`PlanManageEpisode`** uses **`PrefetchScenario`** — align TASK-DETAIL or document intentional reuse. | Opportunistic |
|   | P3 | Testing       | CGF-1-BATCH-18 review | **`FullBranchPipelineTests`**: Live-from-Replay step uses **direct** **`ReplayLoadDsmHandler`**; **`SimHost.ClusterSlave`** deferred **`Commit`** is covered by **`NodeBootstrapperReplayTests`** — optional: single E2E through **`ClusterSlave`** for the pipeline test. | Opportunistic |
|   | P3 | Product       | CGF-1-BATCH-17 review | **`FailLoudRecordReplayStub`**: no **`NodeOpStatus`** NAK — orchestrator may count CGF as ACK-success while brain recording absent; tighten when CGF gets **`NodeOpStatus`** writer and mandatory ACK parity. | Opportunistic |
|   | P3 | Testing       | CGF-1-BATCH-25 review | **`DsmE2eScriptTests`** lacks `[Trait("Category","DsmE2e")]` to enable PR-build filtering via `--filter "Category!=DsmE2e"`. 3-line change. | Opportunistic |
|   | P3 | Architecture  | CGF-1-BATCH-25 review | **`ParseModeString`** is private — tests must go through `Validate()` which requires `NoWait=true`. Elevate to `internal` with `[InternalsVisibleTo]` for cleaner unit tests. | Opportunistic |
|   | P3 | Safety        | CGF-1-BATCH-25 review | `cgf,ci` combo silently drops `ci` token — add explicit validation error when `CI` flag is combined with others. | Opportunistic |
|   | P3 | Specification | CGF-1-BATCH-16 review | §CGF1-S0304 lists **`RecordingConfiguration`** under **Fdp.Kernel**; implementation lives in **`FDP.Toolkit.Replay`** — reconcile TASK-DETAIL / layering note. | Opportunistic |
|   | P3 | Testing       | CGF-1-BATCH-16 report | **`CheckpointIOWorkerTests`** reported as **timing-sensitive** under parallel test load — stabilize (deterministic latch, collection isolation) if CI flakes. | Opportunistic |
|   | P3 | Testing       | CGF-1-BATCH-10 report | IG `ClusterSlave` `SetFilter`: no DDS integration test; manual verification only. | Opportunistic |
|   | P3 | Correctness   | CGF-1-BATCH-06 review | `SteppedMasterController.SeedState` / `GetCurrentTime` derive `TotalWallTicks` from `_unscaledTotalTime * Frequency` while `MasterTimeController` uses a Stopwatch tick accumulator — after `SwitchTo`, barrier-related wall ticks can diverge from `GetCurrentState().TotalWallTicks` passed in. | ✅ CGF-1-BATCH-07 (`_totalWallTicks` field; seeded from state; accumulated in `Step()`) |
|   | P3 | Testing       | CGF-1-BATCH-06 review | `SlaveTimeControllerTests.SeedState_BypassesJitterFilter` seeds `TotalWallTicks = 0` only; does not assert non-zero seeded wall ticks survive the next `Update()`. | ✅ CGF-1-BATCH-07 (`SeedState_NonZeroWallTicks_ArePreservedAfterUpdate`) |
| ✅ | P3 | Testing       | CGF-1-BATCH-27 review | `InjectStory_AutoGeneratesStoryId` in `OrchestratorScenarioPanelTests` verifies DDS round-trip for ManageEpisode payloads but does not exercise the panel button path (direct `Guid.NewGuid()` call in `RenderStoriesSection`). A headless ImGui click simulation test would close this gap. Defer to `ClusterScenarioPanelTests` when old panel tests are migrated. | ✅ CGF-1-BATCH-30 (OrchestratorScenarioPanelTests deleted; behaviour covered by ClusterScenarioPanelTests) |
| ✅ | P3 | Time          | CGF-1-BATCH-27 report | `OrchestratorSubsystem._drillTime` is local orchestrator simulation clock, not the cluster's actual drill time. | ✅ CGF-1-BATCH-29 (panel reads `cache.MasterSimTime`; `_drillTime` is now dead code — see new P3 below) |
|   | P3 | Time          | CGF-1-BATCH-27 report | `StepTime` in `OrchestratorSubsystem.TimeControlRequested` handler is a functional no-op: `_timeKernel.StepFrame(1f/60f)` throws `InvalidOperationException` because `MasterTimeController` does not implement `ISteppableTimeController`. Fix requires swapping to `SteppingTimeController` on pause and restoring on resume. | CGF1-S0507 (IOS wiring touches this) |
| ✅ | P3 | Configuration | CGF-1-BATCH-28 review | `OrchestratorScenarioPanel.RenderArchiveSection` uses hardcoded `C:\FDP_Temp\nas` NAS root for `ScanNasDrills`. | ✅ CGF-1-BATCH-29 (`ClusterScenarioPanel` reads from `ClusterUiCache.ArchivedDrills` / `UnarchivedLocalDrills` published via `AssetInventoryTopic` from ClusterMaster) |
| ✅ | P3 | Hygiene       | CGF-1-BATCH-29 review | `OrchestratorScenarioPanel.cs` (dead code) and `OrchestratorScenarioPanelTests.cs` still exist alongside `ClusterScenarioPanel`. Delete old file; migrate relevant tests to `ClusterScenarioPanelTests` or `Hrot.Orchestrator.Tests`. | ✅ CGF-1-BATCH-30 (both files deleted) |
| ✅ | P3 | Architecture  | CGF-1-BATCH-29 review | `OrchestratorSubsystem._drillTime` field computed but never read (panel reads `cache.MasterSimTime`). Remove dead field + assignment. | ✅ CGF-1-BATCH-30 (dead field removed) |
|   | P3 | Architecture  | CGF-1-BATCH-29 review | `ClusterUiCache.DrainClusterOpStatus()` fallback clears all in-flight transactions when `RequestId` → `TransactionId` match fails. Add `TransactionId` to `ClusterOpStatus` wire format for unambiguous correlation. | Opportunistic |
|   | P3 | Architecture  | CGF-1-BATCH-29 review | `NodeOpCommand` carries no `TargetState` wire field; cache parses from `PayloadJson`. Add dedicated wire field in a future DataModel revision. | Opportunistic |
| ✅ | P3 | UX            | CGF-1-BATCH-27 report | `OrchestratorScenarioPanel._replayDuration` defaults to 3600 s and is never updated from the selected drill's `*.meta.json` after "Load Replay" is clicked. `GetReplayDuration` helper exists and is unit-tested. Wire on load button click. | ✅ CGF-1-BATCH-28 (wired on Load Replay click; reads `recording.meta.json`) |
|   | P3 | Testing       | CGF-1-BATCH-28 review | `StorageGatewayTests` cancellation cleanup tests use pre-cancelled CTS (no files created) — partial-file bag cleanup is structurally correct but not exercised mid-flight. A slow-mock test would give deeper coverage. | Opportunistic |
|   | P3 | Architecture  | CGF-1-BATCH-28 review | `ClusterMaster.ConsumeNodeOpStatuses` has 5 inline operation-type branches (BranchTask, ManageEpisodeTask, TransitionTx, SerializeLocalTask-normal, SerializeLocalTask-archive). Refactor to dispatch table when a 6th type is added. | Opportunistic |
|   | P3 | Configuration | CGF-1-BATCH-28 review | `OrchestratorScenarioPanel.RenderArchiveSection` uses hardcoded `C:\FDP_Temp\nas` NAS root for `ScanNasDrills`. Will be superseded by `ClusterUiCache` / `AssetInventoryTopic` in S0506. | CGF1-S0506 |
|   | P3 | Hygiene       | CGF-1-BATCH-04 review | `public abstract class ISysOpStep` reads like an interface; consider renaming to `SysOpStep` when churn is acceptable. | Opportunistic |
