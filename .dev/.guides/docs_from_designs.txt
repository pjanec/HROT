your task is to manage the process of updating the documentation in docs\projects based on current state of the sources. Your task is not to write the docs yourselfm you should orchestrate it by delegating the job to sub-agents. pls go over individual subfolders of .dev folder (ignore .dev\_DONE) and for each subfolder (which represents a design topic with one or multiple design documents inside - you can use the TASK-TRACKER to find out what was the topic in thes folder) start a sub-agent (use development agent model Claude Sonnet 4.6, do not use the exploration sub-agent!) Tell the sub-agent to analyze the design documents in the folder to see what might have been added to the codebase, check the real codebase state and update the  documentation in docs\projects based on current state of the sources. Make sure all the design files get processed.
Before you start running sub-agent, create a task list where you store the design topics to be processed. Take inspiration from 00-PROJECT-CHECKLIST.md 
Make the sub-agent tell you what he has done - what parts of the docs he changed/added. After the sub-agent finishes, make a git commit and place the change report into it. Then move to another item from your task list and run another sub-agent until all items are done.
You shoud write your task list into file docs\01-TOPIC-CHECKLIST.md.
The sub-agent should not change any source files. He should update existing ones and also add new documentation files if there are new not yet documented projects.
