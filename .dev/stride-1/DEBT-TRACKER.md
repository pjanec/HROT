# Stride Integration — Technical Debt Tracker

**Reference:** [TASK-DETAIL.md](./TASK-DETAIL.md) · [TASK-TRACKER.md](./TASK-TRACKER.md) ·
[Stride-Integration_v0_3.md](./Stride-Integration_v0_3.md)

Debt items found during a phase that are not fixed in that phase are recorded here, prioritized,
and assigned a target phase/task. Add a row when you defer a fix; do not delete RESOLVED rows.

| ID | Source | Description | Priority | Target | Status |
|----|--------|-------------|----------|--------|--------|
| STR-D1 | STR-P0-T3 | `CycloneDdsDisableCodeGen=true` set on `HrotStrideApp.Game`/`.Windows` to dodge a CycloneDDS codegen command-line-length failure when Stride DLLs are in scope. Re-verify Mode 2 DDS still works (topic types are generated in `Fdp.Toolkits` where defined, so the app consuming them should be fine — confirm at bring-up). | P2 | P6 (STR-P6-T2/T5) | OPEN |
| STR-D2 | STR-P0-T4 | `FdpStrideTransform.ScreenRayToFdp` has no unit test and a muddy screen-coordinate contract (comments waffle pixel vs normalized [0,1]). Extract a pure `(Matrix vp, Vector2 ndc)` core overload that the `CameraComponent` overload delegates to, and unit-test it; nail down the pixel/viewport contract. | P3 | P5 (STR-P5-T2/T3, editor picking) | OPEN |
| STR-D3 | STR-P0-T1 (report insight) | `Stride.Engine.Entity` vs `Fdp.Core.Entity` namespace collision — any file using both must qualify (`global::Stride.Engine.Entity`). Latent footgun across all `Hrot.Stride.Core` work. | P3 | ongoing | OPEN |
| STR-D4 | STR-P0-T3 | Stride asset compilation was verified only as an "up-to-date skip" of `StrideCompileAsset`, not a forced clean compile. The real proof is booting the app — exercise it in the STR-P0-T8 end-to-end smoke. Also: `StrideHrotGame`'s GPU/render path is unverified headlessly (needs a GraphicsDevice) — same obligation. | P2 | P0 (STR-P0-T8) | **PARTIAL** (BATCH-03): binding logic proven by fake-factory T7/T8 tests; concrete `StrideVisualFactory` model path compiled+wired and *looks* correct vs the [VERIFY]'d APIs — but **never run against a live GraphicsDevice** (no headless GPU; `StrideHrotGame.Run()` needs SDL2+DirectX; no Stride 4.2 off-screen test harness). **Carried obligation: a developer must run `HrotStrideApp` on a GPU to confirm models actually render.** BATCH-10 wired the live path (entry point → StrideHrotGame → EditorStrideSubsystem → concrete StrideVisualFactory; 6 UrbanCombat spawns; camera bound to compositor slot) — **ready for the human's GPU run to discharge this.** See BATCH-10-REPORT "How to Run and What You Should See". |
| STR-D5 | STR-P0-T6 | `EditorStrideSubsystem` registers sim/post-sim systems flat — no `TogglableSimulationGroup`/`TogglablePostSimulationGroup`. P1's `BulletReverseSyncSystem` must live in a `TogglablePostSimulationGroup`; P5 replay passes that group to `ReferenceReplayLoadHandler`. Introduce the togglable groups when wiring reverse-sync. | P2 | P1 (STR-P1-T5) | RESOLVED (BATCH-06): `BulletReverseSyncSystem` wrapped in a `TogglablePostSimulationGroup("BulletReverseSync")`; `Enabled=false` severability proven. P5 still needs to pass it to `ReferenceReplayLoadHandler`. |
| STR-D13 | STR-P1-T6 | Owned entities' visuals are (correctly) no longer pose-updated by the sync fork — they must follow the Bullet body. But the concrete `StrideVisualFactory` creates the visual as a *separate* entity from the physics body. The concrete `BulletPhysicsBodyService`/factory must unify visual ↔ body (visual is the body or its child, design §6.2) at GPU bring-up, else owned entities render frozen. | P2 | BATCH-17 | **RESOLVED (BATCH-17):** `BulletPhysicsBodyService.CreateBody` attaches the physics component to the entity's existing visual Stride entity (the one with the `ModelComponent`), so Bullet moves the model. |
| STR-D6 | STR-P0-T6 (test run) | 10 pre-existing failures in `Hrot.StrideMock.Tests.SharedApplicationBootstrapperTests` (NedReplication/TimeControl/BuildOrchestration/KernelInitialize/etc.). Source untouched since baseline `6bb3153d` — **not caused by stride-1**. Informational; outside stride-1 scope but flagged for repo health. | P3 | — | OPEN |
| STR-D7 | STR-P0-T6 | `OfflineNetworkFactory` lives in `Hrot.Editor`, which drags Raylib/WinForms/ImGui into `HrotStrideApp.Game` as transitive deps (accepted per §1.1/§8.3, but heavier than necessary for headless/Mode-2). Consider extracting `OfflineNetworkFactory` to a UI-free assembly. | P3 | P5 / cleanup | OPEN |
| STR-D8 | STR-P0-T6 | Minimal TKB (`TkbTemplate("TestUnit", tkbType:1L)`) + `InitialComponents`-based spawn is a P0 shortcut to avoid the `Fdp.Examples.Scenarios` dependency. P1+ needs the real TKB/scenario template chain (`HrotEnvironment.CreateTkb()` + `UrbanCombatNewScenario.RegisterUrbanCombatTkbTemplates()`), which T7/T8 (UrbanCombat demo entities) will require anyway. | P2 | P0 (STR-P0-T8) | RESOLVED (BATCH-03): EditorStrideSubsystem now calls UrbanCombatNewScenario.RegisterUrbanCombatTkbTemplates(TkbDb) in Initialize(). Fdp.Examples.Scenarios ProjectReference added to HrotStrideApp.Game. All 5 types (1001-2003) carry StrideRenderModelDefDto. |
| STR-D9 | STR-P0-T8 | Concrete `StrideVisualFactory.CreateProceduralVisual` creates a **mesh-less** entity (no `ModelComponent`) — a procedural capsule/box is invisible even on a GPU, so the §6.5 "runnable before/without real art" fallback isn't truly functional. P0 demo uses model refs so the smoke is unaffected. Build real primitives via `Stride.Rendering.ProceduralModels` (capsule/box) when a GPU env is available. | P2 | P1 (when GPU run happens) | OPEN |
| STR-D10 | STR-P0-T8 | `StrideVisualFactory.CreateModelVisual` swallows `Content.Load` failures into a silent placeholder (only `Debug.WriteLine`) — a fail-quiet path the DEV-GUIDE forbids; a missing/miscompiled asset would show placeholders instead of failing loud. Replace with a loud failure (or a clearly-visible error marker + real logging) at GPU bring-up. | P2 | P1 (when GPU run happens) | RESOLVED (BATCH-10): `Content.Load` failure now logs to `Debug.WriteLine` + `Console.Error` then throws `InvalidOperationException` with the asset URL and inner exception. The silent placeholder and `CreatePlaceholderEntity` dead-code path are removed. |

| STR-D11 | STR-P1-T2 | `Stride.Physics.Simulation` is `internal`/owned by `PhysicsProcessor`, so the headless seam `IPhysicsBodyService` was used. | P2 | BATCH-17 | **RESOLVED (BATCH-17, pending GPU confirm):** concrete `BulletPhysicsBodyService` implemented against the live `Simulation` (obtained from `SceneInstance.GetProcessor<PhysicsProcessor>()` at `BeginRun`), wired into live editor_stride (NoOp only in headless tests). Capsule->CharacterComponent(+gravity), OrientedBox->kinematic Rigidbody, MoveKinematic via `Simulation.ShapeSweep`. Physics behavior is GPU-verified by the user (Physics Drop=D0 / Physics Walk=F1 harness cases). |
| STR-D12 | STR-P1-T2 | `CrowdAgentUpdateSystem` still mutates `SimTransform.Position` (integrates velocity×dt) even though P1 excludes `LinearKinematicsSystem`. The BATCH-04 instructions explicitly state the P2-T4 refactor (velocity-only, no position write) is deferred. For Phase 1, the CrowdAgentUpdateSystem will fight the Bullet reverse-sync for crowd entities. Document as open until P2-T4 removes the integration. | P2 | P2 (STR-P2-T4) | RESOLVED (BATCH-08): `CrowdAgentUpdateSystem` refactored to write only `CrowdMotorIntent.Velocity`; no longer touches `SimTransform` or `SimVelocity`. `NavTestHarness` updated with `IntegratePositionsFromIntent` stand-in so headless navigation tests remain green. |

| STR-D14 | STR-P2-T4 | The shared `CrowdAgentUpdateSystem` is now **velocity-only for all nodes** (writes `CrowdMotorIntent`, no `SimTransform` integration). The only production consumer of `CrowdMotorIntent` is `BulletCharacterMotor` (Stride). A non-Stride node that relied on this system's self-integration for crowd movement would now freeze crowd agents. Verified **no test regression** vs the pre-stride-1 baseline (SimHost is the fake being replaced; design §5.3 mandates this), but flagged: if non-Stride crowd movement is ever needed, add a `CrowdMotorIntent` integrator there. | P3 | — | OPEN — BATCH-19 confirms the Stride path fully works with real DtCrowd (F5 chain, SC5 headless proof). Non-Stride paths remain un-addressed per spec. |

| STR-D15 | STR-P4-T2 (test run) | **Originally 25** pre-existing failures in `Fdp.Examples.Scenarios.Tests`. **Current baseline at blueprint-integ-1 HEAD (BATCH-25) is 30 failures.** The 5-test increase (3× DdsMsgSerialization + re-confirmed at BATCH-26 post-report) is due to branch divergence from `main` where commit `b200cd14` (FDP-G04 Event-2030 fix) healed those tests. Until `main` is merged into `blueprint-integ-1`, the effective baseline here is 30. BATCH-26 introduces **zero new failures** (stash-isolated verification). Informational; flagged so future batches touching `Fdp.Examples.Scenarios` know the baseline. | P3 | merge main→blueprint-integ-1 | OPEN |

| STR-D17 | BATCH-17 (GPU demo) | The MilitaryAPC (and other vehicles) render the **placeholder `Models/Box2x1x1`** model, not a real vehicle mesh. The collider is now derived from the model's bounding box so it's consistent, but the demo "car" looks like a low box. Swap in a real APC/vehicle mesh (content task) and re-confirm the bbox-derived collider still looks right. | P3 | content / art pass | OPEN |
| STR-D18 | BATCH-17 (GPU demo) | **Vehicle authority deviation.** The OrientedBox vehicle was switched from an FDP-kinematic `MoveKinematic` mover to a **Bullet DYNAMIC rigidbody** (velocity-driven, solver collision) because the hand-rolled kinematic sweep was unworkable (floor-graze, pass-through, pausing). This means **Bullet now owns the vehicle's motion**, not FDP kinematics — a departure from design §6.x intent. Reverse-sync still mirrors the body pose/velocity into `SimTransform`/`SimVelocity`, so FDP sees it. **Revisit for Mode-2 (STR-P6):** a dynamic body's authoritative state/replication differs from a kinematic one (egress/dead-reckoning assumed kinematic vehicle motion); confirm the velocity invariant + handover still hold for a dynamic vehicle, or re-introduce a robust kinematic path. Config note: vehicle uses Friction=0.02, AngularDamping=0, AngularFactor=(0,1,0) (yaw-only), CanSleep=false so the commanded velocity/yaw are achieved (`[VehicleYaw] ratio=100%`). | P2 | P6 (STR-P6-T3/T4) | OPEN |
| STR-D19 | BATCH-17 (GPU demo) | `VehicleWaypointController` (closed-loop go-to-goal steering) PROVES the dynamic vehicle is steerable to a point, but it has **NO obstacle avoidance** — the F3 demo places waypoints in open space. For real vehicle navigation, wire this controller into the navmesh/path pipeline (`VehicleCommandSystem` / DotRecast path → waypoints) so vehicles follow baked routes around obstacles. The controller keeps a minimum speed (bicycle model can't turn at speed 0) and has a minimum turning radius `R_min = wheelBase/tan(maxSteer)` ≈ 2.9 m (points inside it need multi-maneuver, out of scope). | P2 | P2/post-demo (vehicle nav) | **FULLY RESOLVED (BATCH-20):** Navigation now driven through the **PRODUCTION FDP front door** (`NavigationIntent` / `LocomotionChannel`) for BOTH characters and vehicles, not the demo's direct `DtCrowd.RegisterAgent` shortcut. **Character:** F6 "FDP Move Order (char)" issues a `LocomotionChannel` MoveTo action (`ActiveAction=ActionIdMoveTo` + fresh `ActionInstanceId`) — the exact BehaviorTree trigger — so `NavigationIntentBridgeSystem` AUTO-REGISTERS the crowd agent; chain `CrowdAgentUpdateSystem → CrowdMotorIntent → BulletCharacterMotor` walks the mannequin around a wall (headless-proven B20-A1). **Vehicle:** new `VehicleNavigationIntentSystem` (Hrot.Stride.Core, Simulation phase after `NavigationExecutionSystem`) consumes `NavigationIntent` on `VehicleState` entities (the crowd bridge excludes vehicles), calls `INavmeshProvider.PlanPath`, steers via `VehicleWaypointController`, advances corners, sets `NavigationStatus.Arrived`; F7 "FDP Move Order (vehicle)" sets only the intent (no manual PlanPath). Closed-loop headless proof (B20-B2) drives the APC around the wall to the goal; NoPath + no-navmesh guards covered (B20-B3/B4). Prior BATCH-19 (F5) infantry-via-direct-RegisterAgent path retained as a lower-fidelity demo. **NOTE: wired + headless-green but the GPU test of the production front door FAILED — see STR-D21. The F5 (direct-RegisterAgent) character path remains the only GPU-verified nav.** |
| STR-D20 | BATCH-17 | **Latent footgun:** `VehicleKinematicsTkbTranslator` adds a `VehicleState` component to **every** TKB-spawned entity once `VehicleState` is registered (incl. infantry mannequins). This caused the F1 no-animation bug (the vehicle motor zeroed the character's post-collision velocity). Worked around by making `KinematicVehicleMotor` skip Capsule/`CrowdMotorIntent` bodies. Consider making the translator add `VehicleState` only to vehicle-shaped entities. Also: the per-vehicle `[VehicleYaw]` Info-level diagnostic is chatty — downgrade to Debug or remove once vehicle tuning is settled. **BATCH-25 (partial fix, Option b):** the F6 harness now strips `VehicleState` from the infantry entity immediately after spawn + resolution, before issuing the MoveTo order. This unblocks `NavigationIntentBridgeSystem`'s `!HasComponent<VehicleState>` crowd-registration guard. The root cause (translator adding VehicleState to infantry) is NOT fixed in the translator itself — the footgun remains for any code that spawns infantry without the harness workaround. Option (a) — scope translator to `OrientedBox` shapes only — is the correct long-term fix. **RESOLVED (BATCH-26 Option a):** `VehicleKinematicsTkbTranslator` now gates `VehicleState`/`VehicleParams` injection on `ShapeKind == CollisionShapeKind.OrientedBox`. Infantry (Capsule shape) no longer carries `VehicleState` at spawn. F6 harness `VehicleState`-strip workaround is now redundant (translator fix is the authoritative fix). Headless proof: `B26_F6_1_InfantrySpawnedViaTranslator_HasNoVehicleState_AfterFix` and `B26_F6F7_Shape_VehicleHasVehicleState_InfantryDoesNot` both green. | P3 | cleanup | **RESOLVED (BATCH-26)** |
| STR-D21 | BATCH-20 (GPU test) | **Production-front-door navigation (F6/F7) does NOT move the entity on the GPU** (headless tests pass, live fails). **F6 (char):** `bridgeRegisteredAgent=False` — issuing the `LocomotionChannel` MoveTo action (the way `FdpNavigationOrders.IssueMoveTo` does) did NOT cause `NavigationIntentBridgeSystem` to auto-register the crowd agent in editor_stride, and `NavigationStatus` reports `Arrived` immediately while the mannequin sits at spawn (so nothing drives it). Likely: the bridge isn't ticked at the right phase / the channel-action shape the live bridge expects differs from the test's, or `NavigationExecutionSystem` prematurely completes the intent with no agent. **F7 (vehicle):** `VehicleNavigationIntentSystem` plans the path (11 corners) and writes `VehicleState(spd≈2, steer=0.7)`, but the dynamic body never moves (pos frozen, NavStatus=FailedBlocked) → the commanded `VehicleState` isn't reaching/driving the body on this path (note F2/F4 DID move the same body via VehicleState→KinematicVehicleMotor, so compare wiring/phase/ownership; possibly the system's per-tick VehicleState write is overwritten or the motor isn't driving this entity). Investigate after the P5 editor work (deferred at user request 2026-06-05). **FIXED (BATCH-24):** **F6 root cause:** `NavigationIntentBridgeSystem` unconditionally cached `_lastAppliedActionInstanceId[entity]` even when `RegisterAgent` returned `false` (DotRecastDtCrowdProvider deferred-init mode, crowd not yet ready). Fix: `bool cacheActionId` flag; only cache when registration succeeds. Diagnostics added: `[BridgeReg]` NLog + `crowdInit`/`hasCrowdComp` in F6 harness. **F7 root cause:** `VehicleNavigationIntentSystem` ran inside `Kernel.Update()` (Step 4, AFTER the motor at Step 2b), so the motor always read 1-tick-stale `VehicleState` = zero. Fix: explicit `_vehicleNavIntentSystem?.Execute()` at Step 2b BEFORE the motor; idempotent (second kernel call skips replanning). Diagnostics: `[VehicleNav]` + `[VehicleMotor]` NLog + `SimVelocity_2D_spd` in F7 harness. Headless tests added (StrD21NavigationFixTests.cs: 6 bridge-retry + 3 VehicleNav). GPU verification pending. **BATCH-25 additional fixes:** **F6 (STR-D20 footgun):** `VehicleKinematicsTkbTranslator` adds `VehicleState` to infantry — bridge's `!HasComponent<VehicleState>` guard was excluding the soldier from crowd registration. Fixed in F6 harness by stripping `VehicleState` from the infantry entity post-spawn (Option b). Regression test added (BATCH-25-B1/B2). **F7 (FailedBlocked):** GPU diagnostics showed `NavStatus=FailedBlocked` within ~2s of issuing the intent — `NavigationExecutionSystem`'s frustration guard fires while the Bullet body hasn't entered simulation yet (`rb.Simulation == null` → `SetLinearVelocityXZ` no-ops → `SimVelocity=0` → 120-tick frustration limit exceeded). Root cause: `NavigationExecutionSystem` also ran on vehicle entities (it queries `NavigationIntent + NavigationStatus + FrustrationTicks`, which vehicles carry), conflicting with `VehicleNavigationIntentSystem`'s own stuck guard. Fixed by adding `.Without<VehicleState>()` to `NavigationExecutionSystem`'s query — vehicles are now exclusively managed by `VehicleNavigationIntentSystem`. Regression test added (BATCH-25-C1). **Camera upside-down (`CenterOnEntityCommand`):** `RotationFromForward` BATCH-24 shortest-arc replacement had wrong pitch sign in Stride's left-multiplication quaternion convention; fixed by swapping `qYaw*qPitch` → `qPitch*qYaw`. Tests added (BATCH-25-A1/A2 world-up + no-roll assertions). GPU re-verification needed for F6/F7. **BATCH-26 assembled-system proof:** added `B26_F6_1/F6_2/F7_1/F7_2/F6F7_Shape` tests in `AssembledNavIntegrationTests.cs` proving full stack headlessly. Root causes for both F6/F7 confirmed fixed headlessly. GPU re-verification still pending. **BATCH-27 live-vs-test divergence investigation and root causes identified:** (1) F6 live-vs-test divergence: `B26_F6_2` used `TkbCivilianPedestrian` (BrainTier=0, no `BehaviorState`) — `ChannelArbitrationSystem` queries `.With<BehaviorState>()` so it NEVER ran on the test entity → channel always survived → trivially passing. Live uses `InfantrySoldier` (BrainTierBTree, has `BehaviorState.InstanceId=1`). Fix: added `B27_F6_InfantrySoldier_FullBridgePath_BTreeEntity_BridgeEnrolls` test using the live entity type. `IssueMoveTo` stamps `ch.BehaviorInstanceId=1 == InstanceId=1` → arb does NOT clear → bridge registers → `CrowdAgent` added. (2) F7 live-vs-test divergence: `TryResolveNearest` in `FdpMoveOrderVehicle` had no `VehicleState` filter. F6 spawn (-4,2,0) and F7 spawn (-5,3,1.25) are ~1.89 m apart (within 2 m threshold) — if both are active simultaneously, resolver selects the infantry entity, `VehicleNavigationIntentSystem` (`.With<VehicleState>()`) skips it → `plannedCorners=0`. Fix: added `requireVehicleState=true` parameter to `TryResolveNearest`; F7 harness call updated to pass it. Headless proof: `B27_F7_TryResolveNearest_WithVehicleStateFilter_SelectsApcNotInfantry`. Belt-and-suspenders: `NavigationIntentBridgeSystem` now logs `[BridgeReg] WARN` when MoveTo is processed but VehicleState is present (confirms BATCH-26 translator fix is in effect on GPU). GPU re-verification pending. | P2 | post-P5 | **RESOLVED — GPU-VERIFIED (user-confirmed 2026-06-05):** F6 (char) and F7 (vehicle) BOTH move via the production `NavigationIntent` interface. Decisive BATCH-27 root causes: `ChannelArbitrationSystem` cleared the MoveTo order on `BehaviorState` entities (fixed by stamping `ch.BehaviorInstanceId == BehaviorState.InstanceId` in `IssueMoveTo`) and `TryResolveNearest` selected the nearby infantry instead of the APC (fixed with a `requireVehicleState` filter) — on top of B24/B25/B26 (bridge retry-on-failed-registration, VehicleNav-before-motor ordering, STR-D20 infantry VehicleState strip, NavigationExecutionSystem `.Without<VehicleState>()`). |
| STR-D16 | STR-P5-T1 | Stride 4.2.1.2487 has **no immediate-mode debug-shape API** (only `DebugTextSystem`, the compositor `DebugRenderer` render-feature, and `GeometricPrimitive`). So `DebugPrimitiveRenderer3D`'s two-pass resolution/swizzle is done + tested, but the actual GPU **draw** is deferred behind `IDebugDrawSink3D` (only a logging sink exists). Build a concrete sink — a compositor `DebugRenderer` render-stage or dynamic-mesh emitter — at GPU bring-up so gizmos visibly render (incl. P6 remote `ConsumerBuffer` overlays). | P2 | P5/P6 GPU bring-up | **RESOLVED (BATCH-21):** `PooledEntityDebugDrawSink3D` (pooled Stride entities + emissive materials) implemented in `Hrot.Stride.Core`, wired into `StrideHrotGame.BootEditorSubsystem` via new `debugDrawSink` param to `EditorStrideSubsystem.Initialize`. D8 "Draw Test Gizmo" emits R/G/B axis triad, red+white spheres, cyan/magenta/yellow line segments. GPU verification pending user pressing D8. |

Legend:
- **P1 = Critical** — never enters the tracker; always becomes Corrective Task 0 in the next phase.
- **P2 = Should fix** — tracked here, assigned a target phase/task.
- **P3 = Nice to have** — tracked here, best-effort.
- **Status:** OPEN / RESOLVED (do not delete resolved rows).
- **Source / Target** reference a task id (e.g. `STR-P1-T5`) or phase.
