<!--STATUS
state: LIVE
updated: 2026-08-21
current-answer: this folder holds design-INTENT documents copied from .dev/ so all design lives under docs/.
-->
# `docs/designs/` — **design intent migrated from `.dev/`**

⭐ These are the **design documents** copied out of the `.dev/<programme>/` working folders, so that all
design intent lives under `docs/` *(user, `2026-08-21`: "copy the designs from there to suitable place in docs")*.

⭐⭐ **Why they matter** *(user, `2026-08-21`)*: *"the .dev contains implemented intents, but they are
still intents telling often more than the code."* ⇒ each of these describes the **WHY and the meant-to-be**
behind a feature that was built — often more than the code itself carries.

| ⭐ | |
|---|---|
| **What is here** | 93 `*-DESIGN.md` / `*_Detailed_Design.md` docs, grouped by their originating `.dev` programme |
| **What is NOT** | ⛔ the `.dev` ephemera — batches, reports, instructions, reviews, design-talk transcripts — those stay in `.dev/` as the working-history archive |
| ⚠ **Currency** | these describe **implemented** intent. Before treating one as current, **check for a newer `docs/blueprints` (or other `docs/`) design that supersedes it** — the newer one wins |
| ⚠ **Already-in-docs** | 12 `.dev` designs already had a copy under `docs/blueprints/` *(possibly revised there)* and were **NOT** re-copied — e.g. `Blackboard_Authoring_Detailed_Design.md`, `BLUEPRINT-SCENARIO-DESIGN.md`, the `Blueprint_Subsystem_*_Detailed_Design.md` set |
| ⭐ **Originals** | the `.dev/<programme>/` copies remain in place, in their programme context |

⭐ **Lazy upgrade:** these are copied verbatim. When a session next works a feature whose intent lives
here, bring that doc under the `STATUS` / UML convention *(and add a `superseded-by` pointer if a `docs/`
design has moved on)* — ⛔ not in a sweep.
