# RESUME — the interaction-design thread (2026-08-10)

> 🔴 **Read this first if context was compacted mid-session.** It is the smallest complete state of the
> interaction-design work. Branch: `claude/ux-session-resume-i2le7f`. Everything below is **committed**.

## 1. What this session did

Designed **21 of 37** issues (**UXI-35 and UXI-37 designed** 2026-08-14). Three were consolidated into one API + one acceptance catalogue; the rest
have their own feature docs. **67 rulings · 50 corrections**, all contiguous and linked below.

🔒 **NO OPEN DECISIONS.** Rulings **47-67** closed every question this thread had, and added four
cross-cutting architecture rules that constrain everything built later:

| | |
|---|---|
| 🔒 **[Ruling 53](#) — confirmation is resolved at the ORIGIN node** | the infrastructure is **headless by default**; MCP/script/replay dispatch through a context with no confirmation host, and **receivers never ask and never block**. What crosses the wire is pre-authorized |
| 🔒 **[Ruling 55](#) — the KNOWING node decides what to ask** | the origin is **dumb**: it pre-flights over `ICommandGateway`, gets a neutral `ConfirmationSpec?` back, and **renders it verbatim**. `null` ⇒ no dialog. ⚠ **[Ruling 54](#) is superseded** — it made the origin smart |
| 🔒 **[Ruling 56](#) — and that node is CGF** | **one** pre-flight endpoint, not "whoever executes". `ICommandGateway` **already targets only CGF**; the whole intent stack is `CgfLogicPack`; delete is CGF-served too. ⇒ `IIntentAspectProvider` registers on **CGF only** |
| 🔒 **[Ruling 57](#) — it travels as a bus event + translator** | DDS → ingress xlat → **bus** → dispatcher **on the tick thread** → bus → egress xlat → DDS, correlated by `RequestId`. **Proven twice already.** 🔴 Providers read **live ECS**, so a DDS-callback resolution is the forbidden async-ECS access. ⭐ The **Editor drops the outer hops** and runs the identical handler. ⚠ **Progress must travel the same way — it was not designed to** ([Correction 44](UX_Tasks_Detail.md#corrections)) |

⚠ **These three are not UI details.** They govern every request this programme emits, and they were
reached by the user correcting a design that worked in-process and would have failed in a cluster
([Corrections 41-44](UX_Tasks_Detail.md#corrections)).

| Doc | Holds |
|---|---|
| 📐 **[UX_Interaction_API.md](UX_Interaction_API.md)** | ⭐ **the contract** — types, arbitration order, threading, ECB, progress |
| ✅ **[UX_Interaction_UseCases.md](UX_Interaction_UseCases.md)** | **57 cases**, 46 headless / 11 integration / **0 visual-only** + coverage check |
| [UX_Issues.md](UX_Issues.md) | the register — **37 issues** (**UXI-35, UXI-36, UXI-37** filed 2026-08-14) |
| ✅ **[Architect_Question_28_Map_Layers.md](Architect_Question_28_Map_Layers.md)** | **UXI-28 decisions — ALL SETTLED** 2026-08-12 |
| ✅ **[UX_Feature_Map_Layers.md](UX_Feature_Map_Layers.md)** | **UXI-28 design** — 17 cases. 🔴 **Gated on a Windows check**: the layer panel round-trip is complete in source but reported non-working |
| ✅ **[UX_Feature_Selection.md](UX_Feature_Selection.md)** | **UXI-11 design** — 22 cases. One store, `ISelectionState` becomes a view; CGF gets the full chain |
| ✅ **[UX_Feature_Map_Parity.md](UX_Feature_Map_Parity.md)** | **UXI-23 design** — 12 cases. One `MapInteractionPack`; IG's forked `switch` retires. ✅ **§5 closed by [ruling 37](#)** — the 9 orphan ids moved to UXI-32 |
| ✅ **[UX_Feature_Authority_Aware_Writes.md](UX_Feature_Authority_Aware_Writes.md)** | **UXI-29 design** — **26 cases**. Gizmos publish intent; complementary authority gates route local-vs-remote. 🔴 **Blocked on UXI-30** |
| ✅ **[UX_Feature_Multi_Select.md](UX_Feature_Multi_Select.md)** | **UXI-24 design** — 29 cases. Multi-select is **built on the inspector list and nowhere on the map**; **4 dead seams** (16-19). ✅ **§2 ruled**: incompatible items are **absent**, not greyed |
| ✅ **[UX_Feature_Entity_Commanding.md](UX_Feature_Entity_Commanding.md)** | **UXI-32 design** — **37 cases**. ⭐ **`MoveHere`/`Engage`/`Stop` already map onto registered behaviors** (`MoveToLocation`/`FireAtTarget`/`Idle`) — no placeholders needed for the three that are actually on screen. One action id + JSON arg; menu from TKB with `BehaviorCatalog` fallback |
| ✅ **[Architect_Question_29_Entity_Commanding.md](Architect_Question_29_Entity_Commanding.md)** | **UXI-32 — FULLY ANSWERED, rulings 38-46, no open decisions.** Right-click = **immediate tactical intent**; **one action id + JSON arg**; command set from **TKB**; formations = order the commander. ⭐ **Seam-law 20-24** — chain, panel, choke point, the wire's JSON field, the TKB provider stack and the reload contract all already exist |
| ✅ **[UX_Feature_Modal_Surfaces.md](UX_Feature_Modal_Surfaces.md)** | **UXI-16 + UXI-27 design** — 37 cases. ⭐ **`StatusBarManager` is already a delegate registry with 6 registrations**, so progress is one section; 🔴 **modals have no host at all** — 28 `BeginPopupModal` sites across 19 files (**seam-law 25**). One `ModalManager`, two consumers. 🔒 **§2.0: the confirmation gate is at the ORIGIN node** — headless/MCP by default, receivers never ask ([ruling 53](#)). **§2.0b: the KNOWING node computes the confirmation and the origin renders it blind** via an `ICommandGateway` pre-flight ([ruling 55](#)). **§2.0c: that node is CGF** — one endpoint, verified four ways ([ruling 56](#)) |
| [UX_Seam_Inventory.md](UX_Seam_Inventory.md) | prior-art table + `scripts/seam_inventory.py`, `scripts/type_index.py` |
| [UX_Tasks_Detail.md](UX_Tasks_Detail.md#corrections) | **42 corrections** — read before trusting any claim |

**Designed:** UXI-01..**11**, **16**, **23**, **24**, **27**, **28**, **29**, **32** (+ **19**, verified and absorbed into 10). **Refuted:** UXI-26. **Split out:** UXI-25 (ExCon ORBAT),
UXI-27 (progress surface), **UXI-31** (IG's test-hook-only waypoint fan-out).

⚠ **The table above lists only *this session's* docs.** The designs for **UXI-01..08** (dead UI, half-built
decisions, action vocabulary, cross-surface actions, menu-follows-focus, perspective restore, tool model,
layout defaults) are linked from their rows in **[UX_Issues.md](UX_Issues.md)** — that register, not this
file, is the complete index. Every one of the 15 is reachable from there.

## 2. 🔒 Every user ruling, in force

| # | Ruling |
|--:|---|
| 1 | **Tools ≠ actions.** *"An action is what activates a tool."* Two vocabularies, one relationship |
| 2 | **`gizmo ≠ tool`** — stateless gizmos (health bars) are not tools; many per entity |
| 3 | **Modal** = ≤1 active per subsystem; **modeless** = permanent until off, several coexist |
| 4 | **Re-activating a modal tool does NOT cancel it** — toggle only via `ToggleOnReactivate` |
| 5 | Cancellation is **focus-driven**. `CancelsModalTool` on the **action** (not the tool); `false` = *transparent* (AutoCAD) |
| 6 | **No suspend flag can exist** — suspension needs a resume point only the handler knows ⇒ scoped (`PushModal` + dispose) |
| 7 | **Toolbar presence is opt-in** (`ShowOnToolbar`) |
| 8 | **A suspended tool decides its own drawing** — controller never suppresses; tool reads `Activity.State` |
| 9 | **Modal tool stack** required — interrupt and return. It existed once (`MapCanvas.PushTool/PopTool`) and was deleted |
| 10 | **Tool state per subsystem** (B1); an unfocused subsystem's tool stays armed but consumes no input |
| 11 | **`EditorTool`**: no tool classes remain; the enum is a fossil. Tool ids are their own vocabulary |
| 12 | **Escape centralised for modal tools**, gizmo-local cleanup stays |
| 13 | **Long-running cancellables MUST be visible**: `Exclusive` → **modal dialog + progress bar**; else → **status-bar progress + cancel icon** |
| 14 | **Progress API is part of the design**; the widget is its own issue (UXI-27) |
| 15 | 🔒 **Single-threaded async** — handlers stay on the tick thread and yield; **per-action ECB**, real API, no wrapper |
| 16 | **ExCon is DDS-only, no ECS** — reuses the ORBAT UI with its own data model. `int EntityId` is correct, not a defect |
| 17 | **No assumptions.** Read `README.md` + `docs/HROT-PROGRAMMERS-GUIDE.md` before claiming an engine defect |
| 18 | **Layout = one directory.** `fdp_windows.json` lives **next to `imgui.ini`** in *both* places (user `%LocalAppData%\HROT\` and the shipped default). Reset = a directory copy |
| 67 | 🔒 **Asset roots come from a CONFIG FILE, shared by CGF and the Editor, with the repo-source walk-up as fallback** (user, 2026-08-14): *"we need a **config file provided asset path** for the CGF as well as the Editor (**same shared code**), with **fallback to the repo source** as of now."* ⇒ [UXI-37 §5c](UX_Feature_Cgf_Brain_Diagnostics.md). 🔴 **The problem is real and already bites the Editor**: `EditorSubsystem.ResolveAiBehaviorsDir` (`:693-708`) walks up from CWD **and** BaseDirectory for `Hrot.AI.Behaviors.csproj` and points asset write roots at the **source** tree (`:710-721`); on a deployed node blueprints fall back to the output dir and 🔴 **BTree/HSM roots become `null`** ⇒ **a shared defect, not a CGF cost.** 🔴 **And there are already TWO competing path authorities**: `AssetRoots` documents itself as *"Single authority for the two root families"* and resolves from `AppContext.BaseDirectory` (**30 call sites / 12 production files**), while `EditorSubsystem`'s private walk-up **bypasses it** ⇒ **seam law — a declared single authority contradicted next door**; a config file added as a *third* mechanism would make it worse. 🔒 **So put config INTO `AssetRoots` and delete the walk-up**, resolution order **config → source walk-up → BaseDirectory**. ⭐ **The config loader already exists — do not invent one**: `ClusterConfiguration.LoadFrom(filePath)` (`Hrot.Orchestrator/ClusterConfiguration.cs:50-77`) is a JSON file loader that returns `Default` when absent and **throws** when present-but-unreadable, and already carries `NasBasePath` (`:29`) which `EditorBootstrap.ScenariosRoot` reads. ⚠ **Two implementation calls left**: `ClusterConfiguration` lives in `Hrot.Orchestrator` (the Editor references it; CGF must, or the type moves), and `AssetRoots` is a **`static class`** ⇒ **lean: an explicit `Configure(...)` at composition** keeps all 30 call sites compiling; a provider is cleaner but ripples. 🔒 **A configured-but-missing root throws at startup** — silently falling through to the walk-up would reintroduce *"it worked on the dev box"*. ✅✅ **BUILT `2026-08-25`** *(`CE-032`/`CE-033`)*: `AssetRoots.Configure(root)` + `ResolveBase`/`DescribeBase`, wired once in `Program.cs` for EVERY host from a new `--asset-root` option; order **config → source walk-up → output dir**; unset config is byte-identical to before. ⚠ **Two deviations from this ruling's own plan, both measured:** ① the walk-up is **NOT deleted** — it is the middle arm the ruling itself asks for *("fallback to the repo source as of now")*, and `EditorSubsystem`'s two inline copies remain `CE-018`, another lane's file; ② **`ClusterConfiguration` was NOT used and CGF gained no `Hrot.Orchestrator` reference** — `AssetRoots.Configure` takes a plain path, so the runner's own config object supplies it and the packaging question this ruling flagged **does not arise at all**. 🔴 **And the half the ruling did not name:** `AssetsFor`/`RecipesFor`/`AssetsRoot`/`ScenariosRecipesRoot` are what the contributors and `*NewAssetService` resolve from, so they had to honour the config too — otherwise a configured node lists from one tree and creates in another, which is this ruling's own two-authority split reintroduced by its fix. 📄 [as-built](../PROGRAMME_Cgf_Equals_Editor_Gap_Map.md) |
| 66 | 🔒 **Scenario data ≠ immediate simulation state; scenario loading is fully possible from CGF alone** (user, 2026-08-14): *"the components that are missing on the CGF node and present on SimHost are usually **not those carrying the scenario information**. At runtime these are filled from commands coming from CGF. They are important only for taking and restoring a snapshot of current immediate simulation state (**checkpoint**), which is a **different category** from scenario authoring. The system supports also **sending initial state of mandatory ECS components on entity creation** to SimHost, so the scenario loading process is fully covered and possible from the CGF node only."* ⇒ 🔴 **[Correction 47](UX_Tasks_Detail.md#corrections) — my save-time completeness check and SimHost-vs-CGF serializer diff are WITHDRAWN.** Wrong twice: **(1)** `ScenarioSerializer.SerializeEntity(…, **BitMask512 componentMask**)` is **mask-driven** and `AutoSerializer.TryExtract` is per-entity-per-bit, so world-level `RegisterComponent<T>()` implies neither entity presence nor serializer scope; **(2)** the deeper error — runtime kinematic state is a **checkpoint** concern, not a scenario one. ✅ **Verified**: `CreateEntityRequestSystem` compiles `InitialAttributesJson` into components, `SimTransform`/`SimVelocity` as typed spawn fields (`:225-245`) ⇒ **initial ECS state travels with entity creation.** ⇒ **scenario editing joins the authoring tier; the gate collapses to "same component mask on both nodes".** ⚠ **Preview is the one open item** — and ⭐ **it is already a cluster operation**: `EditorPreviewController` (`EditorSubsystem.cs:439-467`) drives **`PreviewClusterOpHandler`** and brackets it with `SwitchToContinuous()` / **`SwitchToDeterministic(new HashSet<int>())`**. ⭐⭐ **The regularity worth keeping: THE EDITOR IS A ONE-NODE CLUSTER** — that empty slave roster is the same trick `MasterSyncTimeControllerAdapter` uses for the debug pause, so **two independent features already run the cluster protocol with a roster of zero** ⇒ distributing either is **supplying the real roster**, not building a distributed version. ⚠ Open for preview: what `PreviewClusterOpHandler` does to the world on enter/exit, and whether exit **restores** or merely unloads |
| 65 | 🔒 **Editing machinery on CGF is welcome; hot reload + save optional; scenario save gated on a component check** (user, 2026-08-14): *"bringing editing machinery onto a runtime node is perfectly OK. It would not hurt at all if also the **behavior hot reload and save** features could be optionally enabled as well. **The behaviors are CGF only**, so all that happens on a single node, similarly to the editor… maybe it could include also **scenario editing** — this would need checking how scenario saving would work for the **CGF-unowned components** (owned by SimHost). Maybe the only SimHost-owned one needed is the **sim transform**, which is replicated to CGF."* ⇒ ✅ **closes [UXI-37 §5 Q1](UX_Feature_Cgf_Brain_Diagnostics.md)** — take `AiDocumentManager` as built, no read-only viewer, no parallel implementation. ⭐ **Hot reload is cheap**: `QuickReloadService`, `HotReloadLogWindow`/`Model` and `BlueprintDebugSession.OnHotReloadBegin/Completed` all live in **`Hrot.Blueprints.Editor`, which CGF ALREADY REFERENCES** (`CgfSubsystem.cs:54`) ⇒ registration, not a distributed problem. ⚠ **One interaction**: a reload **while frozen** must reload and **stay paused** — route it through the halt toggle or it silently resumes the exercise. ✅ **The transform hypothesis checks out** — CGF holds position (`CgfSubsystem.cs:600` tests `HasComponent<SimTransform>`; `CgfEntityPresentationGizmo` projects `SimTransform`/`NetworkTransform`) ⚠ **but the replicated component is `NetworkTransform` (ordinal 10)** while the authoritative Muscle block is **`SimTransform`+`SimVelocity`+`VehicleState`+`VehicleParams`+`NavState`** (`NedReplicationModule.cs:381-392`) — *"the transform"* is two different components depending on the side. ✅ **The save machinery is already on CGF** (`HrotScenarioSerializerFactory.Build` at `:432`, wired into the inspector at `:460`). 🔴 **But the real risk is NOT a missing component — it is REGISTERED-BUT-UNPOPULATED**: `CgfComponentRegistry` calls `KinematicComponentRegistry.RegisterAll`, which registers **`VehicleState`, `VehicleParams`, `NavState`** on CGF's world even though CGF never authors them ⇒ a naive save emits **defaults or stale values instead of visibly omitting them**. 🔒 **Silent wrong data beats no data only in the wrong direction** — a CGF-authored scenario could look complete and quietly reset vehicle parameters on load. ⇒ 🔒 **Required: a save-time completeness check** (CGF must be owner **or** live replication target per emitted component; else refuse and say which), and 🔒 **the settling test is a DIFF of the serializer's emitted component set on SimHost vs CGF for the same scenario — written BEFORE scenario save is enabled.** ⇒ scenario editing is a **third tier**, gated on that diff; diagnostics and behavior authoring/reload are unblocked |
| 64 | 🔒 **Unanswered freeze request ⇒ halt CGF locally and say the cluster is still running; k is expected small** (user, 2026-08-14): *"yes to E, halt locally and say cluster still running. **k is expected small.**"* ⇒ **[DQ30](Design_Question_30_Debug_Pause_Resume.md) is fully closed — A-E all decided.** 🔒 **A debugger must never silently decline to stop** — a partial freeze that says so beats a breakpoint that does not fire. ⭐ **The offline case is NOT a degraded one**: `CgfApplication.cs:107` documents a **no-DDS mode** (*"the node operates without DDS — offline / pure-domain test path"*) where `SlaveSyncController` is installed but the time-mode/lockstep translators are not ⇒ **no cluster exists to freeze, so the local halt IS complete and correct** ⚠ and **no warning is shown** there — a permanent warning in a supported mode is [ruling 49](#)'s dead affordance in another costume. ⚠ **"k is expected small" is an expectation, not a measurement** — [rulings 62-63](#)'s acceptance of the zero-dt timer discontinuity and the stale-remote-view window both rest on it ⇒ 🔒 **measure k once during implementation**, and revisit only if it is large |
| 63 | 🔒 **Freeze and step the WHOLE cluster in deterministic mode; no world-state ingress while frozen** (user, 2026-08-14): *"freeze (sim time) and step the whole cluster in deterministic mode. **No replication and simulation-state-changing ingress while frozen (this might need categorizing the ingress translators).**"* ⇒ closes [DQ30 C and D](Design_Question_30_Debug_Pause_Resume.md). ⭐ **The step protocol already exists** — `MasterSyncController.Step(dt)` + `SlaveMode.Stepping` + `SteppedSlaveController`; ⚠ **within-tick stepping needs no cluster at all** (it walks the node-granular recording, no re-execution), so only tick crossings pay a round trip. 🔴 **The translator categorization is REQUIRED, not a nicety, and [DQ30 A](Design_Question_30_Debug_Pause_Resume.md) proves why**: the **resume arrives through ingress** (`SwitchTimeModeEvent` via the time-mode/lockstep translators), so *"stop ingress"* cannot mean all ingress or CGF can never be resumed. ⇒ **two classes — control plane keeps polling, world state stops with the sim.** ⭐ **Prior art, but NOT the same boundary**: `CgfApplication.cs:113-114` already builds two buses (`_eventBus` + `_orchestrationBus`, *"Control Plane bus"*) — ⚠ **the time events ride `_eventBus`, so the existing split does not separate "keeps running" from "stops"**. ⇒ 🔒 **add a category to `IDescriptorTranslator`** (it carries `TopicName`/`DescriptorOrdinal`/`Direction` today) and gate the freeze on it. 🔒 **Fail-safe default = WORLD STATE** (stops): a miscategorised control-plane translator then fails **loudly** as *"resume does not work"*, whereas the opposite default **silently** leaks live data into a frozen snapshot. 🔴 **And the toggle is the step actuator, not just the halt** — a granted step must re-enable CGF's sim group for exactly one tick |
| 62 | 🔒 **A breakpoint hit on CGF freezes the WHOLE cluster; the resume semantics are ours to decide** (user, 2026-08-14): *"breakpoint hit on CGF should freeze whole cluster. What happens on CGF on resume or on step after the freeze is a question we should analyse ourselves, **the architect is not available now**. My lean is that CGF fast-forwards the gap somehow, as the world cannot jump back in time — maybe some degraded fast forward, just simtime jump to match the cluster with **zero dt** applied to sim tick of CGF, or true sim dt, not sure which is better. Maybe the ingress needs to work also in pause to keep replicating changes made during the pause."* ⇒ [Design Question 30](Design_Question_30_Debug_Pause_Resume.md) — **no architect round; resolved in-house.** ⭐ **The user's lean is ALREADY THE ENGINE'S BEHAVIOUR**: `SlaveSyncController.ApplyResume` → **`ApplyTimeSnap`** sets `_baselineSimTime = evt.SimTimeSnapshot` — a **zero-dt snap to the master's authoritative snapshot**, which every slave already performs after every pause. ⇒ **no new gap-closing scheme is needed**; true-dt fast-forward is **rejected** (it re-executes brain logic so the breakpoint can immediately **re-fire**, and it would run against T+k data, so it is not a faithful replay either). 🔴 **The load-bearing finding is elsewhere — question A**: the barrier resolves inside `SlaveSyncController.Update()` → `DrainModeSwitchEvents()`, which runs **as part of the tick**, and `SyncedWallTicks` is a **wall-clock** anchor ⇒ **if the debug halt stops the whole kernel tick, CGF can never see the master's pause or resume event and DEADLOCKS.** 🔒 **So the halt must stop the SIMULATION systems only** — and ⭐ **the shape already exists**: CGF composes its brain into `TogglableInputGroup`/`TogglableSimulationGroup` (`CgfSubsystem.cs:330-334`). ⚠ **Verify the toggles do not also gate time-sync/ingress-poll, or the deadlock returns.** ⚠ **Three leans still need a nod**: ingress during pause (**lean: no** — a coherent snapshot beats freshness, and the window is bounded by the freeze-convergence latency k, not by how long the operator looks) · step granularity (**lean: step the whole cluster** — stepping CGF alone makes the brain tick against a frozen world, which misrepresents what is being debugged) · master unreachable (**lean: halt locally and say so**). ⚠ **k is unmeasured** — every conclusion assumes tens of ms |
| 61 | 🔒 **Debug from Editor and CGF only; CGF is exact, other nodes are latency-tolerant** (user, 2026-08-14): *"no debugging from ExCon, only from Editor and from CGF. Most stuff to be debugged lives on CGF so it should be possible once we solve the pause issue. **Breakpoint-driven pause and stepping needs to be exact and correct on the CGF node**, while on other nodes contributing to the simulated world (SimHost) **we accept that the pause command comes with a latency** so their world advances a bit and thus is not in perfect sync with CGF. We accept also that **data breakpoints on CGF will work only for ECS components owned by CGF or replicated there**."* ⇒ 🔒 **[UXI-37](UX_Issues.md#uxi-37) needs NO session-protocol redesign** — with no remote operator, `IAiDebugSession`'s single-process contract ([ruling 60](#)) is **honest**, because the debugger always sits on the node that owns the world. ⭐ **The distributed protocol is ALREADY BUILT ON BOTH SIDES**: `MasterSyncController.SwitchToDeterministic(slaveRoster)` + `Step(dt)` (the Editor uses it with an **empty** roster — `MasterSyncTimeControllerAdapter`, *"pauses the local clock without waiting for network ACKs"*), and `SlaveSyncController` has a **`SlaveMode.Stepping`** entered on `SwitchTimeModeEvent(Deterministic)` with a **barrier**, plus `SteppedSlaveController` / `SlaveTimeModeListener` and `TimeNetworkModule.CreateSlaveLockstepTranslator`. ⇒ **the CGF adapter replaces a no-op with two calls, not a mechanism.** ⭐ **And the accepted imperfection is smaller than assumed**: *"Every Deterministic event is an **absolute timeline anchor** from the Master"* (`SlaveSyncController.cs:189`) ⇒ nodes reach the barrier at different **wall-clock** moments but stop at the **same simulation tick**, so the frozen world is self-consistent — just slightly **past** the breakpoint instant. 🔴 **The consequence to design for**: CGF halts at the breakpoint tick **T** while the cluster halts at barrier **T+k**, so CGF is **k ticks behind** a frozen cluster and has k ticks of queued ingress to drain on resume. ⚠ **Stepping mostly needs no cluster at all** — within a tick, `Step*` walks the **node-granular recording** with the clock paused and **no re-execution**; only crossing a tick boundary requests a real tick (`BlueprintDebugSession.Pause` doc, NGS-2.1/2.3) ⇒ **the snapshot/restore problem the user feared is avoided for within-tick stepping** |
| 60 | 🔒 **The real risk in the AI-debug stack: editor-bound assemblies AND an all-in-one-process assumption** (user, 2026-08-14): *"the big issue is the fact that most stuff is editor assembly bound and that it might rely on all-in-one-process simplicity, not taking into account the network distributed nature."* ⭐ **Both halves verified — and the second is VISIBLE AS A NO-OP CLASS.** 🔴 **`CgfSubsystem.cs:825-834` — `CgfNoOpTimeController : IEngineDebugTimeController` with `RequestPause`/`RequestResume`/`RequestStepOneTick` ALL EMPTY**, commented *"(slave node; pause/step are not applicable)"*, while `IsPausedByDebugger` returns real state ⇒ **the read half is live, the request half is dead** — the *"interface called every frame with a dead parameter"* variant of the seam law. 🔴 **And the assumption is in the CONTRACT**: `IAiDebugSession` exposes a raw `Entity? PausedOnEntity` and **synchronous** `Pause()/Continue()/Step*()` with **no node identity, no async, no correlation id** — single-process by construction; making it remote is [ruling 57](#) again. ⭐ **But the seam and the precedent both already exist**: `IEngineDebugTimeController` is in **`Hrot.Blueprints.Core`** (neutral) and the Editor drives it through `MasterSyncTimeControllerAdapter` → the cluster `MasterSyncController` ⇒ 🔒 **one class fixes the CGF half — an adapter that requests a cluster-wide freeze through the master** (`ReplayMasterModule.FreezeTime()` already saves/restores the time scale). ⚠ **The assembly half is NARROWER than I stated** ([Correction 46](UX_Tasks_Detail.md#corrections)): `Hrot.Diagnostics.Breakpoints` + `Hrot.Blueprints.Core` are neutral and **CGF already references `Hrot.Blueprints.Editor`**; only `Hrot.Editor.AiShared` is unreferenced. 🔒 **The fork to settle**: debugging **at** CGF keeps the in-process contract honest (the world is local); debugging CGF **from** ExCon does not — decide which before designing |
| 59 | 🔒 **The three shell-parity questions, answered** (user, 2026-08-14) ⇒ [UXI-35 §6](UX_Feature_Shell_Parity.md#6--questions--all-answered-ruling-59-2026-08-14). **① Orchestrator**: *"mostly invisible part of the infrastructure. No special requirements for its menus nor toolbar — it probably does not need either, or just a minimalistic one as a result of using the same infrastructure shared by all subsystems."* ⇒ **nothing is designed for it**; ⭐ it becomes the **falsifiability test** for the derived-menu principle — if it needs bespoke menu code, the derivation is wrong. **② Open = cluster-wide**: *"yes that is the intent, to make the simulation startable and basically controllable (sim time, replay) from any node for convenience. **The only single-node work is the editor.**"* ⇒ every non-Editor *Open scenario* is a **request to the master**, never a local load ⚠ and it must be worded as what it is (a cluster-wide exercise restart), confirmed at the origin per [rulings 53/55/56](#). **③ CGF splits in two**: asset **authoring** is *"a nice bonus of reusing shared infrastructure, not strictly required"*, but 🔴 **full runtime diagnostics of the brain stack IS required** — *"loading and showing the asset graphs, monitoring their runtime activity with runtime inspectors, watch windows and similar, allowing for debugging at exercise runtime if wrong functioning is suspected"* — plus a bonus of breakpoints (*"could be destructive if needed, i.e. breaking down/stopping the running simulation"*) and maybe stepping (*"might require the snapshot/restore ability which is difficult to get right in distributed environment"*). ⇒ **filed as [UXI-37](UX_Issues.md#uxi-37)**, not shell parity. ⭐ **Far more exists than the ask implies**: CGF **already runs** `DataBreakpointManager` + `DataBreakpointSystem` + a **`DebugSnapshotProvider` with pre-tick snapshots** (`CgfSubsystem.cs:555-568`), `Hrot.Diagnostics.Breakpoints` is a **neutral** assembly carrying `WatchPersistence`, **every window asked for exists** (`AiGraphCanvasWindow`, `AiWatchWindow`, `AiBreakpointsWindow`, `InspectorWindow`, `DiagnosticsWindow` in `Hrot.Editor.AiShared`), and the cluster **can already hard-freeze** (`ReplayMasterModule.FreezeTime()`). 🔴 **Three narrow structural gaps**: the windows sit in an **editor** assembly · the **stepping/session** layer (`IDebugSessionRegistry`, `AiDebugCommands`) is editor-bound · a breakpoint hit must **freeze the cluster through the master**, never stall a slave locally. ⚠ **DATA breakpoints ≠ NODE breakpoints** — do not conflate. 🔴 **BLOCKED on three ACTIVE programmes** (`.dev/blueprint-dbg-1` — breakpoints do **not** pause today · `.dev/blueprint-dbg-2` — sub-tick recorder · `.dev/ai-hsm-btree-vis-edit-2`) |
| 58 | 🔒 **Every subsystem gets a menu and a toolbar — not the same one** (user, 2026-08-14): *"all subsystems need the menu and toolbar, although not the same. **Editor is the richest** with everything. **CGF is almost like the editor, just it works in network distributed mode.** All should allow **opening existing scenarios** and **interactive exercise runtime changes (limited by their ECS component ownership)**. Each should support the **sim time control** and whatever is possible in network way via the **cluster master/slave** control mechanisms. **ReplayBrowser is a completely separate independent beast**, having its own specific stuff."* ⇒ [UXI-35 design](UX_Feature_Shell_Parity.md). ⭐ **The mechanism is already universal — only the registration is missing**: **every** host constructs a `ClusterSlave` (IG `IgNodeBootstrapper.cs:205` · Orchestrator `:115` · CGF `CgfSubsystem.cs:409` · SimHost `NodeBootstrapper.cs:194` · Editor `:787` · ExCon `:184` · generic `HrotNodeBuilder.cs:174`), `TimeControllerFactory` already makes master/slave controllers, and scenario load already fans out from `ClusterMaster` into per-host handlers (`CgfScenarioLoadHandler`, `HrotScenarioLoadHandler`, `ReferenceLiveLoadHandler`). 🔴 **But four shared registries have ONE writer each — the Editor** (`GlobalMenuRegistry`, `MainToolbarManager`, `MenuCommandAdapter`, `ToolbarCommandAdapter`) ⇒ **seam-law instance 30**. 🔴 **And the transport control is copy-pasted twice** (`ClusterTimeControlStatusBarSection` in CGF `:737` + SimHost `:265`) while the Editor puts the same function on the **toolbar** — ⇒ 🔒 **one surface: the toolbar**, since [ruling 13](#) reserves the status bar for activity progress. ⭐ **The design's key move**: the per-host item set is **derived, not authored** — [UXI-29](UX_Feature_Authority_Aware_Writes.md)'s `HasAuthority<T>` says what a host may write, [ruling 49](#) says an item it can never service is **absent** ⇒ **one registration list for the whole product, no host-specific menu code.** ⚠ **Costs one new field**: an action must declare the components it writes. ⚠ **3 open questions** ([§6](UX_Feature_Shell_Parity.md#6-open-questions)): Orchestrator's tier · what *"open a scenario"* means on a slave node · whether CGF includes asset authoring |
| 57 | 🔒 **Pre-flight travels as a bus event + translator, never as a network call in the handler** (user, 2026-08-13): *"on CGF the pre-flight network request should be translated to a local FDP bus event, this one handled by the dispatcher, response sent back as a local FDP bus event, this translated to a network response and sent to the origin. Pre-flight query sent on a networkless Editor goes via FDP bus event only. On origin nodes, where the FDP event bus and translator infrastructure exist, this is similar… **this is a proven concept widely used.**"* ⇒ ⚠ **[§2.0b](UX_Feature_Modal_Surfaces.md) showed `PreflightAsync` on `ICommandGateway` as if that were the transport** ([Correction 43](UX_Tasks_Detail.md#corrections)) — it is a **caller-facing façade**. ⭐ **The template is verified, not proposed**: DDS → `MissionControlIngressTranslator.cs:77-83` `Bus.PublishManaged(MissionControlIntent)` → `MissionControlExecutionSystem` on the tick thread → `MissionControlAckEvent` → `MissionControlAckEgressTranslator.cs:53` → DDS, correlated by `RequestId`. ⭐ **Second instance, already two-phase**: `DeleteEntityRequestSystem.cs:70-74` (neutral `IEntityDeletionRequestSource`/`IEntityAckSink`, `InProgress` ack then final via `EntityRequestFinalizationSystem`). 🔴 **The hard reason it MUST be the bus**: providers read **live ECS state** and only the tick thread may ⇒ resolving pre-flight in a DDS callback is the **async-handler-touching-ECS** violation the engine rules forbid. ⭐ **The Editor drops the two outer hops** — no translator registered, events never leave the bus, dispatcher **byte-identical** ⇒ [ruling 56](#)'s *"one code path"* is literal. ✅ **ExCon qualifies as a bus origin** — `ExConSubsystem.cs:182,186` constructs an `FdpEventBus`; ⚠ **[ruling 16](#)'s *"DDS-only, no ECS"* is about components, not the bus.** ⚠ **Seam-law watch**: the request→bus→system→bus→ack chain is hand-rolled **per command family** (two verified, not enumerated) — pre-flight would be the **third** ⇒ generalise rather than copy |
| 56 | 🔒 **The knowing node is CGF — one pre-flight endpoint, not "whoever executes"** (user, 2026-08-13): *"the central node that decides on whether/what to confirm is the CGF subsystem."* ⇒ names what [ruling 55](#) left abstract, and ⚠ **[UXI-16/27 §2.0](UX_Feature_Modal_Surfaces.md)'s diagram was blurred** — it said *"executing node (SimHost / CGF)"*, reading as though pre-flight could land on either. 🔒 **The applying node and the deciding node are different questions**: a gizmo write may apply on SimHost, but *what to warn about* is CGF knowledge. ⭐ **Verified four ways, and the transport already encodes it**: `ICommandGateway.cs:6` is documented as *"commands from **ExCon/Editor to CGF**"* with **all four methods** targeting CGF ⇒ `PreflightAsync` adds a method to a seam that already has **exactly one peer**; `CgfLogicPack.cs:123-127,149-154` composes the **entire** intent stack (`MissionControlExecutionSystem` · `MissionAdapterSystem` · `TacticalIntentResolutionSystem` · `MissionControlModule` · `CognitiveRuntimeModule`) so `MissionPlanQueue`/`ActiveMissionPlan`/`BehaviorState` are all CGF-side; production composition roots are **exactly two** — `CgfSubsystem.cs:324` and `EditorSubsystem.cs:866` (SimHost builds the pack **only in test harnesses**); and **delete is CGF-served too** — `Hrot.Network.NED/CGF/NedCgfEntityLifecycleAdapters.cs:91-98` consumes `DeleteEntityRequest`, so the *other* confirmable action resolves against the same node. ⇒ 🔒 **`IIntentAspectProvider` is registered on CGF only** — not a cross-host interface; no routing table, no per-aspect node lookup, no merging several specs. ⭐ **The Editor is not a special case** — it composes `CgfLogicPack` in-process, so the same call resolves with **no network hop**. ⚠ **Watch item, not a gap to pre-solve**: an aspect owned by neither CGF nor the origin has no home under this rule; when one appears it either registers on CGF or the rule is revisited **then** |
| 55 | 🔒 **The KNOWING node decides what to ask; the interactive node only renders it** (user, 2026-08-13): *"ExCon can see the mission state, but it should not need to be able to in order to handle the confirmations. ExCon needs to be dumb in this aspect. The checks need to run on the node which 'knows'. The confirmation type etc. needs to come from there — ExCon is just the one who shows it to the user."* ⇒ 🔴 **[Ruling 54](#)'s mirrored describers are REPLACED** ([Correction 42](UX_Tasks_Detail.md#corrections)) — they made the origin smart, which is the thing to avoid. 🔒 **Correct shape: a pre-flight round trip.** The origin sends *"I intend to do X to E"*; the **executing** node runs its own aspect providers over **authoritative live state** and returns a neutral **`ConfirmationSpec?`** (`null` ⇒ ask nothing); the origin **renders it blind** and sends the authorized request only if the human agrees. ⭐ **One registry on one node** — `IIntentAspectProvider { AspectId · DescribeImpact · ClearFor }` — which **kills all three of ruling 54's residual constraints**: no replication requirement, no staleness, no cross-node id drift. ⭐ **And the transport already exists**: `ICommandGateway` is a *neutral* async request/response seam from ExCon/Editor to CGF (`SendMissionControlRequestAsync` → `Task<MissionCommitResult>`), so pre-flight is **one more method**, not a mechanism. ⚠ **The round trip is not on the fast path** — it happens only for actions that declare they may need confirmation, and exactly when a dialog is about to appear anyway |
| 54 | ⚠ **SUPERSEDED by [ruling 55](#) — kept for the reasoning.** **The origin asks WITHOUT knowing what it is asking about — impact describers mirror the clear providers** (user, 2026-08-13): *"how does ExCon know what to ask (and whether at all) — if the mission plan is existing at all? ExCon (or any interactive node) should not inherently know there is a mission planner. It is decoupled even on the target executing node. This might need some more complicated approach."* ⇒ ⭐ **The symmetric answer**: [ruling 52](#)'s `IIntentClearProvider` **performs** on the executing node; an `IIntentImpactDescriber` with the **same `AspectId`** **describes** on the interactive node. Neither ExCon nor the dispatcher knows what an aspect *is* — it renders whatever notes come back. ⭐ **And the state is already there**: `EntityMissionDescriptor` is replicated to ExCon's `IDerRepo` (`NedExConIngressTranslators.cs:322`), which has the same descriptor-bag API as the ECS side (`HasDescriptor<T>`/`GetDescriptor<T>`), and `MissionEditorService.cs:83` already reads it. **The mission *module* — already hosted on ExCon as `MissionPanel`/`MissionEditorService` — registers the describer; the ExCon *subsystem* stays ignorant.** 🔴 **Three residual constraints, named not hidden**: describers read **replicated** state so a warning can be **stale** (advisory only — the executor still applies unconditionally); an aspect whose state is **not replicated to the origin cannot warn**, so its owner must replicate a summary flag or accept silence; and the two halves live on **different nodes**, so **id drift cannot be statically checked** — pair them through one shared `AspectIds` constant |
| 53 | 🔒 **Confirmation is resolved AT THE ORIGIN; the infrastructure is headless by default** (user, 2026-08-13): *"`ClearsPriorIntent` should not always ask, and so deletes and others. Imagine MCP-server-driven operations — no interactive UI modals popping up desired while driven by MCP. Confirmations are allowed for interactive sessions only. The infrastructure must by default allow for headless operations. Requests coming from an interactive session should be marked as such and need to be resolved on the interactive node where the user is sitting **before** the headless and unconfirmable request is sent to the infrastructure. Note we are building a distributed system — ExCon might live on a different node than SimHost and CGF who actually perform the operations."* ⇒ 🔴 **[UXI-16/27 §2.2](UX_Feature_Modal_Surfaces.md) had the gate on the executing side** ([Correction 41](UX_Tasks_Detail.md#corrections)) — in a cluster that would try to pop a modal on **SimHost or CGF**, where nobody is sitting, and would **block every MCP-driven operation**. 🔒 **Correct layering: the confirmation gate lives in the interactive dispatcher, before the request is emitted. What crosses the wire is already authorized — receivers never ask and never block.** ⇒ headless/MCP/script/replay paths are unconfirmable **by construction**, not by a flag. ⚠ **Same rule for progress surfaces**: a headless run must not raise a modal, so `IProgressSink` binds to a **log sink** with no interactive host — and [API §6c](UX_Interaction_API.md)'s *"registration throws when `Exclusive` has no progress surface"* must be scoped to interactive composition or **every headless node throws at startup** |
| 52 | 🔒 **The clear flag must NOT know what planners exist — providers register a clear delegate** (user, 2026-08-13): *"the intent flag should not inherently KNOW what all behaviour queues and planners exist in the system… it should call some internal api where different planners register their clear delegate. Mission plan is such a provider that needs decoupling from the clearing flag."* ⇒ `IIntentClearProvider` + `IntentClearRegistry`; `TacticalIntentResolutionSystem` invokes **all registered providers synchronously** before publishing the assign, and knows nothing about missions. ⭐ **The pattern is already in this exact subsystem** — `TacticalIntentMapperRegistry` is the same shape (dictionary-backed, populated at startup, immutable at runtime, injected into the same system via `CgfLogicPack`), so this mirrors a proven registration seam rather than inventing one. 🔒 **Synchronous invocation preserves [ruling 50](#)'s atomicity** — a provider *event* would re-open the very frame-ordering hazard that ruling forbids. ⭐ **And the scope guard moves to the right place**: [ruling 51](#)'s *"never clear `TargetMemory`"* stops being a fact the caller must remember and becomes a **contract documented on the provider interface**. ⚠ Providers must be **idempotent**; an **empty registry means no clearing**, so hosts without CGF degrade safely |
| 51 | 🔒 **The intent carries a clean-start flag — but it is SCOPED, not "clear everything"** (user, 2026-08-13): *"the entity needs a clean way of cancelling any behaviors and mission plans and whatever so the newly issued behavior starts clear. Probably the intent needs to carry this general-behav-clear flag?"* ⇒ **yes, one flag on `AssignTacticalIntentEvent`** — generalising [ruling 50](#)'s `CancelsMission`. ⭐ **But the census changes what it must do**: `AssignBehaviorEvent` **already performs a clean start for all brain state** (`BehaviorIngressSystem.cs:122-166`) — `InstanceId++`, **detach old stateful slots + provision new**, `BrainBTreeState.State = default`, **HSM instance reset to the new topology**. ⇒ **the flag must NOT re-do any of that.** What actually survives an assign: **① `MissionPlanQueue` + `ActiveMissionPlan`** (the flag's real job) — ⚠ **and nothing else** — a proposed second job, zeroing `BrainBlackboard`, is **withdrawn** ([Correction 40](UX_Tasks_Detail.md#corrections)): the struct is `LayoutKind.Explicit` and **co-owned** — bytes 0-99 are the polymorphic per-behavior params region (so the new behavior redefines it and residue is unreachable), while byte 120 `ExpectedThreatLevel` belongs to `RouteContextSystem` and 126/127 to the interrupt systems, so zeroing would **wipe live cross-system state**. 🔴 **And what must NOT be cleared: `TargetMemory`** — perception is sensory truth, not intent; clearing it **blinds the unit**. Physics/nav status likewise (muscle tier). ⇒ 🔒 **name and document it for what it clears — the *intent* stack — not as a general reset** |
| 50 | 🔒 **An immediate order CANCELS THE MISSION PLAN first** (user, 2026-08-13): *"immediate order should cancel the mission plan first."* ⇒ closes [UXI-32 §2.5](UX_Feature_Entity_Commanding.md)'s *"the mission overrides the operator 20 s later"* risk — the plan does not resume because it no longer exists. 🔴 **But NOT as two events**: `BehaviorIngressSystem` reads `AssignBehaviorEvent` at `:53` and `ClearBehaviorEvent` at `:172`, so **clear runs LAST within a frame** — an abort + order pair landing together would leave the entity **brain-dead with the order silently lost**. Today it happens to work only because the abort is 1 hop and the intent is 2. ⇒ 🔒 **one operation, not two**: `AssignTacticalIntentEvent` gains a `CancelsMission` flag; `TacticalIntentResolutionSystem` (already the owner-side choke point) empties `MissionPlanQueue` + nulls `ActiveMissionPlan` **and** publishes the assign — **no `ClearBehaviorEvent` is published at all**, so the ordering hazard cannot arise. ⚠ **Consequence to surface: the plan is DISCARDED, not paused** — there is no resume; restoring it means re-committing from `MissionPanel` or reloading the scenario |
| 49 | 🔒 **A permanently greyed item is useless — it is absent** (user, 2026-08-13): *"permanently grayed item is useless."* ⇒ closes the question [ruling 47](#) raised on the **other** axis: an action a **host structurally cannot service** is **not shown at all**, not greyed-with-a-reason. 🔴 **[UXI-23 case 23.5](UX_Feature_Map_Parity.md) is inverted** ([Correction 39](UX_Tasks_Detail.md#corrections)). ⭐ **The distinction that survives**: grey means *"not now, try later"* — it is only honest for a **transient** blocker (the [API §5](UX_Interaction_API.md) exclusivity gate, [UXI-08](UX_Feature_Layout_Defaults.md) case 5's *running outside the repo*). A blocker that can never clear in this host is **not a state, it is a fact about the host** ⇒ the item does not belong in that menu |
| 48 | 🔒 **Q29's answers ARE the architect's answers** (user, 2026-08-13): *"if all Q29 are answered, take it as accepted by architect and project to designs."* ⇒ [Q29](Architect_Question_29_Entity_Commanding.md) is **closed and accepted**; rulings 38-46 are binding, and [UXI-32](UX_Issues.md#uxi-32) proceeds to a feature design without a further architect round |
| 47 | 🔒 **Incompatible menu items are ABSENT, not greyed — and carry no reason** (user, 2026-08-13), verbatim: *"I see no reason for showing the reason for items not compatible with all selected items. They don't even need to be grayed. Incompatible ones should simply not be present. In context menu this is fully ok (we do not need huge menu full of grayed items, each with different reasoning for different incompatible entity)."* ⇒ ✅ **confirms [UXR-91](UX_Requirements.md#uxr-91) and [UXI-03 §4](UX_Feature_Entity_Action_Vocabulary.md)**; 🔴 **refutes [UXI-11 §2.4](UX_Feature_Selection.md)'s *"shown, disabled, with a reason"*** ([Correction 38](UX_Tasks_Detail.md#corrections)) and withdraws my two-predicate reconciliation as unnecessary. ⭐ **The reasoning is the part to keep**: per-entity reasons do not aggregate — twelve entities can be incompatible for twelve different reasons, so there is no sentence to show. **Simplifies the design**: one AND over the selection, no `disabledReason` aggregation, no count strings |
| 46 | 🔒 **TKB: disk first, hardcoded fallback, shared code, behind a provider API** (user, 2026-08-13): *"tkb data should be read from disk by the subsystems needing tkb, with fallback to hardcoded stuff. **Shared code, reused.** Network replication might come later. **TKB access should be hidden behind some provider api** to allow for switching implementations (disk/network) later."* ⭐ **The whole stack already exists — FIVE levels** (**seam-law instance 23**), and the storage strategy is only level 1 (user, 2026-08-13: *"loader (storage strategy interface) is just the raw data provider, maybe there is another level of access class?"* — **yes**, [Correction 36](UX_Tasks_Detail.md#corrections)): **L1** `ITkbStorageStrategy` (+ `TkbUnifiedLoader` auto-detecting `.zip` vs dir) · **L2** `TkbDeserializer` + source-gen `[TkbDescriptor]` · **L3** ⭐ **`ITkbDatabase.TryGetByType(long, out TkbTemplate)` — the tkbId lookup, in production at 9+ sites** · **L4** `TkbTemplate.GetDescriptor<T>/TryGetDescriptor<T>` · **L5** `ITkbEntityTranslator.Inject(repo, entity, template)` → ECS · **↺** `ITkbHotReloadEvents` (reload notification). 🔒 **The "hidden behind a provider API" requirement is already met at L3.** 🔴 **The reload contract has subscribers and NO publisher** — `BakedAnimationCache` subscribes and invalidates; the only implementation is a **test fake** (**seam-law instance 24**) ⇒ **the shared loader is the missing publisher**, or a disk reload silently leaves stale baked caches | ⭐ **L5 is ECS-only ⇒ ExCon needs L3 + L4 and no translator.** 🔴 **What is missing is the composition**: the ~6-line load recipe exists **once**, inside `TkbLoadClusterStateHandler.cs:96-103`, with **no fallback** (`FileNotFoundException`) and a differential mtime cache worth keeping; CGF/IG/Editor call `HrotEnvironment.CreateTkb()`, which only registers the hardcoded catalog. ⭐ **That factory is the shared hook — one change reaches four hosts.** 🔒 Log which source won. ⚠ **Network later means one of two different products**: **N1** a network `ITkbStorageStrategy` (fetch the corpus — ✅ fits the interface unchanged, and changes nothing above it) or **N2** a remote `ITkbDatabase` (query per id — the shape fits, but callers assume a synchronous local lookup inside spawn systems). ⚠ **And `RouteTkbExtensions.ApplyRoutePlanToBlueprint` is a no-op stub today** but documented as *"call from every host"* — it runs in `CreateTkb()` and **not** on the disk path, so a post-load step must exist on **both** branches |
| 45 | 🔒 **Formations need no special handling** (user, 2026-08-13): *"formations now solved by issuing entity-specific command/intent to the **commander entity** — no special handling needed. TKB should be solving this case as also the commander entity should have its own specific TKB id."* ⇒ ⭐ **the group-addressing question dissolves** — *"order the platoon"* is *"right-click the platoon commander"*. No roster walk at the menu layer, no fan-out to subordinates, and `UnitRoster`'s 16-cap stops being a UI concern. [Q29 §H](Architect_Question_29_Entity_Commanding.md) |
| 44 | 🔒 **Any subsystem may issue an order, gated on TKB availability** (user, 2026-08-13): *"any subsystem can issue as long as the TKB data available (**it needs to be for ExCon as well — probably a gap**)."* ⇒ eligibility is a **data** question, not an authority one — the gates already permit any node ([ruling 35](#)). ✅ **Gap confirmed and characterised**: ExCon has **zero** `ITkbDatabase` references and a **hand-written** `(id, name)` array (`ExConSubsystem.cs:337-348`) — but it **already resolves per-`TkbType` behavior lists** via the compiled `BehaviorCatalog` (`MissionEditorService.cs:73`). 🔴 **The real blocker is transport: TKB content is replicated NOWHERE** — `EntityMasterTopic` carries only `long TkbTypeValue`; TKB moves as staged `.zip` files, and **only SimHost loads them** (CGF/IG/Editor run the compiled-in `NedTkbCatalog`). ⇒ ✅ **closed by [ruling 46](#)**: disk-first with a hardcoded fallback, so ExCon gets a `TkbDatabase` like everyone else and no wire is needed yet |
| 43 | 🔒 **The command set and its default params come from TKB — new fields** (user, 2026-08-13): *"for now the right-click command behavs and their default params should be loaded from tkb (new fields needed)."* ⇒ the menu is **per entity type**, data-driven. ⭐ **The recipe already exists end to end, for two orders**: a typed intent DTO tagged `[BehaviorContract(name, categories)]` (`DefendAreaIntentDto`) → auto-discovered into `BehaviorCatalog.GetValidBehaviors(tkbType)` → a mapper whose `TargetIntentId` **must match the same string** → `AssignBehaviorEvent`. ⇒ **adding an order = one intent DTO + one mapper + one behavior.** ⭐ `BehaviorProfileDto` (`[TkbDescriptor("AI.BehaviorProfile")]`) already carries `DefaultBehaviorHash`/`BrainTier`/`CanMove`/`CanShoot` — **TKB already references behaviors**, so a command-set descriptor sits beside it. ✅ Adding a descriptor is **backward-compatible by construction** (unknown keys skipped, missing fields fall to defaults) |
| 42 | 🔒 **Menu-generated-from-behaviors is DEFERRED** (user, 2026-08-13): *"menu will be generated from supported behaviors, but as behavs need parameters, it needs much more design to fully clarify it (blends with mission editing, **out of scope now, separate design later**)."* ⇒ this slice ships **TKB-declared** commands with **default** params; full parameter authoring is a later design. ⚠ **Not greenfield when it comes** — a schema-driven behavior-parameter form already ships inside `MissionPanel` (`BehaviorSchemaDiscovery.AutoRegister` → `BehaviorUiRegistry`, `BehaviorUiSetup.cs:24`). Pair it with [UXI-33](UX_Issues.md#uxi-33) |
| 41 | 🔒 **One action id + a JSON argument — no id per behavior** (user, 2026-08-13): *"one action id with json argument could handle any behavior in a generic way… I do not want to alloc a global action id for each possible behavior."* ✅ **Investigated and feasible.** ⭐ **The destination already has the shape** — `AssignTacticalIntentEvent { Entity, string IntentId, string JsonParams }` — **and so does the DDS record**: `GizmoInteractionBatch` carries `int ActionId` **and** `[DdsManaged] string? PayloadJson` on the same struct, filled by `StructUpdate` and left null by `MenuAction` (**seam-law instance 22**). 🔴 **8 hops in the middle carry no argument**; the sharpest is `ImGuiMenuRenderer:83` — it holds the whole DTO and calls `onAction?.Invoke(item.Id)`. ⚠ `GlobalActionRequestedEvent` is blittable ⇒ needs a **managed sibling**, not a new field |
| 40 | 🔒 **`MissionPanel` stays as-is for now** (user, 2026-08-13): *"the mission panel is the right place where missions are edited, it likely needs no urgent changes as it works good enough now (**redesign for better UX needs dedicated design session — just pls note it so we do not forget**)."* ⇒ filed as **[UXI-33](UX_Issues.md#uxi-33)** so it is not lost. ⚠ Whole-plan-replace-only is **known and accepted**; `CMD_APPEND_TASK`/`CMD_INSERT_TASK` being dead is not a defect to fix here |
| 39 | 🔒 **One behavior per order, placeholder bodies allowed** (user, 2026-08-13): *"there will need to be a behavior created for each of them (**full implem out of scope of this — maybe just empty placeholder behaviors for now**)."* ⭐ **This is what makes the slice shippable** — the plumbing proves out end to end without anyone specifying what *Resupply* means. ⚠ But `BehaviorId`s are *"stable forever, never reused"* and the registry must be complete **before the first frame** ⇒ **allocate the ids deliberately now**; a body can be replaced, an id cannot |
| 38 | 🔒 **A right-click order is an IMMEDIATE INTENT, not a mission edit** (user, 2026-08-13): *"mission plan exists both for saving to scenario (and being applied on scenario start) as well as for runtime modification (only whole plan replace implemented now). **The right click commands make sense at exercise runtime as immediate orders replacing any previously active behavior**… the right click menu should issue the tactical intent (and replace any previous behavior)."* ⇒ **Q29 §B answered: B1.** ⚠ [Correction 35](UX_Tasks_Detail.md#corrections) — my lean B2 confused the **planned** form with the **immediate** one. ⭐ Three problems dissolve: no `BaseVersion` conflict, no 8-phase cap, and no partial-failure fan-out. ⚠ **Inherent consequence to surface in the UI**: the mission resumes control at the next phase transition and overrides the operator's order |
| 37 | 🔒 **Commanding is missions, and CGF is a first-class commander** (user, 2026-08-13), verbatim: *"the actions like MoveHere, Engage, Stop, Properties, Teleport, Repair, Reinforce, Resupply, Transfer are unresolved and need a dedicated design pass. **The only supported way of commanding entities now is via a mission having a list of conditional behaviors to perform.** This is not ExCon-only, must be equally supported by the CGF subsystem (who owns the entity brain)."* ⇒ 🔴 **[UXI-23 §5](UX_Feature_Map_Parity.md)'s open decision is CLOSED as ill-posed** — all three options assumed the nine ids were ordinary actions with missing handlers ([Correction 33](UX_Tasks_Detail.md#corrections)); they are not, and my lean B (*"keep them ExCon-only"*) was wrong on both halves. ⇒ **[UXI-32](UX_Issues.md#uxi-32)** + **[Q29](Architect_Question_29_Entity_Commanding.md)**, `RW-H`, **architect pass required before any binding**. ⭐ **Verified: the mechanism exists and is under-adopted** — `AssignTacticalIntentEvent` is a single choke point any node may publish to, already routed by complementary `HasAuthority<BehaviorState>` gates (**seam-law instance 20**); the whole production order vocabulary is **two** mappers, and **none of the nine ids** is one |
| 36 | 🔒 **Drag is unified — one registration, no callbacks** (user, 2026-08-12): *"drag handling needs unification, sharing same implem in all map subsystems."* Today it is wired **5 different ways**; the `onDragCommitted` hook has **exactly one caller** (IG). ⇒ `new EntityDragGizmoDefinition()` everywhere; IG's `onDragCommitted`/`OnEntityDragEnded`/`SendGeoSpatialUpdate` **retire**. ⭐ **Continuous-drag and shift-immediate are reachable only from a TEST HOOK** (`IgApplication.cs:2291-2305`); production is already commit-only, so unification is behaviour-preserving. **Seam-law instance 14**: `MapUserConfig.ContinuousDragUpdates` has no production reader |
| 35 | 🔒 **Two classes of inbound writer, with INVERSE gates** (user, 2026-08-12): *"writing to unowned components is reserved for network replication ingress translators, never for change requests like attrib/descriptors — these must be received by any node and applied only to owned ECS components."* ⇒ **replication ingress**: `if (HasAuthority) skip` — writes **unowned** ghosts **by design**. **Change-request appliers**: `if (!HasAuthority) skip` — **never** write unowned. 🔒 The notion is **native component ownership**, so the descriptor `FIXME` was right. 🔴 **UXI-30's sweep must classify each site first** — inverting a replication translator breaks ghost updates repo-wide |
| 34 | 🔒 **Wire form is economics; the ownership gate is the invariant** (user, 2026-08-12): *"whether to update whole descriptor or just single attrib is the network bandwidth/performance decision, otherwise both should be applied with the ownership gate on the receiver."* ⇒ **one architecture, two encodings** — attribute records for scalars, whole descriptor for structure-changing lists. 🔴 **The invariant does not hold today**: of five appliers, **2 gate on nothing**, **2 gate on the descriptor key**, **1 on native component authority**. [UXI-30](UX_Issues.md#uxi-30) widened to cover all five |
| 33 | 🔒 **The binary path's missing authority check is a SEVERE ERROR** (user, 2026-08-12): *"no reason why the binary path should be different from the json path in this aspect."* ⇒ filed as **[UXI-30](UX_Issues.md#uxi-30)**, a **prerequisite** for UXI-29. ⭐ **Census: zero production senders of `AttributeRecords`** — the fix breaks nothing; the binary path is receive-only with no producers (**seam-law instance 13**). ✅ **`GeoHeading` approved** — one constant + one handler, mirroring `SimTransformAttributeInstaller:81-86` |
| 32 | 🔒 **Intents are adopted, and their wire form is the BINARY attribute change request** (user, 2026-08-12): *"if intent is a local FDP event translated to a network binary attrib change request message, then ok, lets use intents in entity attribute changing gizmos."* ⇒ local `Fdp` bus event → `UpdateEntityAttributeRequest.AttributeRecords` (ATTR2), not JSON, not `dtWorldPos`. 🔴 **Two verified gaps block it**: the **binary path has no authority gate** (`CanWrite` is called only from `JsonAttributeCompiler`), and **no rotation attribute ID exists** (`AttributeIds` has only Name/Affiliation/GeoLat/Lon/Alt) |
| 31 | 🔒 **Pick box and selection ring are shared and identical in every map subsystem** (user, 2026-08-12) — *"should be the same in all map subsystems, shared and reused"*. ⭐ Both **already are single implementations** (`EmitPickBox`, `SelectionHighlightGizmo`); the gap is **adoption only** — CGF has neither, SimHost lacks the ring. 🔒 No per-host variants or parameters. ⚠ **Mismatch found:** pick box **8 px** vs ring **20 px** radius — the visible affordance exceeds the clickable one, identically everywhere |
| 30 | 🔒 **Structural ECS changes go through the command buffer** (user, 2026-08-12): *"why not modify ECS using command buffers, which is safe always?"* ⇒ the `ISelectionState` view records on the ECB rather than writing the repo. ⭐ Already consistent with [ruling 15](#) — a menu item that changes selection **is** an action handler, so it already has a per-action ECB. ⚠ **`EntityQuery` iteration was never at risk** ([Correction 29](UX_Tasks_Detail.md#corrections)) — it is an index scan, not a chunk walk |
| 29 | 🔒 **Multi-delete requires a modal confirmation** (user, 2026-08-12) naming what will be removed; Cancel deletes nothing. ⇒ [UXI-16](UX_Issues.md#uxi-16) gains a hard requirement, delivered via [ruling 13](#)'s modal machinery |
| 28 | 🔒 **Click semantics** (user, 2026-08-12). **Right-click selects.** On an **already-selected** entity ⇒ selection **unchanged**, menu shows items applicable to **all** selected. On a **non-selected** entity ⇒ **clear, then select just it**. **Empty click clears; reload clears.** ⇒ file-manager convention. [UXI-11](UX_Feature_Selection.md) §2.3 |
| 27 | 🔒 **Selection: CGF is identical to SimHost/Editor; ExCon is correctly different** (user, 2026-08-12). *"CGF should be no different from SimHost nor even Editor regarding selection — same visuals, same interaction, same ECS component. **Selection is subsystem-local.** ExCon has no map, it is an ImGui-panel-only subsystem whose role is to control the IG map and CGF and SimHost over DDS; selection is controlled remotely and stored on ExCon in a completely separated way."* ⇒ [UXI-11](UX_Issues.md#uxi-11)'s ExCon half is **refuted** ([Correction 27](UX_Tasks_Detail.md#corrections)); the CGF half stands |
| 26 | 🔒 **Dim is style, tag is filter** (user, 2026-08-12), verbatim: *"dimming is style, tag is layer filter"*. ⇒ **search dims and never becomes a tag**; my open question *"should search also get a hide mode?"* is **withdrawn as ill-posed** — it conflated the two. If *hide-non-matches* is ever wanted it is simply a new tag, no redesign. **`MapCanvas.ActiveLayerMask` survives** as the *"which renderers run"* switch. **Tag width stays `uint`/32** — no change needed |
| 25 | 🔒 **`ZIndex` becomes the draw order and pick priority; dim ≠ hide** (user, 2026-08-12). *"Is there a dedicated sorter field? If yes then of course it should be made working."* ⭐ **There is — and it is dead**: `ZIndex` is set by **no production code** (only 3 lines in `GizmoMap.Example`), so every production primitive is `ZIndex = 0` and sorting collapses to `DebugLayer`. ⇒ the migration is cheap — reproduce only `Entities`→`Perception`→`AiHelpers` as `ZIndex` 0/1/2. **Seam-law instance 12.** ⭐ **Dimming is a *colour* concern**, applied at emit via an `IStyleSource` — no mask, no renderer change, works on the remote terminal |
| 24 | 🔒 **Layer visibility is ALL, and overflow degrades to visible** (user, 2026-08-12). A multi-tagged primitive is **hidden if *any* of its tags is hidden** ⇒ *"hide hostiles"* works and the two axes compose; an untagged primitive is always visible. On the **257th** distinct combination: **degrade to always-visible and log** — never silently drop. ⭐ The rule lives in one place — the backend's per-frame evaluation — so it can change without touching wire, renderer or gizmo |
| 23 | 🔒 **Map layers are TAGS, not a partition** (user, 2026-08-12). *"I would like one entity **or gizmo** to belong to more than one layer."* ⭐ **And the byte survives**: *"maybe the `DebugLayer` byte needs to be dynamically calculated out of the bitmasks"* ⇒ it becomes an **interned combination id**, so `DebugPrimitive` needs **no wire change** and the renderer's filter is unchanged. 🔴 **Blocker found:** `DebugLayer` is also the **sort key** (`DebugPrimitiveRenderer2D.cs:178`) and the **hit-test priority** (`DebugGizmoLayer.cs:447`) — both must move to `ZIndex`. [Q28](Architect_Question_28_Map_Layers.md) §B |
| 22 | 🔒 **Mutate ECS only where you own it** (user, 2026-08-12). *"CGF does not own `SimTransform`, so it needs to send a request to SimHost, not change ECS directly — similar to Delete. **Editor owns all.**"* ⇒ CGF's *Rotate* must mirror `DeleteEntity`'s `DestroyEntityCommand` publish-by-`NetworkId` (`CgfSubsystem.cs:777-785`). 🔴 **No pose-change command exists** — Spawn and Destroy have network paths, pose does not; drag and rotate both `GetComponentRW<SimTransform>` directly ⇒ [UXI-29](UX_Issues.md#uxi-29) |
| 21 | 🔒 **One pose source, one symbol path** (user, 2026-08-12). *"CGF is not different from the others — all should use `SimTransform`, the same gizmo, the same DIS-type/TKB-derived shape; maybe just IG can override via DDS."* CGF's `NetworkTransform` preference is **deleted, not migrated** ([Correction 26](UX_Tasks_Detail.md#corrections)). ⭐ **Colour overrides may still be subsystem-specific** — via the same modification-layer mechanism as IG's DDS layer (`IStyleSource`) |
| 20 | 🔒 **Two classes of map** (user, 2026-08-12). **IG = the production 2D map, remotely controlled via the DDS API**; `StyleResolutionSystem` was written *for it*, DDS-provided styles being its point. **Editor · CGF · SimHost · ReplayBrowser = service-level maps** — sources are **local/user input + ECS, no remote DDS control**. ⇒ **Share the infrastructure where it is generic, reusable and helpful; never make a service map depend on the DDS layer.** [UXI-10](UX_Feature_Entity_Symbology.md) §2.5 |
| 19 | ✅ **The CGF / SimHost initial-view shift is approved** (user, 2026-08-12). Removing the hardcoded `(640, 360)` offset changes what those two subsystems show on first launch — accepted as the correct behaviour arriving. [UXI-09](UX_Feature_Map_Viewport.md) §5 |

## 3. The threading/ECB solution (ruling 15) — the shape

```csharp
var ecb  = new EntityCommandBuffer();                       // per dispatch
var prev = SynchronizationContext.Current;
SynchronizationContext.SetSynchronizationContext(_pump);    // ONLY around the call
var task = descriptor.Execute(ctx);                          // state machine captures _pump
SynchronizationContext.SetSynchronizationContext(prev);
// once per Update(), on the tick thread:  _pump.Drain();
```

| Outcome | Buffer |
|---|---|
| completes | **played back** on the main thread |
| cancelled / faulted | **dropped, never played back** ⇒ atomic commit |

⚠ **`ConfigureAwait(false)` anywhere in the awaited chain breaks it silently** — needs an analyzer or
review rule (UC-44c).

🔒 **This is NOT the withdrawn proposal.** That was an *ambient, process-wide* `SynchronizationContext`
([Correction 21](UX_Tasks_Detail.md#corrections)); this is scoped to the handler invocation only.

## 4. Verified engine facts — do not re-derive

| Fact | Evidence |
|---|---|
| `NativeEventStream<T>.Write` is **thread-safe**, `Interlocked`-reserved, double-buffered | `NativeEventStream.cs:83-101`, README §2.6 |
| `ISimulationView.GetCommandBuffer()` → **per-thread** ECB, `trackAllValues: true` | `EntityRepository.View.cs:13,33-35` |
| ECB records **events too** | `OpCode.PublishUnmanagedEvent = 8`, `PublishManagedEvent = 9` |
| Kernel flushes **all** thread buffers on the main thread at **BeforeSync** | `ModuleHostKernel.cs:519-532` |
| The Editor runs that kernel | `EditorSubsystem.cs:585`, driven `:1617` |
| `EntityRepository.FlushCommandBuffers()` is **dead API** (0 production callers) | `EntityRepository.View.cs:43` |
| Rule 7: background code uses `ISimulationView` + ECB, never the live repo | `HROT-PROGRAMMERS-GUIDE.md` Part 0 |
| 🔒 **Replication ingress writes UNOWNED components on purpose** — `if (!isLocallyOwned) SetComponent(...)`. Its gate is the **inverse** of a change-request applier's, and both are correct | `GeoSpatialIngressTranslator.cs:85-89` |
| **`HasAuthority<T>` is PER-COMPONENT** — a bit in a per-entity `AuthorityMask`; a Brain can own `BehaviorState` while a Muscle owns `SimTransform` on the same entity | `EntityRepository.cs:1060-1063,1120-1127` |
| ⭐ **The authority-routing precedent**: two systems read the same event with **complementary** gates — `if (!HasAuthority) continue` applies locally, `if (HasAuthority) continue` forwards to the owner | `TacticalIntentResolutionSystem.cs:95`, `TacticalIntentEgressTranslator.cs:72` |
| **A generic inbound patch channel exists** — `UpdateEntityAttributeRequest`, authority-gated **per field** inside the compiler | `JsonAttributeCompiler.cs:37-41` (`CanWrite<T>()` → `reader.Skip()`) |
| 🔒 **Commanding chain (UXI-32)** — `MissionControlCommand` → `MissionControlExecutionSystem` (**live repo only**; 10-frame entity wait then NAK) → writes **both** `MissionPlanQueue` (≤8 phases, ints) **and** `ActiveMissionPlan` (managed, unbounded, strings) → `MissionDirectorSystem` → `MissionAdapterSystem` → ⭐ **`AssignTacticalIntentEvent`** → complementary `HasAuthority<BehaviorState>` gates → `ITacticalOrderMapper` → `AssignBehaviorEvent` → `BehaviorIngressSystem` | `MissionControlExecutionSystem.cs:145-149,182-186` · `MissionAdapterSystem.cs` · `TacticalIntentResolutionSystem.cs:95` |
| **`MissionPanel` is a shared 815-line authoring panel** in `Hrot.Presentation`, hosted by **ExCon and the Editor only** — ⚠ **not CGF**, the node that owns the brain. The Editor already syncs map selection into it | `Panels/MissionPanel.cs` · `ExConSubsystem.cs:357` · `EditorSubsystem.cs:1582,1651` |
| 🔴 **Of five imperative mission verbs, two are dead** — `CMD_APPEND_TASK` and `CMD_INSERT_TASK` have **no producer and no handler**, falling to `default: NotSupported`. ⇒ **there is no "add one task" operation**; every editor rewrites the whole plan | `MissionControlExecutionSystem.cs:243-245` |
| **Mission commits are optimistically versioned** — `BaseVersion != currentVersion` ⇒ `VersionConflict` NAK ⇒ a multi-entity order **can partially fail** | `MissionControlExecutionSystem.cs:145-149` |
| 🔒 **`BehaviorId`s are stable forever and never reused; NEVER `string.GetHashCode()`** — use `BehaviorIds` + `TryGetId`. `BehaviorIngressSystem` (Input) is the **sole `BehaviorState` writer**; transitions carry ≥1-frame latency | guide `:469-474` |
| ⚠ **`EntityQuery` iteration is an INDEX SCAN, not a chunk walk** — `_maxIndex` snapshotted at construction ⇒ Add/RemoveComponent mid-iteration does **not** invalidate it. The guide's chunk-iteration ban is scoped to **HSM/BTree `SharedAi` thunks** | `EntityQuery.cs:106-123`, guide `:483-484` |

## 5. 🔴 Verified defects the design closes

| | Evidence |
|---|---|
| Two `_focusedGizmo` arbiters on one bus, no arbitration | `EditorSubsystem.cs:1122-1134`, `:65`/`:31` |
| Per-`Entity` injection leaves two tools alive | `DataDrivenGizmoSystem.cs:74` |
| Double *Mark Target* → two concurrent picks | no guard exists |
| Two `async void` handlers swallow faults | `:1462`, `:1479` |
| `--mode all` first run hides **22** perspective-bound windows | [UXI-06](UX_Feature_Perspective_Restore.md) |
| SimHost/CGF emit **no** per-entity map menu | `SimHostApp.cs:337-345`, `CgfSubsystem.cs:497-500` |
| Editor + ReplayBrowser never set `MapCamera.Offset` ⇒ *Center on Entity* lands at the **top-left pixel** | `EditorSubsystem.cs:1395`, `ReplayBrowserSubsystem.cs:134` · [UXI-09](UX_Feature_Map_Viewport.md) |
| `Offset` is written **once at construction** in all 5 hosts ⇒ **resize decentres every map** | one `Offset` write per site; none in any resize path |
| **Perspective switch transports `Offset` between subsystems** — a screen-space value treated as portable camera state | `MapCameraView.cs:10-18` → `MapCamera.cs:253` → `SubsystemOrchestrator.cs:175-177` |
| 🔴 **`ResolvedStyle` (tint/affiliation/damage, 3-layer merge) is read by no renderer** — the map hardcodes `Rgba32(100,220,255)`, so friend and hostile look identical | `EntityPresentationGizmoShared.cs:92` · [UXI-10](UX_Feature_Entity_Symbology.md) |
| **CGF's semantic shapes are `alpha 0`** (bypasses the shared helper) and it emits **no pick box** ⇒ unselectable | `CgfEntityPresentationGizmo.cs:45-49` |
| **`VisualData.MapShapeName`** is authored, translated, and **read nowhere**; `GetShape`'s `shapeName` is always `null` | `VisualData.cs:33`, `PresentationTkbTranslator.cs:41`, `DebugPrimitiveRenderer2D.cs:410` |
| ✅ **UXI-19 verified** — the Editor emits **two** of every presentation primitive, and the second ignores culling | `StatelessGizmoSystem.cs:104`, `EditorSubsystem.cs:1094-1097` |
| **Selection: two stores, THREE writers, hand-synced** — ⚠ *not* desynced ([Correction 28](UX_Tasks_Detail.md#corrections)); `EditorSubsystem.cs:1226-1230` re-implements `SetSelected` and writes both | `SelectionInteractionSystem:170-172`, `EditorSubsystem.cs:1226-1230,1292-1297` |
| **CGF runs no `SelectionInteractionSystem`** ⇒ no entity ever gets `SelectionState` ⇒ the ring gizmo cannot match | registered by IG/ReplayBrowser/SimHost/Editor only |
| 🔴 **Rotating an entity in CGF does not visibly rotate it** — rotator writes `SimTransform`, gizmo draws `NetworkTransform` | `EntityRotatorGizmo.cs:118-122`, `CgfSubsystem.cs:605` |
| **`SetLayerMask(ushort) { }` is empty** — `MapCanvas.ActiveLayerMask` has **zero** effect on individual primitives; only `LayerControlGizmo`'s 256-bit mask filters, and only 3 bits are used | `Fdp.Presentation/Vis2D/Gizmos/DebugPrimitiveRenderer2D.cs:23`, called `DebugGizmoLayer.cs:100` |
| 🔴 **`ISelectionState` has NO additive operation** — its only mutator is `PrimarySelected`, whose setter is commented *"resets selection to just that one"*. `AddSelection`/`ClearSelection` exist on `DefaultSelectionState` only, are **not on the interface**, and have **zero callers** ⇒ multi-select is unreachable through the shared seam | `DefaultSelectionState.cs:19-53` · [UXI-24 §1.4](UX_Feature_Multi_Select.md) |
| 🔴 **Rubber-band selection never notifies `OnSelectionChanged`** (`ExecuteBoxSelection:202-218`), unlike click (`:84`) and tiny-drag (`:193`) ⇒ the object store and the component **do** desync, in the multi path only | [Correction 32](UX_Tasks_Detail.md#corrections) |
| 🔴 ***"Mark Target for N Units"* can never show N > 1** — registered via `LambdaEntityContextMenuHandler`, which implements only the **single**-entity overload, so the panel hides it at `selCount > 1`; and its count reads the collapsing store | `EditorSubsystem.cs:1452-1495` vs `EntityInspectorPanel.cs:370-398` |
| **The map context menu is a PROJECTION, not a query** — `ContextMenuProjectorGizmo` runs per entity per frame and picks one of 4 pre-serialized JSON constants ⇒ the menu is decided **before** the click and is selection-blind | `ContextMenuProjectorGizmo.cs:97-127`, consumed at `GizmoMap.Presentation/Layers/DebugGizmoLayer.cs:227-235` |
| **Right-click already publishes `GizmoInteractionStartedEvent`** (`:221`) **before** the menu is scheduled (`:227`) ⇒ [ruling 28](#) and [case 11.7](UX_Feature_Selection.md) hold by construction, not by new ordering work | `GizmoMap.Presentation/Layers/DebugGizmoLayer.cs:221` |
| **`GlobalActionRequestedEvent` carries ONE `Entity Target`**; the dispatch loop is one handler call per event ⇒ a `PerEntity` fan-out publishes **N events** and needs **no change** to either | `GlobalActionRequestedEvent.cs:12-18`, `GlobalActionDispatchSystem.cs:26-30` |
| **`MapLayerAssignmentSystem` classifies every entity into 5 layers → `MapDisplayComponent.LayerMask`, and the symbol emitter never reads it** (always `layer: 0`) — ⭐ *the same resolve-then-discard failure as the tint* | `MapLayerAssignmentSystem.cs:97-127` vs `EntityPresentationGizmoShared.cs` |

## 6. ✅ OPEN decisions — NONE

All three questions this thread raised are closed.

| # | Question | Outcome |
|--:|---|---|
| ~~1~~ | The 9 orphan action ids | ✅ **[Ruling 37](#)** — a **capability gap**, not a binding choice ⇒ [UXI-32](UX_Issues.md#uxi-32) / [Q29](Architect_Question_29_Entity_Commanding.md), now answered, architect-accepted ([ruling 48](#)) **and designed** |
| ~~2~~ | Partial applicability in a multi-selection | ✅ **[Ruling 47](#)** — incompatible items are **absent**: not greyed, no reason |
| ~~3~~ | Host-unserviceable actions — greyed or absent? | ✅ **[Ruling 49](#)** — *"a permanently grayed item is useless"* ⇒ **absent**. [UXI-23 case 23.5](UX_Feature_Map_Parity.md) inverted ([Correction 39](UX_Tasks_Detail.md#corrections)) |

## 6b. Needs a **Windows** session (cannot be done here)

| | |
|---|---|
| 🔴 **UXI-28 is gated on it** | the layer-panel round-trip is **complete in source** (every link cited in [§5](UX_Feature_Map_Layers.md)) yet reported non-working. Determine the break **before** building — the same break would swallow the generated panel |
| The golden-path walk | never performed; `UXT` tasks are cut from it |
| ✅ **Closed** | the UXI-09 ImGui question — `cimgui.dll` exports `igDockBuilderGetCentralNode` **and `ImGuiDockNode_Rect`**, so tier T2 is two `DllImport`s with no struct-offset arithmetic |

## 6c. Next steps, in order

1. ⏭ **Designing continues** — user, 2026-08-10: *"keep designing now rather than rewriting tasks later because we find new unexpected stuff"*. ✅ **09 · 10+19 · 11 · 16 · 23 · 24 · 27 · 28 · 29 · 32** all designed this session. 🎯 **Next: UXI-12** (spawn UI ×4), or **UXI-13** (gizmo main-menu block copy-pasted ×4 — a cheap seam-law one).
2. Cut `UXT` tasks — **none cut yet**, deliberately deferred by the user.
3. Remaining undesigned: **UXI-12..15, 17, 18, 20, 21, 31, 33, 34** · **UXI-25** 🔒 blocked on UXI-04 · **UXI-30** specified in [UXI-29 §3.3b-c](UX_Feature_Authority_Aware_Writes.md), needs no separate design doc.
4. ✅ **RESOLVED — host pump + playback sits immediately before `_kernel.Update()`** (`EditorSubsystem.cs:1618`); the kernel flush precedes `Bus.SwapBuffers()` (`ModuleHostKernel.cs:523-534`), so ops land visible **in the same frame**. Precedent: `_aiCoordinator.DrainPendingCallbacks()` (`:1620-1624`). ⚠ *Unpinned:* where ImGui panel drawing sits relative to `EditorSubsystem.Update()` — affects only which frame a synchronous handler commits in. [API §6d](UX_Interaction_API.md#6d--where-the-hosts-playback-sits--resolved-2026-08-10)

## 6d. 🔴 Dependency order — designs now constrain each other

| Order | Why |
|---|---|
| **UXI-30 → UXI-29** | gate the appliers **before** routing intents onto them, or the unguarded write just moves from the gizmo to the receiver |
| **UXI-10 → UXI-11 → UXI-29 → UXI-23** | CGF needs the pick box, then the selection chain, then **legal edits**, then parity. Binding *Rotate* in CGF before UXI-29 gives it an action that writes a component it does not own |
| **UXI-29 → CGF *Rotate*** | [UXI-11 case 10.23](UX_Feature_Selection.md) explicitly does **not** claim to fix it |
| **UXI-04 → UXI-25** | the shared ORBAT panel is impoverished until UXI-04 lands |
| ✅ **Q29 → any order binding — UNBLOCKED** | rulings 38-46 answer it. `Properties` and `Teleport` are **not** orders and may be bound immediately; the other seven are **immediate tactical intents** (never mission edits), each needing an intent DTO + mapper + behavior (placeholder bodies allowed, [ruling 39](#)) |
| **UXI-24 ← ruling 29** | the multi-delete confirmation must sit on the **raw `Delete` key** path, which bypasses the action vocabulary entirely ([UXI-23 §3.4](UX_Feature_Map_Parity.md)) |
| **UXI-03 + UXI-24 → UXI-16/27** | the confirmation is driven by `EntityActionGroup.Destructive`, and case 16.7 (the raw `Delete` **key** confirms too) needs UXI-24 §3.6's key/menu convergence — **without it the key still deletes silently** |
| **UXI-32 ↔ UXI-16/27** | ⭐ **mutual**: UXI-32's `ClearsPriorIntent` is a confirmation consumer, and its `IIntentAspectProvider` is what UXI-16/27's pre-flight calls to build the `ConfirmationSpec` ([ruling 55](#)) |
| **UXI-11 → UXI-03 → UXI-24** | one store, then the descriptor that carries `Execution`, then the fan-out. ⚠ **UXI-24 amends UXI-11**: `ISelectionState` must gain `Add`/`Remove`/`SetMultiple`/`Clear` — its only mutator today (`PrimarySelected`) **collapses the selection to one**, so UXI-11's *"handlers need no change"* cannot deliver multi-select |

## 7. ⚠ Process rules earned the hard way

| | |
|---|---|
| **Rule 6** | every design opens with a **Prior art** section citing the [Seam Inventory](UX_Seam_Inventory.md) — the seam usually **already exists and is under-adopted**. ⭐ **25 instances so far.** From [UXI-24](UX_Feature_Multi_Select.md): **16** the `stateFlags` modifier byte (bits 1-6 free, documented *"always 0"* for clicks), **17** `Vis2DInputMap.MultiSelectMod`/`BoxSelectMod` (zero readers, two owners), **18** `IgApplication.OnCanvasWorldClick` (a complete fan-out reachable only from test hooks — the ruling-36 shape again), **19** `PushContextActions(…, forSelection, …)` (fed the whole id list while the menu beside it is built from `[0]`). From [Q29](Architect_Question_29_Entity_Commanding.md): **20** `AssignTacticalIntentEvent` (a network-transparent, authority-routed choke point any node may publish to — **two** mappers registered), **21** `MissionPanel` (815 shared lines, hosted everywhere except the node that owns the brain), **22** `GizmoInteractionBatch.PayloadJson` (a JSON field on the same wire record as `ActionId`, filled by `StructUpdate` and left null by `MenuAction` — the whole *"one id + a JSON argument"* proposal needs **one existing field filled**), **23** `ITkbStorageStrategy` (a storage-medium provider seam with two implementations and an auto-detecting facade, while the load recipe that uses it is written once inside a SimHost handler), **24** `ITkbHotReloadEvents` (a cache-invalidation-on-reload contract with a live subscriber and **no publisher** — its only implementation is a test fake) |
| **Rule 6c** | ⚠ **never read a reference *count* as adoption — open the call sites.** [Correction 22](UX_Tasks_Detail.md#corrections): 8 tests vs 3 real calls, and I reported "zero consumers" from the ratio. ⚠ **And counting *constructions* misses hosts that default silently** — CGF omits the shape-library argument entirely ([Correction 24](UX_Tasks_Detail.md#corrections)) |
| **Rule 6d** | ⚠ **"the seam is unused" has two very different meanings** — an interface nobody *calls*, vs one called every frame with a dead parameter and no second implementation. UXI-10 was the second kind; the fix differs completely |
| **Rule 6e** | 🔴 **Inventory claims go through the graph index, not grep** (user, 2026-08-13: *"be more thorough in what is easy to read from the existing sources — it is difficult to believe your conclusions otherwise"*). ⚠ **Grep answers *"does X exist"*; it cannot answer *"what is the complete set of X"*** — and almost every claim this programme makes is the second kind. [Correction 37](UX_Tasks_Detail.md#corrections): three rounds of grep produced a four-interface TKB inventory; **one `search_graph(name_pattern=".*Tkb.*", label="Interface")` returned five**, and the missing one was load-bearing. ⇒ **Any sentence of the form "there are N of these" / "nothing implements this" / "these are the levels" starts with `search_graph`.** ⚠ **But measured the other way too**: `trace_path` on `ITkbDatabase.TryGetByType` found **3 callers** (all tests/examples) where grep found **9+ production sites** — C# **interface dispatch defeats the call resolver**. ⇒ 🔒 **graph for inventory and structure, grep for call sites, and never trust either alone for an exhaustive claim** |
| **Rule 6b** | before claiming an **engine-level defect**, read `README.md` + the Programmer's Guide |
| ⚠ | **follow the call one level deeper** — I read a dispatcher and missed synchronization in the stream |
| ⚠ | subagent reports have failed verification **7 times** — re-derive every delegated claim. ⚠ The newest ([Correction 34](UX_Tasks_Detail.md#corrections)) reported a wired *consumer* whose *producer* had zero production callers, and would have had me contradict the user on a point where the user was right |
| ⚠ | resolve a duplicated type by **project reference**, never by namespace |
