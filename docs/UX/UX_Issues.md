# Scenario-Authoring UX — Issue Register (`UXI`)

> **Formalised 2026-08-10.** The "what is wrong" discussion is **closed**; this is its output.
> Mirrors the blueprint programme's [issue tracker](../blueprints/Blueprint_Issues_Tracker.md) pattern.
>
> **Flow:** `UXI-nn` (an issue) → **a feature design doc** → `UXT-nn` (implementation tasks).
> **An issue is not ready to break into tasks until its design doc exists and is agreed.**
>
> 📖 **Vocabulary:** [UX_Glossary_Host_Mode_Subsystem.md](UX_Glossary_Host_Mode_Subsystem.md) — *process*,
> *mode* and *subsystem* are not equal, and "host" here means **subsystem**.
>
> Evidence for every issue: [UX_Current_UI_Architecture.md](UX_Current_UI_Architecture.md) ·
> Scope: [UX_Requirements.md](UX_Requirements.md) · Order: [UX_Cleanup_Path.md](UX_Cleanup_Path.md)

**Complexity:** `WIRING` = call existing code · `RW-L` ≲150 lines · `RW-M` new component / some design ·
`RW-H` new subsystem or architect decision first. 🔴 = correctness / data-loss / trust defect.

**Status:** ☐ open · ▣ design in progress · ✅ designed, ready to cut into tasks · ☑ done ·
⊘ refuted · 🔒 blocked.

## Register

| # | ID | Issue | Cmplx | Req | Design doc | St |
|---|---|---|:--:|---|---|:--:|
| **A** | | **Foundation & hygiene** | | | | |
| | <a id="uxi-01"></a>**UXI-01** 🔴 | **Superseded UI, and the namespace that lies.** `Hrot.UI.Common` builds nowhere yet owns the namespace the live panels declare | `RW-L` | — | [UX_Feature_DeadUI_Removal.md](UX_Feature_DeadUI_Removal.md) | ✅ |
| | <a id="uxi-02"></a>**UXI-02** | **Half-built items need a decision each** — `ScenarioEditorModule`, `SelectionRenderSystem`, `WorkspaceMenuBuilder`, `EditorTool.Select`. Not dead; each encodes an intent | `RW-L` | — | [UX_Feature_HalfBuilt_Decisions.md](UX_Feature_HalfBuilt_Decisions.md) | ✅ |
| | <a id="uxi-20"></a>**UXI-20** `P2` | **The `Hrot.UI.Common` namespace outlives its project.** After [UXI-01](#uxi-01) the name is inaccurate rather than hazardous — ~87 files, 4 test projects, one co-owned file. Never on the critical path | `RW-L` | — | [UXI-01 §2](UX_Feature_DeadUI_Removal.md#2--the-design-decision-delete-now-rename-later-or-never) | ☐ |
| | <a id="uxi-21"></a>**UXI-21** `P2` | **The Workspace document switcher has a model and ~15 tests but no renderer.** `WorkspaceMenuBuilder` builds open-asset entries with active/dirty markers that nothing draws | `RW-L` | — | — | ☐ |
| | <a id="uxi-22"></a>**UXI-22** | ✅ **CONFIRMED, and wider than filed.** Neither SimHost nor CGF calls `Hrot.Common.Diagnostics.Gizmos.GizmoRegistrar` — costing them the selection ring **and** the map entity context menu, health bars, LOS, vis-cone, spatial grid. Absorbed into [UXI-23](#uxi-23) | `RW-L` | [UXR-90](UX_Requirements.md#uxr-90) | [UX_Map_Parity_Baseline.md](UX_Map_Parity_Baseline.md) | ⊘→23 |
| **H** | | **Cross-host parity** | | | | |
| | <a id="uxi-23"></a>**UXI-23** 🔴 | **SimHost and CGF lack the common map-interaction set.** No rubber-band visual, no measure, no centre-on-selected, no delete-selected, and **CGF has no `SelectionInteractionSystem` at all**. Absorbs [UXI-22](#uxi-22). ⭐ **Mostly one missing registrar call per host.** 🔒 **Ruled 2026-08-10: all three share the FULL set; differences are data availability or host rules, never set membership** — so this is wiring, not curation. ⭐ **Absorbs UXI-04 step 5** (turn the map menu on in SimHost/CGF) **and its prerequisite: CGF constructs no `GlobalActionRegistry` at all (0 handlers) and SimHost binds only 4** — none of them Center/Select/Delete. Activating the map menu before binding them yields **inert items** ⚠ **Re-scoped 2026-08-12: it is not one call, and there are TWO dispatch mechanisms.** 🔴 **IG — the richest map host — uses none of the shared registry**, but a hand-written `switch` (`IgApplication.cs:2386-2400`) covering 6 ids. Shared-registry totals: Editor **11** · SimHost **4** ([Correction 30](UX_Tasks_Detail.md#corrections)) · ReplayBrowser **2** · **CGF 0**. 🔴 **9 of 21 ids have no handler anywhere**; 🔴 *Measurement Tool* is **already a dead click** in SimHost and CGF | `RW-M` | [UXR-90](UX_Requirements.md#uxr-90) | [design](UX_Feature_Map_Parity.md) · [UX_Map_Parity_Baseline.md](UX_Map_Parity_Baseline.md) *(inventory)* | ☑ |
| | <a id="uxi-24"></a>**UXI-24** 🔴 | ⚠ **Re-scoped 2026-08-13 — multi-select is BUILT, on one surface, and four separate pieces of it are unreachable.** The filed claim *"no ctrl/shift additive click-select exists anywhere"* is **false** ([Correction 31](UX_Tasks_Detail.md#corrections)): `EntityInspectorPanel.HandleRowClick` (`:410-437`) does ctrl-toggle **and** shift-range, unit-tested, and the panel already routes to the multi overload at `selCount > 1`. What is missing is the **map** half plus the handlers. 🔴 **Four dead seams found**: the modifier byte (`stateFlags`, bits 1-6 free, documented *"always 0"* for clicks) · `Vis2DInputMap.MultiSelectMod`/`BoxSelectMod` (**zero readers**, on **two** owners) · `IgApplication.OnCanvasWorldClick`'s shift+right-click fan-out (**zero production callers** — test hooks only) · `PushContextActions(…, forSelection, …)` (fed the whole id list while the menu beside it is built from `[0]`). 🔴 **And a flagship defect**: *"Mark Target for N Units"* can never show N > 1 — it is registered on the **single**-entity overload, so it hides at `selCount > 1`, and it reads a store whose only mutator collapses to one. 🔴 `ISelectionState` has **no additive operation** at all | `RW-M` | [UXR-91](UX_Requirements.md#uxr-91) | [UX_Feature_Multi_Select.md](UX_Feature_Multi_Select.md) | ✅ |
| **B** | | **Entity actions** | | | | |
| | <a id="uxi-03"></a>**UXI-03** | **No shared action vocabulary.** Identity, label and ordering are re-declared per host — **`Center` 9×, `Delete` 10×** (one cased `"DELETE"`). ⭐ **Re-scoped 2026-08-10: the mechanism already exists** (`SharedContextMenuPopulator` + `IEntityActionController`, unit-tested) and **one** subsystem uses it — the mapless one. Four measured blockers stop the rest; all four are what Q26-B's per-item registration removes | `RW-M` | [UXR-89](UX_Requirements.md#uxr-89) | [UX_Feature_Entity_Action_Vocabulary.md](UX_Feature_Entity_Action_Vocabulary.md) | ✅ |
| | <a id="uxi-04"></a>**UXI-04** | **The same entity offers different actions per surface** — inspector lambdas vs map JSON vs raw-ImGui ORBAT rows (all four ORBAT panels use raw ImGui). ⭐ **The ORBAT seam is already adopted by BOTH Editor and ExCon** (`IOrbatController`+`IOrbatDataProvider`); ExCon keeps its fork because `SharedOrbatPanel`'s whole menu is **one item**. 🔴 **SimHost and CGF emit no per-entity map menu at all** — one missing registrar call, but the fix is a registry-backed gizmo, not that call | `RW-M` | [UXR-85](UX_Requirements.md#uxr-85) | [UX_Feature_Cross_Surface_Actions.md](UX_Feature_Cross_Surface_Actions.md) | ✅ |
| | <a id="uxi-25"></a>**UXI-25** | **ExCon's 434-line ORBAT fork.** Split out of [UXI-04](#uxi-04) by the user, 2026-08-10. ⚠ **Not redundant — the shared panel is impoverished** (its whole menu is *Disembark*), so this is blocked until UXI-04 gives `SharedOrbatPanel` the shared menu. Then: bind ExCon's descriptors to `IOrbatController` and retire `OrbatPanel`. 🔒 ExCon is **DDS-only, no ECS world** — bind via the facade, never via `Entity` | `RW-M` | [UXR-85](UX_Requirements.md#uxr-85) | [UXI-04 §migration](UX_Feature_Cross_Surface_Actions.md#migration) | 🔒 |
| | <a id="uxi-05"></a>**UXI-05** | **The main menu is the only surface that does not follow focus.** Windows filter by `WindowScope.{Global,PerspectiveBound}` and the map follows `SwitchMapOwner` — the menu is a flat union. ⭐ **Copy `MainToolbarManager`'s filter, not `WindowScope`** — `string? Perspective`, `null` = global. 🔴 **The union is not the registry** (Editor is its only writer, 10 items) **but four copy-pasted `BeginMainMenuBar` blocks that never check focus** — so this **is [UXI-13](#uxi-13)** seen from the other side | `RW-L` | [UXR-86](UX_Requirements.md#uxr-86) | [UX_Feature_Menu_Follows_Focus.md](UX_Feature_Menu_Follows_Focus.md) | ✅ |
| | <a id="uxi-06"></a>**UXI-06** 🔴 | **The default perspective can be a non-perspective.** `defaultPersp` is an `ISubsystem.Name`; for `--mode all` that is **`Orchestrator`**, whose windows are all `Global`/empty-perspective — so **first launch hides all 22 perspective-bound windows** (SimHost 5 · IG 8 · ExCon 6 · CGF 3) and no `fdp_windows.json` ships to mask it. ⚠ **Re-scoped after user review: dropping `BTree`/`HSM`/`Blueprint` on restore is DESIRED** (document-driven, documents not persisted) — the design now makes that deliberate | `RW-L` | — | [UX_Feature_Perspective_Restore.md](UX_Feature_Perspective_Restore.md) | ✅ |
| | <a id="uxi-26"></a>**UXI-26** | ⊘ **REFUTED as filed** ([Corrections 21](UX_Tasks_Detail.md#corrections)). The claimed race does not exist: `NativeEventStream<T>.Write` is documented thread-safe, `Interlocked`-reserved and double-buffered, so publishing from a continuation is the **designed** path. ⚠ **What remains is narrower and unverified:** the handlers read the **live `EntityRepository`** off-tick rather than an `ISimulationView`, against Programmer's-Guide rule 7 — which is written for background *modules*, so its application to a UI continuation needs a ruling. ⇒ Addressed structurally by giving handlers a context exposing **only** snapshot + ECB + bus | `RW-L` | — | [UX_Interaction_API.md §1, §6](UX_Interaction_API.md#1--retracted--and-what-the-engine-actually-mandates) | ⊘ |
| | <a id="uxi-27"></a>**UXI-27** | **Activity progress surface — status bar + modal dialog.** Split out of the [interaction API](UX_Interaction_API.md#6c-progress-reporting--part-of-the-design-per-the-ruling) by the user, 2026-08-10: a long-running cancellable **must** show progress and a cancel — modal dialog when `Exclusive`, status-bar bar+icon otherwise. ⭐ Seam exists: `StatusBar.RegisterSection` (`LocalWindowController.cs:70-75`). Own design because it is a shared shell widget with its own concerns (ordering, concurrent activities, overflow) | `RW-M` | — | — · contract in [API §6c](UX_Interaction_API.md#6c-progress-reporting--part-of-the-design-per-the-ruling) | ☐ |
| **C** | | **Tools** | | | | |
| | <a id="uxi-07"></a>**UXI-07** 🔴 | **A tool is not a thing.** No abstraction, no current-tool state, **six** activation idioms — `Edit`/`Route`/`Rotate` each reachable by **two pipelines in one class**. 🔴 **Two exclusive-focus arbiters share one event bus with no arbitration**, so two "exclusive" tools can act on the same drag; exclusivity is also only per-entity. Toolbar cannot show active state *even in principle*; `Select` is dead; the enum names **four deleted classes**. ⚠ **The programme's first genuinely new abstraction — prior art is empty**, except the **modal tool stack**, which *was* implemented and deleted: `MapCanvas.PopTool` survives only as a **broken `<see cref>`** | `RW-M` | [UXR-81](UX_Requirements.md#uxr-81), [UXR-84](UX_Requirements.md#uxr-84) | [UX_Feature_Tool_Model.md](UX_Feature_Tool_Model.md) · [Q27 ✅answered](Architect_Question_27_Tool_Model.md) | ✅ |
| **D** | | **Layout** | | | | |
| | <a id="uxi-08"></a>**UXI-08** | **No shipped default layout.** ⭐ **Wider than filed: layout state lives in TWO files in TWO roots** — `imgui.ini` in `%LocalAppData%\HROT\` and `fdp_windows.json` **next to the exe** (open/closed, perspective, UI scale). Resetting one gives a **half-reset**. 🔴 The repo-root `imgui.ini` is **tracked *and* `.gitignore`d, never copied to output, never read** — an accident, not a default. Path logic duplicated in 2 shells | `RW-M` | [UXR-04](UX_Requirements.md#uxr-04) | [UX_Feature_Layout_Defaults.md](UX_Feature_Layout_Defaults.md) | ✅ |
| **E** | | **Map** | | | | |
| | <a id="uxi-09"></a>**UXI-09** 🔴 | **Camera setup copy-pasted 5×** ([Correction 23](UX_Tasks_Detail.md#corrections) — ReplayBrowser is the fifth), **every copy stale**, and nothing is occlusion-aware. 🔴 **Wider than filed:** two hosts centre on the **top-left pixel** (`Offset` never set); `Offset` is written **once at construction** in all five ⇒ resize decentres every map; and **perspective switch copies one subsystem's `Offset` into another** via `MapCameraView`. `DockspaceLayout` exists but sizes the dockspace *host* — no camera reads it | `RW-M` | [UXR-18](UX_Requirements.md#uxr-18) | [design](UX_Feature_Map_Viewport.md) | ☑ |
| | <a id="uxi-10"></a>**UXI-10** 🔴 | **Two symbology pipelines that never meet.** `StyleResolutionSystem` (278 L, 3-layer TKB→DDS→operator merge) computes tint, affiliation and damage every tick into `ResolvedStyle` — **read only by text UI**. The map hardcodes `Rgba32(100,220,255)` for every entity, so **friend and hostile are indistinguishable** while `ForceId`'s own doc says *"rendered as green/blue/red"*. `VisualData.MapShapeName` is authored, translated, and **read by nobody**; `GetShape`'s `shapeName` is always `null`. 🔴 CGF's shapes are **alpha 0**; CGF emits **no pick box**. ⚠ Filed wording refuted in detail ([Correction 24](UX_Tasks_Detail.md#corrections)) | `RW-M` | — | [design](UX_Feature_Entity_Symbology.md) | ☑ |
| | <a id="uxi-11"></a>**UXI-11** 🔴 | ⚠ **Re-scoped 2026-08-12 by [ruling 27](UX_RESUME_INTERACTION.md) — half of it was never a defect.** 🔒 **ExCon is correct as filed-wrong**: it has **no map**, is an ImGui-panel-only subsystem controlling IG/CGF/SimHost over DDS, so its selection is remote and separately stored *by design* ([Correction 27](UX_Tasks_Detail.md#corrections)). 🔴 **CGF is the real defect**: it must be **no different from SimHost or the Editor** — same visuals, same interaction, same ECS component. Today it runs **no `SelectionInteractionSystem`**, so no entity ever gets a `SelectionState` component and the ring gizmo cannot match. 🔴 **And a wider find: selection is stored TWICE everywhere** — the ECS `SelectionState` (written only by `SelectionInteractionSystem:170-172`, read by the ring) and a parallel in-memory `ISelectionState` (written directly by menu handlers, e.g. `EditorSubsystem.cs:1297`, `CgfSubsystem.cs:595`). **Nothing syncs them.** ⭐ Overlaps [UXI-23](#uxi-23) | `RW-M` | — | [design](UX_Feature_Selection.md) | ☑ |
| **F** | | **Duplication** | | | | |
| | <a id="uxi-12"></a>**UXI-12** | **Spawn UI ×4** — `SpawnerPanel`, `MiniExConPanel`, `SimHostSpawnPanel`, plus an inline combo in ExCon's ORBAT | `RW-M` | [UXR-83](UX_Requirements.md#uxr-83) | — | ☐ |
| | <a id="uxi-13"></a>**UXI-13** | **Gizmo main-menu-bar block copy-pasted ×4**, bypassing the overload built for it — `DebugGizmoLayer.DrawMainMenu()` exists and **only a standalone viewer calls it**. ⭐ **Same defect as [UXI-05](#uxi-05)**: these four blocks *are* the unfocused menu union. UXI-05 step 2 makes them correct; this makes them one | `RW-L` | — | — | ☐ |
| | <a id="uxi-14"></a>**UXI-14** | **`PanelConstants` copied verbatim** by ExCon; **`MapLayerBits`** hand-synced with a comment admitting it | `RW-L` | — | — | ☐ |
| | <a id="uxi-15"></a>**UXI-15** | **IG runs two entity inspectors at once** — its own 78 L and the shared 593 L | `RW-L` | — | — | ☐ |
| **G** | | **Correctness & robustness** | | | | |
| | <a id="uxi-16"></a>**UXI-16** 🔴 | **No `Delete` confirms, in any of the five hosts.** Every one fires immediately | `RW-L` | [UXR-15](UX_Requirements.md#uxr-15) | — | ☐ |
| | <a id="uxi-17"></a>**UXI-17** | **Two `async void` menu handlers** — unobserved exceptions | `RW-L` | — | — | ☐ |
| | <a id="uxi-18"></a>**UXI-18** | **Editor's JSON parser reads `children` without a `ValueKind` guard** — a non-array throws `InvalidOperationException`, which its `catch (JsonException)` does not catch | `RW-L` | — | — | ☐ |
| | <a id="uxi-19"></a>**UXI-19** 🔴 | ✅ **VERIFIED 2026-08-12 — the Editor draws every entity twice.** Projector keys differ by one component (`IgEntityPresentationGizmo` needs `CullingState`, SimHost's does not), matching is a **superset** test (`StatelessGizmoSystem.cs:104`), the Editor registers **both** (`EditorSubsystem.cs:1094-1097`), and `MapCullingSystem` gives **every** `SimTransform` entity a `CullingState`. ⚠ **It also defeats culling** — the SimHost gizmo ignores `IsVisible`. **Absorbed into [UXI-10](#uxi-10) §3.4** | `RW-L` | — | [design](UX_Feature_Entity_Symbology.md) | ☑ |

| **H** | | **Filed 2026-08-12 out of the UXI-10 analysis** | | | | |
| | <a id="uxi-28"></a>**UXI-28** 🔴 | **Five parallel visibility mechanisms, none connected.** (1) `IMapLayer.LayerBitIndex` vs `MapCanvas.ActiveLayerMask` gates whole layer objects — `DebugGizmoLayer` sits at bit 31 and nothing ever clears it; (2) `MapDisplayComponent.LayerMask` — 5 named layers computed per entity every 3 s by `MapLayerAssignmentSystem` and **read only by selection rings and picking, never by the symbol**; (3) `DebugPrimitive.DebugLayer` vs `LayerControlGizmo`'s `LayerMask256` — the **only** filter that reaches drawn primitives, using **3 of 256 bits**, silently all-visible where the gizmo is unregistered (**CGF**); (4) `CullingState` — the actual gate on the symbol; (5) `GlobalDebugSettings.DebugLayerMask` + `IGizmoVisibilityPolicy`, both deferred-dead. 🔴 **`SetLayerMask(ushort) { }` is an empty body** — the canvas mask has zero effect on primitives. **4 independent layer-definition sources.** ⭐ Same *resolve-then-discard* failure as [UXI-10](#uxi-10)'s tint. Folds in [UXI-14](#uxi-14)'s `MapLayerBits` duplication | `RW-M` | — | [decisions](Architect_Question_28_Map_Layers.md) · [design](UX_Feature_Map_Layers.md) | ☑ |
| | <a id="uxi-29"></a>**UXI-29** 🔴 | **No ownership-respecting pose-change path exists.** Spawn and Destroy have network command paths (`SpawnEntityCommand`, `DestroyEntityCommand`); **pose does not** — `EntityDragGizmo.cs:155` and `EntityRotatorGizmo.cs:118` both `GetComponentRW<SimTransform>` on the live repo. Per [ruling 22](UX_RESUME_INTERACTION.md), the Editor owns everything so direct write is correct there, but **CGF owns nothing** and wires *Rotate* anyway (`CgfSubsystem.cs:605`) ⇒ a write it has no authority for, overwritten by the next DDS sample. Its own *Delete* already does it correctly, by publishing `DestroyEntityCommand`. ⭐ **Re-scoped 2026-08-12: the channel exists** — `UpdateEntityDescriptorRequest(dtWorldPos)` is acked and owner-side authority-gated, and **IG already uses it**; plus a generic per-field-gated `UpdateEntityAttributeRequest`. 🔴 **4 gizmos write unguarded** (drag, rotator, vertex, route) **and `NetworkSpawningSystem.ProcessUpdate` applies updates with no authority check** | `RW-M` | — | [design](UX_Feature_Authority_Aware_Writes.md) | ☑ |

| | <a id="uxi-30"></a>**UXI-30** 🔴 | 🔴 **ENGINE DEFECT — the binary attribute path applies remote patches without checking authority.** `CanWrite<T>()`/`CanWriteManaged<T>()` are called **only** from `JsonAttributeCompiler` (`:40`, `:58`); `BinaryInterpreter.Apply` (`:102-128`) dispatches every `AttributeRecord` straight to its handler, with *"unknown IDs are silently skipped"* as the only filter. ⇒ **a node applies an unowned patch verbatim** — the JSON path's central safety property, absent on its binary twin. 🔒 **Ruled a severe error 2026-08-12**: *"no reason why the binary path should be different from the JSON path in this aspect."* ⭐ **Census: ZERO production senders of `AttributeRecords` today** — the fix breaks nothing, and this is a latent hole rather than a live one. **Prerequisite for [UXI-29](#uxi-29)**, which would be the path's first producer. ⚠ **WIDENED 2026-08-12 by [ruling 34](UX_RESUME_INTERACTION.md)** — the defect is not one path but the **absence of a consistent invariant**: of five appliers, **two gate on nothing** (binary records; `NetworkSpawningSystem.ProcessUpdate:162-175`), **two gate on the descriptor key** (`UpdateEntityDescriptorRequestSystem:142,190`) and **one on native component authority** (JSON). The geo one carries a `FIXME` naming the discrepancy. ⇒ **every applier gates, on one notion of authority**. 🔒 **[Ruling 35](UX_RESUME_INTERACTION.md) settles which: native component ownership** — the geo `FIXME` was right. 🔴 **Trap:** the sweep must **classify before touching** — **replication ingress translators write unowned components by design** (`GeoSpatialIngressTranslator:85-89` writes only when `!isLocallyOwned`); their gate is the **exact inverse** and must be preserved | `RW-M` | — | [UXI-29 §3.3b-c](UX_Feature_Authority_Aware_Writes.md) | ☐ |

| | <a id="uxi-31"></a>**UXI-31** | **IG's shift+right-click "append waypoint to every selected vehicle" is unreachable in production.** `IgApplication.OnCanvasWorldClick` (`:2067`) fans `CmdAppendPersonalWaypoint` over every entity carrying a selected `SelectionState` (`:2077-2109`) — a complete, correct multi-target order. Its **only** callers are `TestHook_SimulateShiftRightClick` (`:1798`) and `TestHook_SimulatePlainRightClick` (`:1808`). ⚠ The production sibling `OnCanvasClicked` **is** wired (`:772-776`) but is called with `shift: false, ctrl: false` at **all four** call sites, so the modifier branch is dead by construction. ⭐ **Same shape as [ruling 36](UX_RESUME_INTERACTION.md)'s continuous-drag find** — a finished feature behind a test-only door. 🔒 **Deliberately NOT folded into [UXI-24](#uxi-24)**: whether the operator *should* get shift+right-click-to-waypoint is a feature decision, not a multi-select one | `RW-L` | — | — | ☐ |

**Counts:** **31 issues** · **16 designed** (UXI-01..11 + 19 + 23 + 24 + 28 + 29) · 13 🔴 · UXI-26 ⊘ refuted · **UXI-19 verified and absorbed into UXI-10** · UXI-22 folded into UXI-23 · UXI-25 split out of UXI-04 · **UXI-28, UXI-29 filed 2026-08-12** out of the UXI-10 analysis · **UXI-31 filed 2026-08-13** out of the UXI-24 census.

## Dependency order

```
UXI-01 ──────────────────────────────────────  first, always: removes an editing trap
UXI-02 ──────────────────────────────────────  independent of UXI-07 after all
UXI-03 ───── UXI-04 ── UXI-06 ── UXI-05   ⚠ 06 BEFORE 05: menu items key on perspective ids
UXI-07 ──────────────────────────────────────  Q27 ✅ answered; A1 fixes the 🔴 outright IF the 3 picker adapters convert too
UXI-08, UXI-09, UXI-10, UXI-16, UXI-17, UXI-18  independent — any order
UXI-24 multi-select ── shapes UXI-03's API; UXI-11 (shared selection) precedes it
UXI-23 parity ──────── delivered BY UXI-03/04/07/11 + owns UXI-04's step 5 and its binding prerequisite
UXI-25 ExCon ORBAT ─── 🔒 blocked on UXI-04 (shared menu must exist before the fork can collapse)
UXI-12..15 ────────────────────────────────── after the seams exist
```

## Rules

1. **An issue references a feature design doc.** Detail lives in the design, not here — this register
   stays a register.
2. **No tasks are cut before the design is agreed.** That is the gate this layer exists to enforce.
3. **Evidence is code**, cited `file.cs:line`, and **re-derived before building** — the
   [Corrections table](UX_Tasks_Detail.md#corrections) has 11 rows, five of them our own claims.
4. ⚠ **A code comment describing future work is not evidence the work is pending.** Correction 11 came
   from reading `"populated in PACK2-E002"` as a live plan when that migration had already completed in
   a different shape. Check whether the work landed before planning around the comment.
5. **One design per session prompt**, summarised before moving on (user, 2026-08-10).
6b. 🔴 **Before claiming an engine-level defect, read the engine's rules.** `README.md` (§2 ECS, §2.4 ECB,
   §2.6 event bus) and [`docs/HROT-PROGRAMMERS-GUIDE.md`](../HROT-PROGRAMMERS-GUIDE.md) (rule 7:
   background code uses `ISimulationView` + `IEntityCommandBuffer`; §8.1: UI threading) document the
   invariants with `file:line` citations. [Correction 21](UX_Tasks_Detail.md#corrections) cost a false
   🔴 issue and a withdrawn design because this was skipped. ⚠ *Also: follow the call one level deeper —
   the synchronization was in the stream, not the dispatcher I grepped.*
6. ⭐ **Every design opens with a "Prior art" section** citing a row of the
   [Seam Inventory](UX_Seam_Inventory.md) — **including when the answer is "nothing exists"**. The prior
   is that the seam *already exists and is under-adopted*: eight instances this session, no
   counter-example. ⚠ A call-site scan cannot find an unadopted seam — that is what the inventory is for.
   Check the wrapper (`LambdaFoo`/`DefaultFoo`) before calling an `IFoo` unadopted.
