// <auto-generated />
// Asset: HsmTwoRegionParamsDemo (00000146-0000-0000-0000-000000000001)
// BlueprintId: 0x1434647B
// StructureHash: 0x3033B16023FA281A

using System;
using System.Runtime.CompilerServices;
using System.Runtime.InteropServices;
using System.Numerics;
using Fdp.Core;
using Fdp.Interfaces;
using Fdp.ModuleHost.Abstractions;
using Fdp.Toolkit.Blueprints;

namespace Hrot.AI.Behaviors.Generated;

public static class HsmTwoRegionParamsDemo_1434647B_Bp
{
    public const int BlueprintId = unchecked((int)0x1434647B);
    public const ulong StructureHash = 3473314764111357978UL;
    public static readonly global::System.Guid AssetId = new global::System.Guid("00000146-0000-0000-0000-000000000001");
    
    [global::System.Runtime.InteropServices.StructLayout(global::System.Runtime.InteropServices.LayoutKind.Sequential)]
    public struct Params
    {
        public int Value;
    }
    
    [global::System.Runtime.InteropServices.StructLayout(global::System.Runtime.InteropServices.LayoutKind.Explicit)]
    public struct WorkingState
    {
    }
    
    public static int StateSize => global::System.Runtime.CompilerServices.Unsafe.SizeOf<WorkingState>();
    
    private static unsafe void InitDefaultWorkingState(WorkingState* dst)
    {
        *dst = default;
    }
    
    [global::Fbt.Kernel.GeneratedAiPrimitiveAction(bTreeAction: false, bTreeCondition: false, hsmAction: true, hsmGuard: false, blueprintCall: false)]
    public static global::Fbt.NodeStatus TickCore(
        ref Params p,
        ref WorkingState ws,
        global::Fdp.Core.Entity self,
        global::Fdp.Core.EntityRepository world,
        float time)
    {
        return global::Fbt.NodeStatus.Success;
        
    }
    
    public static unsafe void HsmActivity(void* instance, void* context, global::Fhsm.Kernel.Data.HsmCommandWriter* writer)
    {
        var bridge = (global::Fdp.Toolkit.Behavior.Systems.HsmKernelBridge*)context;
        var world = (global::Fdp.Core.EntityRepository)global::System.Runtime.InteropServices.GCHandle.FromIntPtr(bridge->WorldHandle).Target!;
        
        // E3b (§28.7): the HOST's params region, so a resolver can read the host's own
        //   variables BY NAME through IHostVariableAccess — its first implementation.
        // CE-297: the SEED comes from HERE, not from the kernel's instance pointer.
        // P3-C: and 'here' is the entity's ROOT PARAMS SLOT — BrainBlackboard is retired.
        // CE-305: the EXTENT comes from the slot, never from MaxBehaviorParamByteSize. The
        //   constant was right while the region was a fixed byte[100]; since P3-C it is only
        //   RootParamsBytes(def) wide, so a 100-byte bound lets IHostVariableAccess read past
        //   the slot into the next occurrence (§29.10 — the ANCHOR was specified, the EXTENT
        //   never was).
        byte* __hostParams = global::Fdp.Toolkit.Behavior.RootParamsAccess.RequireRootBytes(
            world, bridge->Self, out int __hostParamsLen);
        
        // O7/E3: this occurrence's OWN params AND working state, keyed by the (region, state)
        //        the kernel stamped (O6) and by the hosting machine's id from the instance header.
        int occurrenceKey = global::Fdp.Toolkit.Behavior.HsmOccurrence.KeyFor(instance, AssetId, writer);
        ref var ws = ref global::Fdp.Toolkit.Behavior.HsmOccurrence.ResolveOrAttach<Params, WorkingState>(
            world, bridge->Self, occurrenceKey, StructureHash, out bool freshlyAttached, out Params* __params);
        if (freshlyAttached)
        {
            // E3a SEED (§28.4): the bytes this thunk read LIVE before params moved into the
            //   slot. Copying them makes the move byte-identical at the first dispatch.
            // E3b-0 (§28.6): …and WHICH bytes is the STATE's own binding, not always the
            //   first variable — that is what lets two parallel regions differ.
            int __seedOffset = global::Fdp.Toolkit.Behavior.HsmOccurrence.SeedParamsOffset(instance, writer);
            ref byte __rootParams = ref global::Fdp.Toolkit.Behavior.RootParamsAccess.RootRef(world, bridge->Self);
            *__params = global::System.Runtime.CompilerServices.Unsafe.As<byte, Params>(
                ref global::System.Runtime.CompilerServices.Unsafe.AddByteOffset(
                    ref __rootParams, (nint)__seedOffset));
            // C1′ (§28.7): the RESOLVE stage — the one place IHostVariableAccess is non-null.
            global::Fdp.Toolkit.Behavior.HostedParamResolvers.TryRun(
                AssetId, ref *__params, world, bridge->Self, global::Fdp.Toolkit.Behavior.HsmHostVariableAccess.For(instance, __hostParams, __hostParamsLen));
            InitDefaultWorkingState((WorkingState*)global::System.Runtime.CompilerServices.Unsafe.AsPointer(ref ws));
        }
        ref var p = ref *__params;
        TickCore(ref p, ref ws, bridge->Self, world, world.SimulationTime);
    }
    
}

[global::Fdp.Toolkit.Blueprints.Attributes.BlueprintRegistrar]
public static class BlueprintRegistrar_HsmTwoRegionParamsDemo_1434647B_Bp
{
    public static unsafe void Register(global::Fdp.Toolkit.Blueprints.BlueprintRegistryStaging staging)
    {
        staging.Add(HsmTwoRegionParamsDemo_1434647B_Bp.BlueprintId, new global::Fdp.Toolkit.Blueprints.BlueprintDefinition
        {
            Name = "HsmTwoRegionParamsDemo",
            Kind = global::Fdp.Toolkit.Blueprints.BlueprintDispatchKind.AiPrimitive,
            StructureHash = HsmTwoRegionParamsDemo_1434647B_Bp.StructureHash,
            StateSize = HsmTwoRegionParamsDemo_1434647B_Bp.StateSize,
            AssetId = new Guid("00000146-0000-0000-0000-000000000001"),
            StateClrType = typeof(HsmTwoRegionParamsDemo_1434647B_Bp.WorkingState),
        });
        global::Fhsm.Kernel.HsmActionDispatcher.RegisterAction(unchecked((ushort)HsmTwoRegionParamsDemo_1434647B_Bp.BlueprintId), (global::System.IntPtr)(delegate* <void*, void*, global::Fhsm.Kernel.Data.HsmCommandWriter*, void>)&HsmTwoRegionParamsDemo_1434647B_Bp.HsmActivity);
    }
}
