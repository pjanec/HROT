<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <OutputType>Library</OutputType>
    <TargetFramework>net8.0</TargetFramework>
    <ImplicitUsings>enable</ImplicitUsings>
    <Nullable>enable</Nullable>
    <AllowUnsafeBlocks>true</AllowUnsafeBlocks>
    <RootNamespace>Hrot.CGF</RootNamespace>
    <AssemblyName>Hrot.CGF</AssemblyName>
  </PropertyGroup>

  <ItemGroup>
    <AssemblyAttribute Include="System.Runtime.CompilerServices.InternalsVisibleToAttribute">
      <_Parameter1>Hrot.CGF.Tests</_Parameter1>
    </AssemblyAttribute>
    <AssemblyAttribute Include="System.Runtime.CompilerServices.InternalsVisibleToAttribute">
      <_Parameter1>Hrot.ClusterRunner</_Parameter1>
    </AssemblyAttribute>  </ItemGroup>

  <ItemGroup>
    <!-- Orchestration DDS types and IClusterStateHandler -->
    <ProjectReference Include="..\Hrot.Map.Common\Hrot.Map.Common.csproj" />
    <ProjectReference Include="..\Hrot.Network\Hrot.Network.csproj" />
    <ProjectReference Include="..\Hrot.NED\Hrot.NED.csproj" />
    <ProjectReference Include="..\Hrot.Common\Hrot.Common.csproj" />
    <!-- CycloneDDS transitive via DataModel, but explicit for DdsParticipant -->
    <ProjectReference Include="..\FDP\ExtDeps\FastCycloneDds\src\CycloneDDS.Runtime\CycloneDDS.Runtime.csproj" />
    <!-- FdpLog<T> + FdpEventBus -->
    <ProjectReference Include="..\FDP\Kernel\Fdp.Core\Fdp.Core.csproj" />    <!-- SwitchTimeModeDescriptorTranslator (CGF1-A.2, BATCH-09) -->
    <!-- Scenario serialization toolkit for ScenarioLoadClusterStateHandler (CGF1-S0307) -->
    <!-- Orchestration toolkit contracts -->
    <!-- CgfLogicPack: Brain-tier modules, executors, NetworkEntityMap -->  </ItemGroup>

</Project>
